---
description: Auto-curated learned facts and preferences for the `runtime` package (packages/runtime) and OCJS/replicad/assimpjs forks
globs:
  - "packages/runtime/**"
  - "kernels/**"
  - "repos/opencascade.js/**"
  - "repos/replicad/**"
  - "repos/assimpjs/**"
alwaysApply: false
---

# Learned: runtime

Auto-maintained by the `agents-memory-updater` subagent. New bullets are
capped at 200 chars and 12 bullets per section. The existing entries below
were harvested verbatim from `AGENTS.md` during the one-time context
cleanup; they may exceed the 200-char cap.

## Learned User Preferences

- `repos/opencascade.js` fork-isolation stance: zero outbound docs/research/, ../../docs/, or R# tokens; README persona-routed ≤200 lines; CI `fork-hygiene` job in `general.yml` blocks regressions.
- OCJS bindings PoC pattern: build a smaller symbol-subset YAML to validate the proposed approach with minimal-iteration builds before triggering the full multi-hour regeneration.
- Toolchain-parity changes (e.g. vendored LLVM/libcxx for libclang↔libc++ alignment) MUST validate via a FULL host build of the published downstream consumer (replicad-opencascadejs) emitted to a `src-vendored/` sibling of `src/` and diffed against the published symbol list before any Docker rebuild — minimal-yml PoCs miss linker-level regressions.
- Adding new C++ via `BUILTIN_ADDITIONAL_BIND_CODE` is a smell signalling the bindings codegen is missing the architecturally correct fix — try generic generator-layer fixes first before any manual binding.
- Pin GitHub/dependency versions to exact commit hashes for reproducibility; use `pnpm patch` for dependency patches, do not manually create patch files; when behavior regressed from something that previously worked, prefer config changes over code changes — find the regression; replicad source/replicad-opencascadejs edits in `repos/replicad` are in scope (close collaboration with upstream maintainer); fork remotes track upstream's primary branch — assimp uses `master` (not main), tinyusdz uses `release` (not main); push fork commits to the matching upstream branch name; never hand-write full SHAs from short forms — always run `git rev-parse <short>` to obtain the canonical full hash before pinning in CMakeLists/submodule configs (synthesised back-halves silently produce non-existent commits that break `FetchContent_Declare` at configure time); commit messages **and source code** on upstream/external repos must describe the change itself — never embed local research doc IDs (`R1`, `R5`, `R8`, etc.) in commit subjects, code comments, or python codegen rationale, since other contributors don't have access to those references (audit `bindings.py`/`generateBindings.py` for stray `R1`/`R6`-style cites when applying gap-analysis recommendations); the same rule applies to internal plan files, JSDoc, prose docs, and runtime source comments — never reference research codes like `Half-A R9 of blueprint v4` or "Finding 8" inline, only describe rationale (consumer-facing prose like "no client.connect() ceremony required" is also banned because it leaks internal narrative into user docs)
- Long OCJS builds (10-30+ min) use NX caching via `pnpm nx build ocjs` and dependent targets — user grants unlimited build time for sweeping bindings work, leave them unattended.
- Module-scope options pattern: options "have no ownership" — live as module-level constants/exports, never owned by an instance; dogfood across runtime call-sites so breakage fails loudly.
- PoC isolation rule: when validating against a downstream consumer (e.g. replicad), reproduce the consumer's calls **inside** the PoC instead of importing it — keeps the experiment auditable.
- Use `using` declaration syntax everywhere GC is needed in OCJS PoC/sample code — for brevity and to fail loud via the `require-using-on-disposable` oxlint rule.
- esbuild plugins: prefer built-in loaders (text/binary/base64/dataurl/file) and TC39 `with` import attrs over bespoke VFS plugins; keep `?raw` handling generic so new query params slot in.
- When OCJS Docker and host dep versions diverge (libclang/python), roll Docker FORWARD to match host — both paths must pin identical versions so bindings match byte-for-byte.
- OCJS Docker builds must be a thin layer over the host build pathway — centralized `DEPS.json` pins shared by Docker and host; never reinvent/redeclare toolchain deps separately in the Dockerfile.
- Build/cache error messages must be path-agnostic with an explicit action — never embed transient filenames (e.g. `pch.h`) that may be repro-irrelevant by the time a user reads the log.
- OCJS bindings filter: NEVER strip single/multi-threading symbols even when currently unused — future multi-threaded builds need them; audit docs must explicitly carve those symbols out from removal.
- When extracting domain logic into standalone packages, invert dependencies — the domain package owns its types/schemas, consumer packages depend on it (not the reverse); derive computed values from canonical sources rather than duplicating data across schemas; for runtime/kernel manifests authored as Zod, convert to JSON Schema during worker-side startup discovery so Zod instances never cross `postMessage` to the main thread; no backwards compatibility or deprecation paths for unreleased/internal APIs — roll forward with breaking changes (after a Netlify-style cutover, also strip all stale comments referring to the old approach so the codebase doesn't carry historical artifacts); do not add shims or adapters for consumers that don't exist yet; library-api-policy: no `void`-returning Promise-typed methods (always `Promise<void>`), no IIFE-just-to-consume-a-thenable patterns (`void (async () => …)()` is banned), no `await Promise.resolve(x)` where the awaited value is already settled (use direct value/rebind), no thin re-export barrels in `libs/`/`packages/` `index.ts` — consumers must import each symbol directly from its source module so deep imports stay legible (the rule is enforced workspace-wide and each package has explicit `exports` map subpaths rather than a single `.` aggregator); kernels must be fully self-contained (no shared cross-kernel schemas/types like `occt-shared-schemas` — each kernel duplicates its schemas to remain independently extractable); kernels treat a `main()` that returns `undefined` (no return statement or explicit `return undefined`) as a successful no-op with an empty `issues` array — never emit warning issues for missing returns (tests assert `issues.length === 0`, not "warning when undefined"); appearance/material props (color, opacity, metalness, roughness, density) live as flat top-level keys on shape return values — never nested under an `appearance` key — for parity with Zoo's per-shape `appearance()` and to enable future engineering queries (e.g. stress under load via density); `opacity` is preferred over `alpha` (more descriptive, aligned with PBR conventions); `defineKernel`/`defineTranscoder` author APIs accept Zod schemas only — JSON Schema and defaults are resolved internally so authors don't repeat themselves; schema-driven runtime APIs must use generic inference end-to-end (kernel formats, transcoder edges, `client.export` options) — no `Record<string, unknown>` escape hatches when a schema can drive the type; `RuntimeClient` API splits intent: `client.render()` is for autonomous-mode rendering only (UI panes, may be superseded by newer renders) and resolves to `RenderOutcome` (a discriminated union — supersession is a typed branch, not an exception; `NoRenderOutcomeError` surfaces when no render has settled yet — names chosen over the earlier `RenderSettlement`/`NoRenderResultError` for legibility), while `client.export(format, ?input)` is for on-demand use (CLI, testing, headless export) — the format-only overload uses default export options for the format and the no-input overload reuses the kernel's most-recent native handle (a runtime invariant after every successful render) for near-instant live-pane exports; the prior `setFile`/`setParameters` lower-level methods were renamed to `openFile`/`updateParameters` to make intent explicit and avoid ambiguous semantics; bundled transports expose **`TransportPlugin` callables** (`webWorkerTransport({...})`, `electronUtilityTransport({...})`, `inProcessTransport({...})`); `createRuntimeClient` materializes the plugin once during construction (**`transportPlugin.materialize()`**) — same shape as kernels/middleware plugins — runs **`terminate()` → `transport.close()`** unconditionally; **`connect()` stays zero-arg** and runs **`handle.open()`** / **`workerClient.initialize(...)`**; `RuntimeReconnectError`/`KernelRuntimeMismatchError`/`BundlerOwnershipConflictError`/`ConnectOptions` and the second-call mismatch detection were all deleted (no reconnect, no `runner`/`fileSystem`/`sharedMemory` fields on client options); `defineRuntimeTransport` is the canonical primitive — each transport implementation solely owns its own performance-improving capabilities (SAB, transferable, sharedAbort) and the runtime _core_ never reads wire-level capability bits (would violate library-api-policy antipattern §22); transports own their default worker URL internally (`webWorkerTransport({ fileSystem })` resolves `new URL('@taucad/runtime/worker/web', import.meta.url)` inside the transport package, not at the consumer site) — consumer-site `new URL(...)` chunks pulled all kernels/middleware/transcoders into the module graph and produced circular deps that hung Vite/Rolldown production builds (works in `nx dev`, hangs in `nx build`/`nx serve`), so consumer-side URLs are reserved for the niche multi-bundler override case via an optional `url` field; transport plugin authoring follows the same `defineX → XPlugin` shape as `defineKernel`/`defineTranscoder` (factory + plugin collapsed inside `defineRuntimeTransport`, no separate `.client()`/`.host()` exposed to the consumer — `createRuntimeClient`/`createRuntimeHost` materialise the right side internally); event subscription stays on the unified `client.on(event, handler)` (rejected per-event `client.onParametersResolved`/`onProgress`/etc. — the unified API gives type-exhaustive coverage when new events are added); `parametersResolved` and `exported` notifications were collapsed into `render`/`export` call results in the channel model, so they no longer exist as separate notifies (autonomous renders still emit notify events for live UI updates); `terminate()` must resolve cleanly with no hanging promises; runtime errors expose typed error codes for programmatic handling — never assert via string-matching the error message (string-match guards in `RuntimeClient` are a smell to remediate with a code-based discriminator); test helpers internally call `client.export` (not `client.render`) to align tests with the same serialised bytes consumers see; `inProcessTransport({...})` auto-defaults `fileSystem` to `fromMemoryFs()` when none is supplied so headless/test consumers (`createNodeClient` + `replicad()` + `esbuild()`) keep working zero-config without wiring a transport+filesystem pair — without this default tests fail with `filesystem not available - initialize must complete first with fileSystemPort`; the descriptor records whether the FS is auto-defaulted vs user-supplied so explicit `fromNodeFs()` / `fromMemoryFs()` overrides still win
- OCJS BREAKING_CHANGES.md scope: v2 (last public release `2.0.0-beta`) → v3 delta only; drop upstream/1.1.x references and intermediate 'supersedes' notes; retain full CHANGELOG history.
- When scrubbing local research/recommendation codes (`R7` etc.) from OCJS sources, retain intent via a concise descriptive comment only when valuable — never leave bare cite-style references.

## Learned Workspace Facts

- OCCT `BRepGraph` is normalized B-Rep storage, not an intermediate/feature-tree cache; its internal caches are not plumbed into `ModelingAlgorithms`.
- OCJS Docker LLVM trimming must keep all `include/`; Linux libc++ stores `__config_site` under target-triple paths, so `include/c++` only breaks bindgen.
- Never end OCJS Docker mega-RUN chains with `|| true`; bash masks failed `cmd1 && ...` chains as success, yielding false-green generated images.
- `@taucad/opencascade.js` has no runtime deps or `ws` peer; ship `LGPL-2.1-only WITH Open-CASCADE-Exception-1.0` plus `LICENSE.OCCT-Exception`.
- `repos/opencascade.js/docs-site` is the canonical OCJS Fumadocs/Next.js documentation site; legacy Alpine viewer archived under `docs-site/legacy-viewer/`; CI `fork-hygiene` rules apply.
- replicad-opencascadejs migrated `additionalCppCode` (inline string) → `additionalCppFiles` (separate .cpp files) in `build-config/custom_build_single*.yml` for separation of concerns.
- OCJS multi-threaded WASM variant: `build-configs/full_multi.yml` config emits `opencascade_full_multi.*` to `dist/`; single vs multi-thread perf tracked in `repos/opencascade.js/BENCHMARKS.md`.
- OCJS ships only `Symbol.dispose` (no `Symbol.asyncDispose`) — docs and samples must NEVER reference `await using`/`asyncDispose`, even with a fallback note; `DisposableStack` is the canonical way to stack disposals.
- External repos in `repos/` managed via `repos.yaml` and `pnpm repos`; gitignored and cursorignored; add to `.oxlintrc.json` ignorePatterns; assimp architectural rule: exporters must NOT carry per-source-format mapping — importers declare units/coordinate-system as scene metadata so each exporter applies transformations to its own spec (GLB is Y-up/meter spec, 3MF is Z-up/mm spec; spec-compliant GLB input is a hard precondition — non-spec-compliant GLB = user/dev error); Tau `@taucad/converter` loaders/exporters do NO geometry computation — all coordinate-system/scaling transforms live inside assimp importers/exporters via scene metadata (`AI_METADATA_UP_AXIS` aligned with FBX convention, accepted as overlap), exporters apply only the minimal transform needed at the spec edge (no import-side post-processing), and the contract is validated via round-trip import→export geometry preservation tests across all format pairs; `repos/opencascade.js` is a fork (`taucad/opencascade.js` from `donalffons/opencascade.js`) and is published to npm as `@taucad/opencascade.js` (currently at 3.0.0-beta.x preview tagged `beta` — replicad-specific files are stripped before `npm pack`; v3 ships consolidated `BREAKING_CHANGES.md` + `CHANGELOG.md` + `README.md` at the package root that supersede `repos/opencascade.js/docs/occt-v8-migration.md`; the package-renaming history is intentionally omitted from the changelog because the fork intends to merge back upstream); OCJS tests are TS-only (pytest dropped — 30-min build acceptable to keep parity with all other OCJS tests); `repos/assimpjs` is a fork (`taucad/assimpjs` from `kovacsv/assimpjs`) published to npm as `@taucad/assimpjs` and consumed via pnpm alias (`assimpjs: npm:@taucad/assimpjs@<version>`); `repos/assimpjs/assimp` is a fork (`taucad/assimp` from `assimp/assimp`, upstream branch `master`); `repos/assimpjs/assimp/contrib/tinyusdz` tracks `taucad/tinyusdz` from `lighttransport/tinyusdz` (upstream branch `release`, not `main`); tinyusdz upstream PR target is `dev` (not `release`) — all CI workflows (`linux_ci.yml`, `macos_ci.yml`, `windows_ci.yml`, `codeql-analysis.yml`) only trigger on `pull_request: branches: [ dev ]`; tinyusdz is C++14, **no C++ exceptions** (uses `nonstd::expected` instead), dependency-free (no codegen), Google clang-format 2-space indent (run `clang-format -i` before commit); every PR auto-reviewed by upstream `claude-code-review` bot — disclose AI co-authorship explicitly in PR body; USDZ output validated via `usdchecker --arkit` (Apple USD Tools) — Sdr-compliance violations like `MismatchedPropertyType` for `UsdUVTexture::st` (expected `float2`, got `texcoord2f`) reproduce against synthetic minimal USDA fixtures; pxrUSD's own `usdcat` canonicalises booleans as `1`/`0` (not `true`/`false`) — both forms are spec-valid; lib3mf v2.4.1 integrated via `Lib3MFBridge` for 3MF import/export (3MF is now a fully-wired native export option — the prior `export3mf` runtime util was removed in favor of the unified converter transcoder pipeline); three WASM variants: mini (import-only), all (full import+lib3mf), exporter (export+lib3mf); assimpjs `ConvertFileList` accepts optional `options` third param for per-format export options (e.g., 3MF units, slicer metadata); silencing assimp/tinyusdz debug logs (e.g. tinyusdz USD exporter chatter visible from CLI exports) requires editing C++ sources, rebuilding the WASM variant, and relinking via the `dist→replicad-opencascadejs/src → npm pack → tarball install` flow — runtime suppression is not possible post-build; `-O4` wasm-opt pass applied to exporter WASM variant for size optimization; 3MF axis/unit transforms propagated via assimp `ExportProperties` API (`3MF_EXPORT_UPAXIS`, `3MF_EXPORT_UNIT`) read directly by `Lib3MFBridge.cpp` — distinct mechanism from scene-metadata-driven exporters (`gltf`, `fbx`, `dae`, `obj`, `ply`, `stl`, `3ds`, `x`, `x3d`, `usda`, `usdz`) which use `bakeContractTransformIntoMeshes` to apply `AI_METADATA_UP_AXIS` + `AI_METADATA_UNIT_SCALE_TO_METERS` at the spec edge; `step`, `m3d`, `pbrt`, `assbin`, `assxml`, `assjson` have no axis-transform support; `@taucad/converter` `exportFromGlb(glbData, outputFormat, exportProperties?, sceneMetadata?)` forwards both per-format ExportProperties and aiScene metadata into `assimpjs.ConvertFileList`; `repos/emscripten` is a shallow reference clone (not a fork — no `fork:` field in `repos.yaml`); per-build emsdk scoping: assimpjs WASM uses `repos/assimpjs/emsdk`, OCJS/replicad WASM uses `repos/opencascade.js/deps/emsdk` — never cross-mount these (build flag/env mismatches break `__cpp_exception` linkage); `repos/opencascade.js` WASM build uses `BUILTIN_ADDITIONAL_BIND_CODE` (Python layer), full builds (10-30+ min) use `nohup`; `bindings.py` has `_classify_js_type`/`_build_dispatch_tree`/`_codegen_dispatch_tree` for val-based constructor/method overload dispatch, `processEnum` uses `enum_value_type::string` (string enums); `bindings.py` `needsWrapper` only wraps builtInTypes/enums/C-string `T&` via `getReferenceValue`/`updateReferenceValue` + `emscripten::val` (`{current: value}` pattern); `Handle<T>&` output params use unified return-by-value via `emscripten::value_object` (returns properties as JS object instead of mutating `{current}` refs); OCCT convention: `const handle<T>&` = input, non-const `handle<T>&` = output (never bidirectional); `const` on method guarantees `T&` params are output-only; suffix-free symbol generation removes `_N` overload subclasses; `shouldStripParam` unconditionally strips primitive output params (`double&`, `int&`) from JS signatures — callers no longer pass ceremonial `0` args; NCollection auto-discovery generates `using` declarations replacing deprecated `_SAFE_DEPRECATED_PREFIXES`; `bindings.py` emits explicit type defs for runtime methods requested via `sEXPORTED_RUNTIME_METHODS` (e.g. `HEAP32`, `HEAPU8`, `HEAPF32`) so consumers gain full type safety on direct WASM heap reads; namespace wrapping in generated `.d.ts` removed — fix type errors in the python codegen layer rather than adopting `--emit-tsd`; `dts-validation.test.ts` tracks `.d.ts` `any` count to prevent type resolution regressions; build system uses `build-cache.py` keyed on `OCJS_EXCEPTIONS`/`OCJS_SIMD` + other env vars; when build flags change, PCH must be rebuilt (`./build-wasm.sh pch`) and stale cache entries cleared from `repos/opencascade.js/cache/`; `OCJS_EXCEPTIONS=1` enables `-fwasm-exceptions` (Node.js supports `WebAssembly.Exception` — the real constraint is all `.o` files AND the linker must consistently use `-fwasm-exceptions`, otherwise `__cpp_exception` becomes an unresolved import); `OCJS_SIMD=1` enables `-msimd128` (Safari-compatible baseline SIMD); set `OCJS_RELAXED_SIMD=1` alongside it to additionally emit `-mrelaxed-simd` (Chrome/Firefox-only at time of writing — not supported by Safari 26.x, see `docs/research/safari-wasm-relaxed-simd-incompatibility.md`); `OCJS_BIGINT=1` enables `-sWASM_BIGINT=1`; `OCJS_EVAL_CTORS_LEVEL` controls `-sEVAL_CTORS=N`; `build-flags.json` manifest validates compile-time flag consistency across cached stages; `build-manifest.json` stores symbol validation results (compiled count, missing symbols, WASM/JS sizes); `build-wasm.sh` uses MD5 hash-based patch freshness detection; `DEPS.json` `commit` field enables stamp-based dep version checks (doxygen, consistent with occt/rapidjson/freetype); OCJS canonical build presets live in `repos/opencascade.js/build-configs/configurations.json` (named, e.g. `O3-wasm-exc-simd`) and pair with a source YAML — `full-exceptions.yml` for the published `@taucad/opencascade.js` full WASM, `replicad_single.yml` (or `custom_build_single_v8.yml` in replicad-opencascadejs) for replicad's WASM; replicad rebuilds must match the perf flags recorded in `repos/replicad/packages/replicad-opencascadejs/build-config/replicad_single.provenance.json` (the canonical source-of-truth — also embedded in the published tgz at `tarballs/package/src/replicad_single.provenance.json` after install); WASM experiments defined in `scripts/experiments/*.yml`, orchestrated by `scripts/wasm-experiment.sh`, artifacts stored in `tarballs/experiments/` with `provenance.json` metadata; benchmark CLI `--wasm-variant single-exceptions` maps to `replicad_with_exceptions.*`, `--wasm-variant single` maps to `replicad_single.*`; both root `package.json` and `packages/runtime/package.json` must reference consistent replicad/replicad-opencascadejs tarballs — version mismatch causes `UMin` runtime errors; NX `build` target depends on `validate` (runs `validate-build.py` → `<variant>.build-manifest.json`) and `provenance` (runs `provenance.py init` + `finalize` → `<variant>.provenance.json`); use `pnpm nx build ocjs` to get both artifacts in one pass (separate `validate`/`provenance` invocations cause NX `link` cache to restore `dist/`, wiping prior step's output); replicad WASM single-variant v8 config at `repos/replicad/packages/replicad-opencascadejs/build-config/custom_build_single_v8.yml`; WASM rebuild integration into Tau: copy dist→replicad-opencascadejs/src, `npm pack`, move tarball to `tarballs/`, `pnpm install`, copy-assets via `copy-files-from-to.cjson`, then run `packages/runtime` tests; `pnpm patch` failure mode: stale `node_modules/.pnpm/*patch_hash*` directories can persist with intact `patch_hash` markers but unmodified file contents after a botched install — symptom is cascading TS2835 errors (e.g. `@taucad/kcl-wasm-lib`, `@langchain/openai`) from missing patched type declarations; recover by removing the stale `*patch_hash*` dirs and re-running `pnpm install` so the patch is re-applied; replicad PBR property is named `metalness` (not `metallic`) — `repos/replicad/packages/replicad/src/export/assemblyExporter.ts` was renamed in v8.56 to align with `ShapeConfig.metalness` consumed by the Tau replicad kernel; STEP PBR uses XCAF Levels 1-3 (color, full PBR material constants including metalness/roughness/opacity, and density) round-tripped through OCCT — replicad-opencascadejs is built with the XCAF symbols required for the C++ glTF writer to accept material options directly (no JS-side post-processing) and replicad/opencascade kernels both pass appearance/density flat-keys straight through without per-format branching; after the WASM/source rebuild, regenerate Monaco typings via `pnpm nx extract-replicad api-extractor` so `libs/api-extractor/src/generated/replicad/*` reflects the renamed field; `repos/ForgeCAD` from `KoStard/ForgeCAD` (fork `rifont/ForgeCAD`) for AI-native CAD exploration; `repos.yaml` supports optional `commit` field for pinning repos to specific commit hashes (`pnpm repos add -c <hash>`, `pnpm repos sync` honors pinned commits); `repos/vscode` used as architectural reference for FS events, IPC, caching, and binary transfer patterns; OpenCASCADE STEP export via XCAF MUST call `STEPCAFControl_Writer.Perform(document, filePath, progress)` — the alternate `Transfer(document, '', progress)` overload silently activates multi-file mode when the filename is the empty string and emits an empty STEP body (was the smoking-gun bug behind the opencascade kernel STEP export regression); volume-parity round-trip tests read the bytes back via `STEPControl_Reader → TransferRoots → OneShape → BRepGProp.VolumeProperties → props.Mass()` (e.g. 6000 mm³ for a 10 mm cube, 9000 mm³ for the 10³+20³ assembly) rather than asserting on serialised text; the published `@taucad/opencascade.js` `full-exceptions` build (and every preset in `build-configs/configurations.json`) compiles to **wasm32** — the smoking guns are `MAXIMUM_MEMORY=4GB` (the wasm32 ceiling, 2³² bytes) plus the absence of `-sMEMORY64=1` anywhere in the tree, while `-sWASM_BIGINT` is unrelated to memory64 (only legalises i64 boundaries as JS BigInt vs i32 pairs); a wasm64 variant would require adding `-sMEMORY64=1` and rebuilding every dep including freetype, plus a wasm-bigint-aware glue layer; binding-codegen (`bindings.py`/`buildFromYaml.py`) MUST treat missing `typedef` resolutions as fatal with a developer-facing diagnostic that names the missing typedef and suggests the YAML/`bindgen-filters.yaml` change required to fix it — silent fallbacks (resolve-to-`any`, resolve-to-inner-element-type, etc.) regress consumer type safety and hide upstream config errors, so the codegen must throw at generation time rather than degrade output; whenever a manual binding (`BUILTIN_ADDITIONAL_BIND_CODE` + `builtin-bindings.d.ts` entry, e.g. the historical `FairCurve_Batten/MinimalVariation::Compute` free-function wrappers) can be replaced by the generic auto-discovery + output-param-strip pipeline, prefer removing the manual binding (drop the YAML filter, drop the `.d.ts` declaration, regenerate, and rewrite tests against the auto-generated instance method signature including OCCT's per-class output-param naming like `Code` vs `ACode`) — the goal is zero or minimal manual bindings layered on top of the generator so upstream OCCT churn doesn't break maintenance; OCCT V8.0.0 final release lands at upstream commit `d3056ef80c9668f395da40f5fd7be186cae4501f` (final 8.0.0 planned May 7, 2026); migration path RC4 → RC5 → Beta1 → Final tracked in `docs/research/occt-v8-{rc5-to-release-migration,final-migration-stocktake,final-migration-stocktake-2,final-migration-stocktake-3}.md` (stocktake docs serve as canonical blueprints with no open questions — answer outstanding questions inline before declaring a stocktake done rather than leaving "resolve during implementation" markers); the V8 series introduces BRepGraph (graph-based BRep representation, MUST be bound), the NCollection size_t API migration (#1212), modern differential properties packages (`Geom2dProp`/`GeomProp`), and Gordon surface construction (`GeomFill_Gordon`/`GeomFill_GordonBuilder`); non-CTOR classes like `GeomBndLib`, `ExtremaPC`, `BRepGraph`, `NCollection_IncAllocator`, `BRepMesh_FaceChecker` get bound via a generic non-CTOR class pattern (static-method/utility-class binder shape) so API surface coverage is maximised without per-class manual registration; the V8 typedef-cache regression where `getTypedefedTemplateTypeAsString` collapsed `occ::handle<NCollection_BaseAllocator>` to `int` is the smoking gun — upstream `Standard_Handle.hxx` introduced an `occ::` namespace alias that collided with template-typedef dictionary key normalisation between `occ::` and `opencascade::`; the architectural fix is to normalise both forms in the typedef cache (recovered ~702 of ~2950 stray `any`s, ~50% of the regression). OCJS python build environment is pinned via **UV** (`uv venv` + `uv pip install`) on both host and Docker (replacing deadsnakes PPA and `python -m venv` — UV is the May-2026 best-practice for cross-platform Python toolchain pinning); reproducibility wins over leveraging emsdk's pre-existing interpreter, and host + Docker must pin to identical UV-resolved versions so bindgen outputs match byte-for-byte. `OCJS_STRICT_TYPES=1` is enabled in the Docker bindings build so new `unknown`/missing-typedef regressions surface loudly at codegen time rather than silently shipping degraded types to consumers. The non-exceptions WASM build was dropped entirely from both `@taucad/opencascade.js` and `replicad-opencascadejs` (only `*-exceptions` configs ship now — `OCJS_EXCEPTIONS=1` is de-facto required, the size delta from `-fwasm-exceptions` is small enough that maintaining two configs isn't worth it); `replicad-opencascadejs` ships only `wasm`/`js`/`js.symbols`/`provenance.json`/`build-manifest.json` for the exceptions variant, and the replicad kernel's `single-exceptions` variant was renamed to `single`. The bindings-generator deduplication step that suppressed duplicate struct registrations was removed in favour of proper TU-level sharing (the `IntTools_Context::UVBounds` smoke-test reproducer hits a missing-symbol RBV failure when the dedup step skipped `IntTools_Context` registration); symbols list in OCC config is unchanged — the fix is structural in the binding generator, not per-symbol filtering. OCCT deprecation handling uses **"replacement registrations"** at both yml + `Common.py` levels — the modern replacement is registered for OCJS so the library can "forget" deprecations exist, paired with auto-generated `using` declarations replacing deprecated typedefs; `_SAFE_DEPRECATED_PREFIXES` and its derivative logic are completely removed (no JS-only deprecation shims — codemods handle migration of consumer code instead). NCollection container types are bound via auto-discovery + a generic `using`-declaration generator (NCollections are the explicit exception to the "no domain-specific patterns" rule; never add manual mappings like `BVH` or per-class hardcoded NCollection variants — every other type stays in the generic algorithm); `_resolve_stl_type` resolves `vector<T>` to `T[]` and concrete-length tuples like `[T, T]` for 2D vectors when the array length is statically known from the C++ template parameters; canonical templated strings are intercepted via libclang AST walks (not string-matching on `<` substrings, which is brittle and was rejected in review). `tarballs/opencascade.js-3.0.0-beta.<commit-hash>.tgz` is the OCJS V3 beta tarball naming convention; `replicad`/`replicad-opencascadejs` are repacked together at `0.21.0-v8.40` (lockstep version bump matched to the OCCT branch). Builds use `python -m ocjs_bindgen --filter <ClassName>` for single-class bindgen reproducers when isolating type-resolution regressions. OCJS RBV blueprint (`docs/research/ocjs-unified-rbv-blueprint.md` + the P0+P1 stocktake `ocjs-rbv-blueprint-p0-p1-stocktake.md`) wires return-by-value containers with an auto-emitted `[Symbol.dispose](): void` method ONLY when at least one of the container's child fields is itself a disposable C++ class (pure-data containers stay free of `Symbol.dispose` so consumers can use them without `using`/`await using` ceremony); the container's disposer is a shared `EM_JS` callback that iterates the disposable members and invokes each child's `[Symbol.dispose]()` — chosen over per-instance JS proxy wrapping because it is CSP-safe, Node.js 24 compatible, keeps memory overhead near-zero, and avoids V8's rejection of `using` on certain val::object shapes; the custom oxlint rule at `libs/oxlint/src/rules/require-using-on-disposable.js` enforces `using`/`await using` declaration syntax on every binding return-value whose type implements `[Symbol.dispose]` so disposable leaks fail lint before they ship. "Input-passthrough RBV" (Option B in the blueprint) is the canonical fix for the primitive in+out parameter class-of-bug exemplified by `gp_Trsf::Transforms(double&, double&, double&)` — the prior `_emitOutputParamBinding` default-constructed primitive outputs and silently produced zero-input transforms that propagated through replicad/other consumers; the fix unifies semantics so callers pass the input value, receive an updated copy back through a `result` object, and the bindings generator emits `optional_override` lambda fan-out (`k+1` overloads where `k` is the trailing-optional run length) so calls like `surface.Bounds()` and `BRep_Tool.Curve(edge, loc)` no longer throw `BindingError` at runtime.
- OCJS WASM allocator PoC: `mimalloc` beats `dlmalloc` by ~8% real-work geomean, `emmalloc` slower — mimalloc is the chosen variant for benchmarks; PoC uses minimal-yml builds for fast iteration.
- OCJS build-config naming convention: `default` → `single-threaded`, `O0-debug` → `debug`, `Os-noLTO-simd` → `single-threaded-smallest`, `O3-multi-threaded` → `multi-threaded`.
- All OCJS configs ship with `OCJS_EXCEPTIONS=1`, `EVAL_CTORS=true`+`EVAL_CTORS_LEVEL=2`, closure compiler on; `O3-wasm-exc-simd` and `O3-noLTO-simd` dropped (superseded by `single-threaded`).
- OCJS `OCJS_STRICT_TYPES` defaults to OFF; bindgen logs warnings when typedef resolutions are missing instead of failing, so degraded types are visible without blocking builds.
- OCJS WASM exposed via `@taucad/opencascade.js/wasm` subpath export — consumers do `import wasmUrl from '@taucad/opencascade.js/wasm?url'`; deep dist/* paths are not part of the package surface.
- OCJS PCH stale-mtime class-of-bug (clang/emcc embeds `pch.h` mtime inside `pch.h.pch`, every Nx/Docker cache restore rewrites the on-disk mtime and breaks the embedded check) is eliminated architecturally by adding `-Xclang -fno-pch-timestamp` to the emcc PCH build in `paths.py` (industry-standard flag from ccache/Chromium/Bazel — content hashes still validate header changes); pair it with adding `generatorCode` to `pch.inputs` in `project.json` so future PCH-build code edits invalidate the Nx cache, and the regression-guard error message must be path-agnostic with an explicit action ("ensure buildPch passes -Xclang -fno-pch-timestamp to emcc") instead of referencing transient `pch.h`/`pch.h.pch` filenames.
- OCJS canonical multi-tool boolean uses `BRepAlgoAPI_BuilderAlgo` over chained `BRepAlgoAPI_Fuse` — ~5.66× speedup; `samples.{mjs,py,cpp}` default to it; native-LTO is the parity reference.
- OCJS bindgen + post-link validators must share a **single `manifest_registry` module** — every symbol-resolution mechanism (direct `.cpp.o` compilation, NCollection typedef alias via `ncollection-manifest.json`, BUILTIN_ADDITIONAL_BIND_CODE / consumer additionalBindCode via `additional-bind-symbols.json`) needs exactly one loader so the link-time `yaml_build.verifyBindings` view and the post-link `scripts/validate-build.py` view of "missing" can never disagree; consumers never re-parse C++, never run regex on source, never re-derive set-difference math. Phase 2 of the producer/consumer manifest contract uses **libclang AST extraction** (`ast/parse.py::parse_additional_bind_code` + `ast/walker.py::extract_class_registrations`) — Embind/em_js/val stubs in `ast/parse_stubs/emscripten/` shadow the real headers so libclang 18 can extract `class_<T>("Name")` CALL_EXPRs without needing emsdk's libcxx-23 (which requires Clang 19+ builtins). `OCJS_STRICT_VERIFY` is removed and `|| true` is gone from `build-wasm.sh` — `verifyBindings` always raises on `truly_missing`. Provenance schema is bumped to `wasm-build-provenance-v1.1` with `nCollectionManifest.{linked,total,dropped}` written by `yaml_build.main` (in-process call only, no CLI flags) so `generate-docs.mjs` / `docker-e2e-validate.sh` hard-fail with a regenerate-pointer error when the sidecar is missing (no fallback to `compiled - requested` arithmetic — that math counted `extra_compiled` not auto-linked NCollections).
- OCJS bindgen cross-class scope convergence: replace any d.ts-payload regex scrape (e.g. `_NCOLLECTION_TOKEN_RE`) with a **structural** lift — codegen records every emitted-or-attempted C++ class identifier into `TypescriptBindings.referenced_classes`, pipeline serialises the set onto every `.d.ts.json` fragment under `referenced_classes`, and the link layer (`_compute_yaml_class_scope`) unions those sets into the YAML scope on the next cycle; the resolver layer (`resolver/strategies/{fallback,template}.py`) MUST call `_record_referenced_class` BEFORE the export filter so unresolved-but-attempted identifiers still propagate. Diagnostic gates pointing at the regression must name the canonical entry point (`TypescriptBindings.resolve_type → referenced_classes`), never the consumer side. `verifyBindings` differentiates `truly_missing` (WARNING) from `alias_resolved` (INFO) by consulting `ncollection-manifest.json` — NCollection typedef aliases (e.g. `TColgp_Array1OfPnt → NCollection_Array1_gp_Pnt`) whose canonical mangled form IS compiled are demoted to INFO. Sentinel tests that depend on the regenerated bindings tree (`tests/sentinel/test_link_ncollection_reachability.py`) fail loudly with self-describing remediation text ("Regenerate via `pnpm nx run ocjs:generate`") until the Docker image is rebuilt — this is intentional and documented in the research doc's Implementation Status section.
- OCJS multi-threaded link noise budget: `mallinfo` is a legitimate mimalloc-debug-stats undefined symbol — allowlist it in every `mainBuild.allowedUndefinedSymbols` (OCJS `full.yml`/`full_multi.yml` + replicad `custom_build_{single,multi}.yml` *and* their rendered `build-config/` siblings when `ytt` is unavailable). OCCT-internal `[-W#pragma-messages]` deprecation noise (`NCollection_Vector.hxx`, `TColStd_IndexedDataMapOfStringString.hxx`) fires transitively via `BUILTIN_ADDITIONAL_BIND_CODE` and is collapsed into a single `INFO: filtered N OCCT-internal deprecation pragma(s)` line by `_filter_occt_deprecation_pragmas` in `link/yaml_build.py`. libclang `-Wunused-command-line-argument` noise from emsdk's `-stdlib=libc++` is silenced at the parse-flag boundary in `ast/parse.py` (`-Wno-unused-command-line-argument`) and pinned by a hermetic unit test. OCJS-side `OCJS_CONFIG` must be declared as an Nx env input on every cache-affected target (`apply-patches`, `pch`, `generate`, `compile-bindings`, `compile-sources`) or cache restores silently serve the wrong artefact.
- OCJS opt-in browser-only MT variant lives at `build-configs/full_multi_browser.yml` + `configurations.json::multi-threaded-browser` and layers `-sGROWABLE_ARRAYBUFFERS=1` + `-sENVIRONMENT=web,worker` (NO `node`) on top of `full_multi`. Node.js / Bun / Deno lack `WebAssembly.Memory.prototype.toResizableBuffer()` as of May 2026 — including `node` in ENVIRONMENT ships a binary that throws `TypeError: wasmMemory.toResizableBuffer is not a function` on init under vitest/jest. The default shipped MT build keeps `-sGROWABLE_ARRAYBUFFERS=0` precisely because Node is the dominant test-harness runtime; the browser variant is a *flag* delta on top of `full_multi.yml`, not a *scope* delta (sentinel `tests/sentinel/test_full_multi_browser_yaml.py` keeps the two YAMLs in scope-parity via `BROWSER_SCOPE_EXEMPT = frozenset()`). The wasm-opt MT ~0.1% reduction (vs ~4-7% for ST) is expected, not a regression — `EVAL_CTORS=2` is dropped under pthread workers so the constructor folding wasm-opt would otherwise do has no work to eliminate.
- `packages/runtime` is consumed as source via package.json exports, not built output; transport layer owns SAB/`MessagePort` lifecycle as a hybrid Channel — `@taucad/rpc` provides the canonical `Channel` primitive (request/response with deferred resolution + fire-and-forget `notify` + event-stream subscriptions in one wire) used by both runtime worker dispatch and editor/file-system RPC, replacing per-domain pendingRequests Maps; v5 RPC blueprint collapsed `notify` and `request` into a single send path with a custom hybrid wire format (single-letter discriminator like `k:'r'` request / `'p'` response / `'n'` notify, chosen over JSON-RPC for compactness + native transferable/binary support — full wire-kind table at the end of `docs/research/runtime-channel-blueprint-v5.md`); fully type-safe Request/Response/Notification with no `unknown` over the wire; the same Channel abstraction must work across `MessagePort` (browser worker), `worker_threads` (Node), Electron `MessageChannelMain` IPC, and future WebSocket transports without consumer branching; topology evolution: `Browser ↔ WebWorker` (today) generalises to `Renderer ↔ MainThread ↔ Runner` for Electron — a 3rd thread (`worker_threads`) is required because the Electron main thread cannot also be the runner (must stay responsive); the `Runner` primitive routes heavy work (`createGeometry`, `exportGeometry`) off the host's UI thread regardless of environment; `RuntimeFileSystemHandle` is a discriminated union (not a thin wrapper) bundling identity + transport metadata so callers can dispatch on `kind` without prop-drilling — `fromMemoryFs`/`fromNodeFs`/`fromWorker`/`fromFsLike` factories return tagged handles consumed by the transport at construction time (e.g. `webWorkerTransport({ fileSystem: fromMemoryFs(...) })`), not by `client.connect()` — v6 transports own FS/sharedMemory/runner so those fields were removed from `createRuntimeClient` options; `FileStat` (canonical, `@taucad/types`) is the unified stat type — the previous `ProviderFileStat` was unified during the filesystem-target rework; `examples/` workspace at repo root hosts external-consumer integration smoke tests (`examples/cli-headless`, `examples/electron-tau` — the Electron PoC validates Tau runtime over `MessageChannelMain` IPC end-to-end via Playwright, exercising Renderer→MainThread→Runner with monaco editor + threejs viewer + parameter form via `@taucad/react`'s `useRender` (params arrive from the runtime client, never synthetic — re-implementing runtime concerns like parameter extraction or geometry generation inside an example app is an architectural smell; examples must consume the runtime through public surfaces (`useRender` listeners, `client.on`) so integration regressions surface); Electron uses one `utilityProcess` per `RuntimeClient` (since multiple `cad.machine` instances render concurrently) with FS authority on the main thread (matches VSCode), and the kernel-host script ships with the Electron app rather than the runtime package; e2e validates Monaco source edits propagate to params-form labels and a glTF bbox-reader visual-validates bbox changes when params change) — examples projects are real pnpm workspace members that import published-shape `@taucad/*` exports to catch entry-point regressions (these would otherwise hide because internal apps consume `packages/runtime` as source); kernel/runtime worker entries are exposed as dedicated package.json `exports` subpaths so `new URL('...', import.meta.url)` works under every bundler (Vite, webpack/rspack, electron-forge, esbuild) — never bundle the worker URL inline; test mocks in `packages/runtime/src/testing/kernel-testing.utils.ts`; `createRuntimeClientOptions` merges options via `deepmerge` — plugin arrays match by `id` and replace, non-array fields are deeply merged; Vite plugins in `@taucad/vite` with `*.vite-plugin.ts` suffix, `vite:` prefix, Vite 8 hook filters; gitignored `src/**/wasm/` dirs populated by `copy-assets` target via `copy-files-from-to.cjson`; `parameterFileResolverMiddleware` merges per-geometry-unit parameter file overrides from `.tau/parameters/<entry>.json` with code-level defaults; two parameter sources: (1) `getParameters` extracts schema/defaults from source code, (2) `.tau/parameters/<entry>.json` stores user overrides organized as named parameter groups with `activeGroup` selection; `FileParameterEntry` = `{ activeGroup, groups }`, `ParameterGroup` = `{ values }`; project machine manages groups via `switchParameterGroup`, `createParameterGroup`, `deleteParameterGroup`, `renameParameterGroup` events; context uses `parameterEntries: Map<string, FileParameterEntry>` keyed by entry file path; kernel worker watches per-geometry-unit parameter file via `registerWatchPath` for auto-re-render; middleware `getDependencies` hook returns file paths to include in the dependency hash — parameter file resolver uses it so per-geometry-unit parameter file changes invalidate the geometry cache; `BundleResult.unresolvedPaths` tracks import paths that failed to resolve (ENOENT) during bundling — kernel worker watches these paths so creating missing files triggers re-render recovery; runtime protocol includes `'buffering'` worker state (`idle → buffering → rendering`) so debounce windows are visible to UI consumers — runtime worker is the authoritative source of truth for worker state; `@taucad/runtime/testing` requires `// @vitest-environment node` in UI tests (esbuild-wasm incompatible with jsdom); `Tessellation.angularTolerance` is in degrees — replicad/OCCT APIs expect radians, convert with `* (Math.PI / 180)` at the boundary (same pattern as OpenCascade kernel); replicad mesh uses single-WASM-call C++ extractor (`extractMeshData`) with SLProps-based surface-derived normals — byte-for-byte parity with OCCT's glTF writer; replaces prior JS-side `ComputeNormals()` approach; `cad-preview.machine.ts` passes parameters directly to kernel (not via filesystem middleware); render timeout: `setOptions({ renderTimeout })` protocol command (always milliseconds), `KernelWorker.executeRender` uses `Promise.race` with configurable timeout, `RenderTimeoutError`/`isRenderTimeoutError` guard; ALL times across the codebase are in milliseconds (Node.js de-facto convention) — never mix seconds/ms across an API; ALL identifiers drop the `Ms`/`ms` suffix (constants, fields, params, types) and document the unit via JSDoc (`/** Milliseconds. */`) at the declaration site — only fields whose unit is mandated by an external boundary (Node `fs.Stats.mtimeMs`, OTEL `*Seconds` histogram inputs, CSS s/ms strings, Docker/Fly.io health-check seconds, observability JSON payloads like health `responseTimeMs`, benchmark report fields, AI-SDK reasoning-timing fields under `providerMetadata.common`) keep an explicit unit suffix; enforced workspace-wide by two custom oxlint JS rules in `libs/oxlint/src/rules/`: `no-time-unit-suffix` (allowlisted suffixes for the external-boundary cases) and `no-bare-time-identifier` (bans bare time-noun identifiers `timeout`/`debounce`/`delay`/`interval`/`ttl`/`throttle`/`period`/`lifetime`/`expiry`/`expires` — require descriptive prefixes like `refreshDebounce`, `renderTimeout`, `entryTtl`, `coalescingWindow` so the reader knows "X of/for what?"; `duration`/`elapsed` are exempt because they are descriptive measurement nouns); persisted UI graphics settings (`GraphicsViewSettings`) carry a `schemaVersion: 2` discriminator — `parseGraphicsViewSettings` migrates v1 (seconds) to v2 (milliseconds) by multiplying the legacy `renderTimeout` by 1000; `fromNodeFs` provides Node.js filesystem integration for headless/CLI usage; `execute-code-node.ts` evaluates user code in Node.js via `vm.Module` (polymorphic with browser's data-URL worker approach); runtime is ESM-only (no `require`) — env-detected branches use `taucad:` prefix constants for module identifiers; package version injected at runtime via `package-info.ts` (env-aware: bundler define for browser/worker, dynamic read for Node) so GLB/glTF metadata embeds the exact `@taucad/runtime` version cross-environment — exposed publicly to external consumers via the `@taucad/runtime/metadata` subpath export (canonical name documented in `library-api-policy`; chosen over the shorter `/meta` for explicitness); WASM heap detachment ("Cannot perform %TypedArray%.prototype.slice on detached or out-of-bounds ArrayBuffer") is mitigated by copying TypedArrays at the JS boundary — `GROWABLE_ARRAYBUFFERS` (`wasmMemory.toResizableBuffer`) is still experimental in Node 24 and requires a runtime flag, so the copy approach remains the correct architectural choice; runtime geometry two-layer transport: byte array (`Uint8Array`) and shared memory (`SharedPool`-backed `SharedArrayBuffer`); domain-driven SAB ownership: FM machine allocates file pool SAB (`filePoolBuffer`), RuntimeClient allocates geometry pool SAB — `cad.machine` reads `snapshot.context.filePoolBuffer` from FM and passes via `connect({ port, filePoolBuffer })`; `client.geometryPool` is the only pool on the public `RuntimeClient` interface (no `client.filePool`/`filePoolBuffer`); `sharedMemory` options only configure `geometry`; `GltfContentDelivery` pooled variant is `{ delivery: 'pooled', key }` (no `pool` field); SharedPool population is a framework concern — the dispatcher's `toTransportGeometry()` auto-stores geometry before checking for pooled delivery (store-then-check with inline fallback), not middleware; `RuntimeClient.resolveGeometry()` encapsulates SAB→ArrayBuffer resolution so consumers are transfer-mode agnostic; browser `TextDecoder` rejects `SharedArrayBuffer`-backed views — copy at consumer boundary required; `LruMap` in `libs/utils` provides in-memory LRU caching for geometry-cache and parameter-cache middleware (L1 memory + L2 filesystem); OC-based kernels (Replicad, OpenCascade) share `packages/runtime/src/kernels/occt/` (`oc-kernel-error.ts`, `oc-exceptions.ts`, `oc-tracing.ts`) for `wrapOcForExceptions`/`wrapOcWithTracing`/`formatRuntimeErrorWithOc` so `WebAssembly.Exception` decodes into `KernelError:`-prefixed `KernelIssue`s instead of `[object WebAssembly.Exception]`; the shared module is structurally typed via `OcExceptionInstance` and generic on the concrete `OpenCascadeInstance` so each kernel keeps its own WASM-binding type at the call site (kernel-self-containment policy is scoped to schemas/types, not runtime utilities); each kernel has a `*.plugin.ts` file co-locating extensions, detect patterns, builtinModuleNames, and `new URL` worker reference; `canHandle` removed from kernel protocol — plugin extensions are the sole file-detection mechanism; `kernel-factories.ts` is a pure ESModule dynamic-import factory; transcoders are injectable format converters (bytes→bytes) following the same co-located `*.plugin.ts` pattern as kernels; `defineTranscoder` mirrors `defineKernel` — declarative `edges: TranscoderEdge[]` (replaces `discoverEdges`/`canTranscode`) drives type-safe `input.to` inference; `transcoder-factories.ts` barrel; `TranscoderEdge` declares conversion capabilities (from/to/fidelity); converter transcoder wraps `@taucad/converter`; transcoders live in `packages/runtime/src/transcoders/`; export option schema resolution: each kernel plugin defines self-contained per-format export Zod schemas via a single `exportSchemas` map (replaced separate `exportFormats`+`exportOptionSchemas` — keys define the supported format list); tessellation is per-kernel, not global — removed from `CreateGeometryInput`; render and export pipelines share the same per-kernel schema with different defaults; JSON Schema-first merging during plugin resolution; resolved manifest exposes complete JSON Schema for all consumers; OpenSCAD uses native `$fn`/`$fa`/`$fs` tessellation params (not OCCT linear/angular tolerances) — render path merges user-defined + default params with user values taking precedence, export path always overrides with UI-supplied values; OpenSCAD uses 2-stage parameter extraction — extract all params first (including tessellation `$fn`/`$fa`/`$fs`), then filter UI-facing params before exposing to consumers, so user-defined tessellation values can be detected and merged with kernel defaults without a separate scan; default coordinate system convention is Z-up across all formats (replicad/opencascade kernels are Z-up natively); OpenSCAD kernel emits glTF Y-up natively (deviates from the Z-up convention) so transcoded 3MF/assimp exports must propagate `coordinateSystem` through to assimp's per-exporter axis-transform flag rather than relying on the kernel's GLB orientation; transcoded format exports merge intermediate-hop options (e.g. Replicad→GLTF→USDZ retains GLTF tessellation/coordinateSystem); `client.export` is fully type-safe via generic inference that merges native-kernel-format schemas with downstream transcoder schemas; protocol uses `activeKernelChanged` events with a discriminated `KernelId` union (not raw string) for kernel-aware UI/export route selection; OCCT `BRepMesh_IncrementalMesh` caches triangulation on `TopoDS_Shape` — pass `decreate=true` to force re-mesh when tessellation tolerance decreases (otherwise stale fine mesh is returned); `defineKernel` exposes a single `options` field on both `createGeometry` and `exportGeometry` (renamed from `renderOptions` for symmetry) and the value is always required (defaults to `{}` when no schema is declared); native-handle caching uses `serializeHandle`/`deserializeHandle` on `defineKernel` with the serialize return type captured as a generic so deserialize is fully type-safe — `ensureNativeHandle` is an internal runtime middleware capability (not a `defineKernel` API) that re-hydrates handles via the geometry-cache middleware so kernels don't have to re-implement preheating; `@taucad/json-schema` package owns Draft-07 JSON Schema TS typings — fully self-contained, no external `json-schema` npm types dep; `CapabilitiesManifest` shape: collapsed parallel arrays into a single `routes` array (no `routeId`), fidelity lifted onto kernel definitions, helpers `routesFor`/`bestRouteFor` exposed on `RuntimeClient`, manifest replays current value to late subscribers; JSON Schema-first merging chosen over Zod-first for runtime resolution while preserving Zod authoring DX (`z.object({})` retained — problematic generated types filtered out instead of switching to `z.unknown`)
- OCJS Phase 4 regen made genuine `std::optional<T>` emission live; null/undefined policy is strict-by-default: undefined→default, null→default only where C++ admits null (row 30), else reject.
