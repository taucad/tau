---
description: Auto-curated learned facts and preferences for repo-wide tooling (oxlint, ESLint, frontmatter validation, Nx daemon, GitHub Actions)
globs:
  - "libs/oxlint/**"
  - "tools/**"
  - "nx.json"
  - ".github/**"
  - ".oxlintrc.json"
  - ".oxfmtrc.json"
  - "scripts/**"
alwaysApply: false
---

# Learned: tooling

Auto-maintained by the `agents-memory-updater` subagent. New bullets are
capped at 200 chars and 12 bullets per section. The existing entries below
were harvested verbatim from `AGENTS.md` during the one-time context
cleanup; they may exceed the 200-char cap.

## Learned User Preferences

- Light tasks (docs, preview, dev-mode renders) must NEVER declare `dependsOn` for heavy build targets (multi-min WASM rebuilds) in `project.json` — opening docs should serve whatever artefact is available, not silently trigger a fresh bindgen/WASM build.
- GitHub Actions release-notes / PR @-mentions must resolve to the GitHub login (parse it from the `<id>+<login>@users.noreply.github.com` form in commit author emails), never the real name from `git log %an` — real-name forms like `@richard fontein` silently break the `@username` autolink in PR bodies and release notes.
- HTML references cache a visual PDF plus inert semantic snapshot; Markdown comes from the snapshot and is accepted only after raw/rendered legibility review.

## Learned Workspace Facts

- oxlint DI overrides key off FILENAME (`apps/api/**/*.service.ts|controller.ts|gateway.ts`): injectable classes elsewhere (e.g. an outbox) must be renamed `*.service.ts` or new-cap/consistent-type-imports fire on decorators; Nest parameter decorators must also be in the `.oxlintrc.json` new-cap `capIsNewExceptions` list (`Ip` was missing).
- tau-lint bans `Ms`-suffixed identifiers (no-time-unit-suffix — document `/** Milliseconds. */` instead) and fire-and-forget `.then/.catch` chains (no-async-iife) — restructure as `void this.asyncMethod()` where the method owns try/catch.
- Frontmatter docs validation runs via `pnpm docs:validate` (Nx target `scripts:validate-frontmatter`) over ~280 docs in `docs/policy/` + `docs/research/` — frontmatter `title` must match the H1 byte-for-byte (including backticks/code spans) and an `updated:` field older than 180 days emits a warning; reference-category docs that snapshot upstream sources are exempt by convention but the validator currently still flags them, so bump the `updated:` after a content review or extend the validator to skip `category: 'reference'`. Nx daemon can cache a stale failed project graph from a transient plugin-worker startup timeout (verbose logs show the worker subscription retrying ~8 times before connecting; `./tools/pkgcheck.plugin.ts` and `@nx/react/router-plugin` are the usual victims when spawned last) — `pnpm exec nx reset` clears the daemon and the cached failure; setting `NX_PLUGIN_NO_TIMEOUTS=true` (combine with `NX_DAEMON=false NX_VERBOSE_LOGGING=true` for diagnosis) bypasses the worker startup timeout in one-off invocations. Hybrid oxlint + ESLint linting: oxlint runs first, ESLint handles residual rules; custom Oxlint JS plugins in `libs/oxlint/`; tsgolint (typescript-go) provides type-aware JSDoc codeblock checking via `source_overrides`; rule tests use `oxlint-disable` syntax (ESLint 9 RuleTester strips `eslint-disable` from `getAllComments()`); when only oxlint flags a rule, prefer `oxlint-disable` form — `eslint-disable` for those same names produces "unused directive" warnings since the two linters use divergent rule IDs (e.g. oxlint `no-empty-function` vs ESLint plugin variants); ESLint plugin rules require the plugin prefix in disable directives (e.g. `no-barrel-files/no-barrel-files`, not bare `no-barrel-files`); `oxlint-disable-next-line` only suppresses one line — use block-level `oxlint-disable`/`enable` for sections with multiple violations; tsgolint requires absolute paths — JSDoc codeblock virtualPath must be resolved against the workspace root when `context.filename` is `<input>` (RuleTester default); MDX parser exported separately at `@taucad/oxlint/mdx-parser` (not a property of the ESLint plugin object); `validate-mdx-links` checks internal dead links (relative + absolute, including Fumadocs route groups); `validate-mdx-external-links` checks remote URLs via subprocess with disk cache at `node_modules/.cache/tau-lint/external-links.json`; generated report artifacts under `apps/api/reports/` and tsdown-built `scripts/dist/` must be added to `.oxfmtrc.json` ignore (otherwise format runs keep re-flagging generated TS); workspace `no-restricted-imports` rule prohibits sibling-relative imports (`./foo.js`) — every package and kernel must use `#`-prefixed subpath imports; husky `commit-msg` hook validates scope across `apps/`, `packages/`, `libs/`, AND `kernels/` (kernels added when openscad was extracted); GitHub Actions must use Node-24-compatible action versions (`actions/cache`, `actions/setup-node`, `nrwl/nx-set-shas`) — `.github/actions/setup-nx/action.yml` is the canonical place to bump SHAs; running `pnpm nx test ui app/routes/projects_.$id/...` shell-expands `$id` to empty (the segment is read as a shell variable) and silently runs the wrong path — from `apps/ui` use `pnpm exec vitest --run 'app/routes/projects_.$id/<file>.test.tsx'` with the path quoted so the literal `$id` is preserved
- Runtime typecheck configs (`tsconfig.json`, app/lib/spec) must not contain cross-project `references`; source exports resolve package types, guarded by `scripts:validate-tsgo-runtime-references`.
- oxlint's type-aware verdicts depend on tsconfig membership: files outside every tsconfig get default-project types (no `noUncheckedIndexedAccess`), flipping `no-unnecessary-condition`/`no-unnecessary-type-assertion` verdicts. Give scripts a tsconfig home (e.g. tsconfig.spec.json include) BEFORE trusting those rules; typed eslint needs the same or it fails with project-service parsing errors.
- Coverage thresholds only bind when coverage is enabled: `pnpm nx test <project>` green proves NOTHING about a vitest `coverage.thresholds` gate — run `--coverage` explicitly to execute it. geospec's scaffold-era 100% gate first actually executed 2026-07-17 at 87.5% stmts / 72.5% branches with all tests green: duplicate instrumentation (same-line hit+zero statement pairs), node-uncoverable browser guards with no v8-ignore hints, worker/pool code invisible to in-process v8 coverage, and non-code assets swept in by `include: ['src/**/*']` all inflate the denominator before any real gap counts.
- The agent-harness Bash tool RESETS its working directory between calls (including after background-command launches): a compound command that assumes the previous call's `cd` can run against the tau ROOT — where a stray `git add -A && git commit` triggers husky commit-msg (scope validation) + lint-staged oxfmt over the whole staged monorepo. Always `cd <absolute repo path> && …` inside EVERY git-mutating/build command. Recovery map when it happens: lint-staged DROPS its backup stash after formatting "succeeds" (only the hook's failure text logs the sha, e.g. "git stash (300be348)"), but the dropped commit stays reachable — its ^2 INDEX commit holds the complete staged pre-format tree (after add -A that's the ENTIRE intentional worktree): `git checkout <sha>^2 -- . && git reset`. Do NOT restore from `git stash list`'s surviving "lint-staged automatic backup" without checking its parent against current HEAD — stale backups from prior sessions linger and silently overlay old file states.
