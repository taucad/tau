---
description: Auto-curated learned facts and preferences for the `ui` app (apps/ui)
globs:
  - "apps/ui/**"
alwaysApply: false
---

# Learned: ui

Auto-maintained by the `agents-memory-updater` subagent. New bullets are
capped at 200 chars and 12 bullets per section. The existing entries below
were harvested verbatim from `AGENTS.md` during the one-time context
cleanup; they may exceed the 200-char cap.

## Learned User Preferences

- Source-context Share dialogs own publication recipient management; published viewer pages stay consumption-focused and should not carry redundant owner share controls.
- State machines own lifecycle and state logic; UI clients send events only and never decide open/close; avoid ref/state for sync guards
- Folder icons in `chat-message-tool-list-directory` open the file explorer pane on desktop and no-op on mobile, mirroring the file-icon → editor-open pattern.
- User chat-message bubble height matches the focused textarea variant — overflow text stays invisible/unreachable until focus; never add an inner scrollbar in the collapsed state.
- Use `react-virtuoso` for virtualization, not `@tanstack/react-virtual`; follow patterns in `combobox-responsive.tsx`; when virtuoso size/measurement bugs surface (e.g. tiny chat-message bubbles for very long text), fix via the slicing approach (cap items measured per pass, hand off to the existing virtuoso scroller) rather than removing virtuoso — never trade away virtualization for a perf hit on long content
- Never blow away the entire IndexedDB database — user work is stored there
- Chat composer toolbar, model/kernel selector triggers, and other paired breakpoint UIs: apply the same visible/control changes to both desktop and mobile layouts (including `chat-textarea-desktop.tsx` / `chat-textarea-mobile.tsx` when split) when the feature exists on both; floating affordances on the active streaming user message (e.g. inline stop button) must be overlayed/absolutely-positioned, never reserve extra vertical space in the message layout, and reveal only on hover/focus of the active `chat-message` root; matching the model selector hover-card pattern applies to peer triggers like the kernel selector for parity; the entire chat composer wrapper must remain the click target for focusing the inner contenteditable textarea (regressions where padding/decorative regions stop forwarding focus are real DX bugs)
- On-demand viewer screenshot (e.g. `capture_screenshot` for an unopened file) must mount the viewer pane as a true background tab in the first slot — never force-activate it; forcing tab focus to capture is a UX smoking-gun bug.
- Screenshot overlay (purple chip) renders only on chat-attachment paths (drag-to-composer / camera button); thumbnail, download, and copy paths must skip the overlay entirely.
- Screenshot overlay shows full editor path (e.g. `lib/part.ts`), never `/projects/:id`; chip is fully opaque purple, label sits to the right of the kernel icon (not above).
- `apps/ui/app/chat-clients/` wrappers compose AI-SDK public abstractions only — never reach into wire internals like SSE `[DONE]` sentinels; the wrapper layer localizes future chat-API changes to one place.
- Selector trigger/pill text must read from the same source as the dropdown's selected state — divergent display-vs-selection sources are a recurring latent-bug class (ComboboxResponsive callsites).
- Chat skeleton/loading lives in ONE layer (chat-history's TurnGroup); never duplicate render-gates at the route or `chat-interface-desktop.tsx` level — multiple gates produce visible flash mid-stream.
- Editor machine is the sole owner of the open-tab set; Dockview is a pure idempotent reconciler keyed by stable `paneId`; file path is a property of the tab, never its identity. Filesystem mutations flow through `file-operation-participants.ts` (`ContentChangeEvent` → editor + project intents); UI components never send `renameFile`/`closeFile` directly. See `docs/research/editor-filesystem-surface-audit.md` R3.
- `monaco-editor` runtime is quarantined to `*.client.{ts,tsx}` modules (React Router v7 erases them server-side); `import type * as Monaco from 'monaco-editor'` is unrestricted. Enforced by `no-restricted-imports` in `eslint.config.mjs`. See `docs/policy/ssr-bundle-policy.md` rule #7.
- Prefer foreground/monochrome over primary/purple brand accent on functional surfaces (chat submit/stop button, viewer edge lines, docs syntax); keep it theme-aware (white in dark, dark in light).

## Learned Workspace Facts

- Monaco highlight off-by-1 drift originates from stale font measurements (not the highlighter) — fix at the font-measurement source, never post-process highlight bounds.
- Editor architecture: machine owns openFiles, ref-counting, force-close; dockview subscribes only; use unique panel IDs (not file path); single FS worker with provider-based backends (`AbstractProvider` → `DirectIdbProvider`, `FsAccessProvider`, `OpfsProvider`, `MemoryProvider`); `ProviderRegistry` is a pure factory (no active backend state or provider cache); `MountTable` for multi-backend mount points — `mount(prefix, provider, config)` with `MountConfig = { backend, ...options }`; `FileService.mount(prefix, backend)` / `unmount(prefix)` is domain-agnostic — callers compose domain paths (e.g., `/projects/${id}`), FileService never knows about projects; `FileService.reconfigure()` removed — mount/unmount is the only configuration surface; `CrossTabCoordinator` for multi-tab sync; `SharedPool`/`SharedMemoryArena` in `packages/memory` for zero-copy content sharing; FM machine allocates file `SharedArrayBuffer` and posts to FM worker + creates `SharedPool` for `FileContentService`; `FilePool` type (was `ContentPool`) in `@taucad/runtime/filesystem`; `FilesystemObserverBridge` wraps native `FileSystemObserver` API; ZenFS dependency removed — direct IndexedDB/OPFS/FS Access API implementations replace it; `packages/filesystem` large-repo layer: `InMemoryFileTree` (O(1) stat/readdir), `ResourceWriteQueue` (per-parent-dir writes), dedicated import worker for off-thread fetch+extract+write; `fromSafeAsync` (`#lib/xstate.lib.js`) replaces `fromPromise` for all UI XState async actors; `chat-editor-dockview.tsx` is the active editor; file-type rendering uses viewer registry pattern; two filesystem watch planes: kernel fast path (dependency-scoped) and UI tree path (directory-scoped) — do not merge into one coarse stream; chat activity folding: `assistant-message-activity.ts` groups tool/reasoning parts into Cursor-style collapsible sections (`chat-activity-group.tsx`, `chat-activity-section.tsx`, `chat-activity-fold-context.tsx`); groups stay open during streaming and auto-close on completion; user toggle overrides persist; tool rows within groups default collapsed and use full-width row triggers (entire row toggles open) while keeping the chevron inline next to the label (not pushed to the right edge); all chat-tool cards (`chat-message-tool-*`, activity sections, etc.) render their `<verb> <description>` text via the shared `chat-tool-label.tsx` component — no per-tool styling, single point of adjustment to keep spacing/coloring consistent across call sites; chat-tool labels follow a unified two-tone hover palette (muted verb + foreground description on hover, mirroring `chat-message-reasoning.tsx` "Thought") — `viewer-link.tsx`/`file-link.tsx` share the same hover ramp; never use green/orange/red text on chat parts (color is reserved for the leading icon only; trailing chevrons follow the muted/foreground palette, never a status color); chat-tool icons render with NO color in success states (success doesn't draw attention); `ChatToolError` rows pass caller-owned `noun` (e.g. `file read`) paired with `getToolErrorTitle` (`Attempted`, `Stream interrupted during`, …) and use no destructive icon tone — compile diagnostics in `chat-message-tool-get-kernel-result` still use `Failed to compile` with destructive icon on that in-card row only; standardized chat-tool verbs unify across grep/glob/web-search as "Searched", web browser as "Visited"/"Visiting pages", `test_model` success as "Tested X requirements", compaction as "Summarized chat context", unknown-part fallback as "Received unknown part <part-type>"; `chat-tool-error.tsx` is a Radix `Collapsible` with the error body inside the collapsible content — same UX as `chat-message-reasoning`/`chat-message-tool-get-kernel-result` errors; pane-header actions (`paneview-header.tsx`, `chat-parameters.tsx`) render on hover only across all panes — no special "main geometry unit" exemption — and the opacity transition lives on the parent action-group container so children inherit one animation rather than each declaring its own; the parameter group selector is hover-gated and renders on every geometry unit pane (not main-only); group summaries expose both `summaryVerb` (past tense, e.g. "Explored") and `summaryVerbActive` (present participle, e.g. "Exploring") with the active form rendered only while streaming/chat is active (cancelled or completed flips back to past tense); active group titles render with the same `text-shimmer` effect used by ongoing tool rows like `chat-message-tool-read-file.tsx` — shimmer + present-tense titles are scoped to the trailing message only, so previous messages always show past tense even while chat is still streaming a later message; exploration phase summary aggregates kernel verification ("X renders"), screenshot capture ("X screenshots"), `test_model` ("X tests"), file reads, web/code searches (merged into a single "X searches" item), and grep/list calls; reasoning appears inside groups but never in group titles; file/CAD mutation operations are never grouped (always separate); no nested groups — bridged groups flatten inner contents; the latest in-flight group remains visually ungrouped (parts shown inline) until the next part arrives that signals end-of-group; binary-file editing pipeline: `FileContentService.resolve()` returns a discriminated `FileContentResult` (`loading | text | binary | too-large | orphaned | error`) sniffed via `apps/ui/app/lib/seems-binary.ts` (BOM + NUL heuristic on first 512 bytes) — extension-based `binaryExtensions`/`isBinaryFile` removed; `BoundedFileCache.set()` returns boolean and never silently drops oversized files (was the perpetual-loading smoking gun); `BoundedFileCache` is keyed only on `path` (no `mtime` versioning) and exposes `entries()` for prefix-walk eviction; byte-only consumers call `resolveBytes()` which throws typed errors (`BinaryFileError`, etc.); open-editor outcome contract: once a file's outcome reaches `text`, it must only transition to `text(b)`/`orphaned`/`error` for the lifetime of the open editor — never back to `loading` (mirrors VS Code's `TextFileEditorModel` contract) — reverting to the shared `loadingOutcome` sentinel triggers a full `<CodeEditor>` unmount/remount that loses Monaco's focus/cursor/scroll/IME state even with `keepCurrentModel`, and is the smoking gun for "editor flashes after every buffering cycle"; worker `fileChanged` echoes on open paths must do a background `resolve(force:true)` and swap to a new `text` outcome rather than `outcomes.delete`; suppress self-write echoes via a TTL set on in-flight `write()` paths, then graduate to a typed `origin: 'self' | 'external'` field on `ChangeEvent`; the same rule applies to `directoryChanged`/`invalidateUnderPrefix`; `proxy.listen('fileChanged', …)` fans into BOTH `treeService.handleWorkerFileChanged` AND `contentService.handleWorkerFileChanged` (added in `fc548205b` to close the chat-agent stale-cache hole — `read_file`/`grep`/`edit_file` previously served stale bytes when other tabs / OS editor / runtime worker writes mutated files); five `ChangeEvent` types (`fileWritten`/`fileDeleted`/`fileRenamed`/`directoryChanged`/`backendChanged`) with backend identifiers `'indexeddb' | 'opfs' | 'webaccess' | 'memory'` (NOT `'idb'`); cross-project events are filtered; `FileTreeNode` is intentionally lightweight (`{id, name, children?}` — no stat metadata), so never hard-code `size: 0`/`mtimeMs: Date.now()` when forwarding to consumers (RPC adapters, tool schemas) — synthesized zeros lead the chat agent to invent "stale cache" stories; use `readDirectoryEntriesWithStats(path)` (parallel `proxy.stat` per immediate child) and forward real `size` + ISO-8601 `modifiedAt` only when `mtimeMs > 0`; diff-viewer cross-language fix: do NOT use Shiki's `transformerNotationDiff` (`// [!code ++]` markers) — only works when the host language grammar recognizes `//` as a comment, so USDA/bash/JSON/STEP/STL silently leak markers AND lose `diff add`/`diff remove` styling — instead build a per-render `line` transformer that calls `addClassToHast` from `shiki/core` directly from the diff metadata (line index → class), sidestepping grammar-aware comment detection; expose `data-shiki-state="loading" | "ready"` on the viewer root and `waitFor(() => container.firstElementChild?.dataset['shikiState'] === 'ready')` (oxlint `naming-convention` forbids `dataset.shikiState` shorthand for non-camelCase keys); `@shikijs/transformers` removed from root `package.json` after this migration; `chat-message-reasoning.tsx` uses sticky-bottom auto-scroll (pin while streaming, pause when user scrolls up, resume when user reaches bottom again); while reasoning is in-flight the collapsible's collapsed state retains a multi-line minimum height equivalent to the scrolling area rather than fully hiding — the collapsed view always shows the latest streaming lines so users can still glance at progress without re-expanding; components depending on `ChatToolCard` (whose `useCookie` calls `useRouteLoaderData`) need a router stub or `vi.mock('#components/chat/chat-tool-card', …)` in tests — pattern from `chat-message-reasoning.test.tsx`; model brand icons in `chat-message-data-usage.tsx` resolve via `<SvgIcon id={model.details.family} />` for parity with `chat-model-selector.tsx` (e.g. Gemini → Google icon, not VertexAI provider icon — provider routing is selector-only, not visual identity); chat textarea uses TipTap for rich text editing with `@-reference` chip parsing (context suggestion, slash commands, chip node rendering via `apps/ui/app/components/chat/tiptap/`); `@-reference` chips round-trip via `@path` serialization — shared `ContextChip` component (`onRemove`/`interactive` props) for visual consistency, `rehype-at-references` uses `<mark>` elements (not `<span>`) with `AtReferenceContext` for O(1) file/chat validation; slash command chips (`/command`) share the same parse/rehydrate/render pipeline as `@path` references; chat panels use Dockview `PaneviewReact` for multi-geometry-unit grouping; `chat-kernel` decomposed into `chat-kernel-*` sub-files; settings dialog uses `SettingsSection` union with `SettingsGroup` dividers — Models and Agents are settings tabs (not standalone routes); `modal={false}` on Radix DropdownMenus in chat UI to avoid O(n) DOM traversals with large DOMs; `ClientOnly` component accepts `fallback` prop for layout-stable skeleton rendering during SSR/hydration; `chat-textarea-skeleton.tsx` prevents layout shift; `HeroViewerGate` defers Three.js/CAD viewer loading on landing page behind `requestIdleCallback`; file-manager machine two-phase init: `connectingWorker` (fast: worker+bridge+proxy) → `initializingServices` (slow: backend+mount+readDir+services) → `ready`; `SharedWorkerGate` opens after first phase sets `context.worker`; `ThrottledWorker` in `packages/filesystem` for chunked FS event delivery (100/chunk, 200ms delay, 10k buffer) between `EventCoalescer` and port emission; scroll shadow utilities (`scroll-shadows-x`/`-y`) use `mask-image` + `animation-timeline: scroll()` with `@property initial-value: black` — no `--scroll-shadows-bg` variable needed, works on any background; `bg-*` must be on a parent/wrapper, not on the same element as `scroll-shadows-*` — placing both together makes the mask fade the background too, revealing a mismatched color underneath; `paneviewStyleOverrides` sets `--dv-paneview-active-outline-color:transparent` to suppress focus outlines; dockview sash cursors overridden to `col-resize`/`row-resize`; dockview CSS rules (outside `@layer`) outrank Tailwind v4 utilities — override with `!important` suffix (e.g., `[&_.dv-pane-body]:overflow-y-hidden!`); `parameters-number-field.tsx` is the unified Blender-pattern slider-input component — single field where the slider track IS the input, replacing the separate `ParametersNumber` slider + `ParametersInputNumber` pair; follows `docs/policy/engineering-forms-policy.md` layout rules (45% label width, right-aligned numerics, container-query responsive collapse); `PaneviewHeaderTitle` uses HTML `dir='rtl'` attribute for left-side path truncation (CSS `direction` property alone does not trigger `text-overflow` truncation correctly); route `handle.actions()` renders project-scoped header actions (e.g. Export button via `project-export-action.tsx`) between `AlphaBadge` and `Commands` in the page header; `chat-parameters.tsx` exposes a context menu (dots-on-hover + right-click, mirroring `chat-file-tree.tsx`) with "close geometry unit" and "Quick export" — Quick export opens an `ExportSelector` (`apps/ui/app/components/files/export-selector.tsx`, analogous to `file-selector.tsx`) listing all export formats per geometry unit using the `chat-converter` best-path logic; closing a geometry unit surfaces a centered "Reopen renderer" overlay button in `chat-viewer.tsx` so the GLB can be restored (close is non-destructive, never blow away the geometry); the parameter pane context menu also exposes "Open in viewer" (via `viewer-link.tsx`) and "Open in editor" alongside close/Quick-export; `viewer-link.tsx` (`apps/ui/app/components/files/viewer-link.tsx`) opens a file in a new viewer tab with a tooltip — analogue of `file-link.tsx` for the viewer rather than the editor — used by tool artifact links (e.g. `test_model` GLB outputs); `SvgIcon` `id` is strongly typed as a closed icon-key union (not raw `string`/empty); `useModels` (`apps/ui/app/hooks/use-models.tsx`) centralises model fallbacks via the same map-based lookup pattern as `use-kernel.tsx` — unknown model families resolve to an `'unknown'` family key which `SvgIcon` renders as nothing, replacing scattered anthropic-logo defaults at callsites; it also owns per-user model availability via the sparse `chatModelOverrides` cookie (`overrides[id] ?? model.recommended ?? false`), exposes `availableModels`/`recommendedModels`, drives the Settings → Models search/switch list with `View All Models`, and the chat model selector footer's `Add models` action opens that settings tab; `chat-message-tool-edit-file.tsx` detects no-op edits (0 lines added / 0 lines removed by the underlying patch) and renders the simpler inline `chat-tool-file-operation.tsx` style with copy "Edit attempted, no changes" rather than the full collapsible card — empty/confusing collapsibles for zero-diff edits are a UX smoking-gun signal; `chat-message-tool-test-model.tsx` collapses success and failure into ONE render path — single `ChatToolCard variant='minimal'` with identical header chrome, no nested cards on failure, the only failure cue is `ChatToolCardIcon tone='destructive'` and `isDefaultOpen={hasFailures}` to auto-expand failures (per-file `RequirementIndicator`s nested inside `[data-target-file]` groups; no header-level aggregate); `viewer-link.tsx` always renders an inline lucide `Box` cube icon to the LEFT of the filename to visually differentiate it from `file-link.tsx` (which uses extension-based icons) — inline-icon vertical centering uses `align-middle relative -top-px` (NOT `align-text-bottom`, which sags below baseline; NOT negative margin, which shifts the line box) so the icon optically centers against lowercase text without altering line-box height; all chat-tool cards (and tool rows rendered inside activity groups) must constrain to the `chat-history.tsx` virtuoso scroller width — never introduce horizontal overflow or an x-scroll bar at the chat-history level (regressions usually come from inline tool components forgetting `min-w-0`/parent-width clamping when migrated out of the card wrapper); chat-tool title-row ellipsification is a single-line truncation contract enforced by `apps/ui/app/components/chat/title-row-truncation-contract.test.tsx` — `file-link.tsx`/`viewer-link.tsx` rendered inside a chat-tool label must be `display:inline` (not block) and inherit the parent's `min-w-0` truncation context so the verb + filename collapse uniformly as one string ("Failed to compile main..."), never letting the link establish its own intrinsic-content min-width that protects the filename while truncating only the verb; concurrent chat-stream architecture splits draft ownership from streaming ownership: `<ChatInstanceRegistry>` (`apps/ui/app/hooks/chat-instance-registry.ts`) is an in-memory registry of live `<ChatInstance>` mounts owned by `<ChatRegistryProvider>` (`apps/ui/app/hooks/chat-registry-provider.tsx`) at the app shell with per-chatId scoped subscriptions and stable snapshot refs (no `useSyncExternalStore` tearing); `<ChatInstance>` (`apps/ui/app/hooks/chat-instance.tsx`) owns `useChat` + `chatPersistenceMachine` + streaming/RPC and self-registers; `<ActiveChatProvider>` (`apps/ui/app/hooks/active-chat-provider.tsx`) is a per-subtree binding to the currently active chat for chat-input UIs (composer, message editor) and owns only the `draftMachine` in real (`chatId`) or ephemeral (`undefined`) mode — marketing/homepage routes pass `undefined` to skip IndexedDB writes; `<GlobalChatFlushGuard>` (`apps/ui/app/components/global-chat-flush-guard.tsx`) is a single app-shell flush guard that fans out `flushNow` to every registered chat's persistence + draft actors on page unload, replacing per-route `FlushOnCloseGuard`/`HomepageChatFlushOnCloseGuard`; one `ChatRegistryProvider` mounted near `root.tsx` works for every route (project, homepage, marketing) — no forked provider for non-project routes; chat composer accepts dragged viewer panes (`chat-viewer-dockview.tsx`) by invoking `capture-view-screenshot.utils.ts` to attach the current pane view as a chat image, and dragged editor tabs become `file-link` pills — drag detection routes through type-stamped data-transfer signatures in `chat-input-drop-handler.ts` and the entire `chat-textarea.tsx` is the drop target (not just the inner contenteditable area); the same `capture-view-screenshot.utils.ts` powers a camera button in `chat-viewer-controls.tsx` (`capture-view-control.tsx`) that captures the current pane view to active chat images on click; sticky-user-message-per-turn lives in `chat-history.tsx` by grouping each user message with its trailing assistant message into a turn-pair container that virtuoso treats as a single item — never per-message rows for the trailing turn — so the latest pair sticks to viewport height without a footer hack/spacer/`ResizeObserver` (page-per-turn is unacceptable, wastes vertical space) and chat-history's CSS variables are co-located in the component itself (not `apps/ui/app/styles/global.css`) to keep it self-contained; scroll behavior for sticky-pair transitions is `instant` (not `smooth`) to avoid mid-stream scroll jumps; `ChatError` renders as an adornment **inside** the last `TurnGroup` (directly under that turn's messages with `ml-4 mr-1.5`), not in the Virtuoso `Footer` — so chat-history's `min-h-(--chat-live-turn-min-h)` calculation includes the error banner and the live-turn pair stays viewport-sized; collapsible open/close (`genericDetailsOpen`) lives **inside** `chat-error.tsx` itself (no lifted `isOpen`/`onOpenChange` props) and the props collapse to just `className`, with specialised variants accepting `className` and the generic path applying `cn('min-w-0', className)`. Agent `list_dir` empty-directory false-positive: the agent calling `list_dir('')` vs `list_dir('/')` must be normalised to the same workspace-root resolution at the RPC tool-input boundary — the prior bug surfaced as `read_file`/`grep`/`list_dir` reporting an empty workspace immediately after creating a fresh chat (false-positive that pushed the LLM to invent `mkdir`/scaffolding tool calls) and was a path-resolution logic gap, not a race condition; never special-case `''` to mean an unloaded FS; public publication viewer route lives at `apps/ui/app/routes/v.$id/` (`/v/:id` URL) with `fork-action.tsx` driving "fork into my workspace" flows; share/publish modal components under `apps/ui/app/components/publish/` and the `apps/ui/app/routes/projects_.$id/project-share-action.tsx` trigger keep the modal as a `Dialog` (the popover-positioning variant was scrapped after iteration) — primary CTA reads "Copy Link" with link icon → tick on success → reset (mirrors `apps/ui/app/components/copy-button.tsx`), description auto-populates from project description, mobile gets extra margin; private/unauthorized/invalid-code landing pages each render their own elegant error screen following best-practice patterns (sign-in CTA styled with the `release-badge.tsx` purple ramp, never harsh red); publication viewer layout follows `docs/research/publication-viewer-layout-blueprint.md` — viewer stage + parameters above README card (rendered via `markdown-viewer.tsx`), GitHub-style file explorer below the viewer; per-user-per-view tracking for fork/view counts published with each view; mobile UX patterned on `projects_.$id_.preview/route.tsx`; the public viewer route fully opts out of the project app shell (no nav, no header) via the `apps/ui/app/components/layout/page.tsx` handle API (mirrors `apps/ui/app/routes/auth.$/route.tsx`) — a wordmark logo with tooltip sits top-left for brand recovery, fullscreen + AR triggers anchor to the bottom-right of the viewer (reusing the shared AR component at `apps/ui/app/components/cad/{use-ar.ts,ar-button.tsx}` — moved out of `apps/ui/app/hooks/use-ar.ts` so editor and publication viewer share one implementation, never reimplemented), and the desktop layout flips parameters to the right of the viewer (height-matched to the viewer pane) with the file explorer to the left so the page reads as a highly optimised standalone sharing surface; internal markdown links to **resource routes** (loader-only, no `Component` export) like `.txt`/`.mdx`/`.webmanifest` MUST set `reloadDocument` on the react-router `<Link>` (`MarkdownHyperlink` calls `isResourceRouteHref(href)` in `apps/ui/app/components/markdown/markdown-hyperlink.tsx` to detect them by extension) — otherwise react-router intercepts the click, fetches the loader, and renders nothing because the route has no Component, leaving the previous page's layout shell visible while the URL bar updates (smoking-gun "empty sidebar after clicking llms.txt link" symptom); the helper covers the existing `/llms.txt`, `/llms-full.txt`, runtime-scoped llms pages, and per-page `*.mdx` links in one place, never per-call. Infinite grid spacing is owned by the **XState graphics machine**, not the React renderer: `apps/ui/app/components/geometry/graphics/three/react/infinite-grid.tsx` and `grid.tsx` are thin wrappers that forward `materialProperties.smallSize`/`largeSize` from `useGraphicsSelector((s) => s.context.gridSizes)`, while the actual zoom-aware sizing lives in `calculateGridSizes` inside `apps/ui/app/machines/graphics.machine.ts` (`visibleWidthAtDistance = 2 * cameraPosition * tan(fov/2)`, snapped within each decade via `mantissa < sqrt(10) ? 10^e : 5·10^e`) — the baseline knob is the named `BASE_GRID_SIZE_COEFFICIENT` (currently `4`, was an implicit `5`) used as `baseGridSize = visibleWidthAtDistance / BASE_GRID_SIZE_COEFFICIENT`; lower coefficient = coarser grid, snap and imperial paths are independent so adjustments preserve the zoom-responsive scaling. Centralised header/cache helpers in `apps/ui/app/lib/react-router.lib.ts` are the single import point for Netlify-required headers — consumers must call the lib helpers and never re-import the underlying constants from `cache.constants.ts` directly (only the lib imports from constants, callers never double-import — keeps the lib free of constant churn and makes hosting-provider swaps a one-file change). Per-route prerender opt-in via React Router 7's `handle` export (e.g. `export const handle = { prerender: true }`) — never centralise the prerender list in `react-router.config.ts`; the config reads each route's handle so adding a prerendered page is a one-file change inside the route folder, mirroring `meta`/`links` ergonomics. SSR bundle budget unifies under `size-limit` (NOT a bespoke `check-ssr-budget` Nx target — same task, different package; budget ratchets to the measured floor with an explicit `index.js` cap to catch quiet drift); split-component refactors must demonstrate they actually shrink SSR by counting lazy chunks under `build/server/` against the measured baseline, otherwise the split adds no value. Vite content-hashed asset filenames make Netlify cache purge unnecessary on deploy (etag-based revalidation handles the cutover); `apps/ui/app/routes/sitemap[.]xml/route.ts` (resource route — same `reloadDocument` rule as llms.txt) plus a production-grade `robots[.]txt/route.ts` are the canonical SEO surfaces. Fumadocs `<TypeTable>` props embed JSON-encoded type metadata via HTML-entity escaping (`&#x22;…&#x22;`) which is unreadable for both humans and LLMs in `llms.txt`/`llms-full.txt`; `apps/ui/app/lib/fumadocs/llm-stringify-type-table.ts` re-renders the prop blob as a TS-shaped block (flat prop bullets with NO blank line separators between items — agents don't need the legibility, blank lines are wasted tokens) when generating the LLM-friendly text outputs (consumed by the runtime docs `llms-full.txt` route), preserving field types/descriptions in a form models can actually parse — the architectural rule is to bypass the HTML/MDX rendering pipeline entirely for `llms.txt` rather than band-aid HTML-entity unescaping (`<br>` tags are banned in llms.txt for the same reason: zoom out and avoid the source of the escaping problem instead of unescaping downstream). Env-aware route loaders for SEO files (`apps/ui/app/routes/robots[.]txt/route.ts`, `apps/ui/app/routes/sitemap[.]xml/route.ts`) source the production hostname from the shared `appDomain` constant (`apps/ui/app/constants/meta.constants.ts`) and assemble URLs via the `new URL(path, base)` constructor — never string concatenation — so misconfigured paths fail at build/test time rather than serving silently broken links. Compile-time type assertions are preferred over runtime tests when the same coverage can be expressed in the type system: e.g. `apps/ui/app/constants/kernel-monaco-extensions.constants.ts` uses typed satisfies-style guards so the matching `*.test.ts` was deleted (don't maintain both — pick the type-level check and remove the runtime mirror)
- Multi-workspace UI lives in `apps/ui/app/components/settings/filesystem-settings.tsx` + the files route; per-project workspace switcher intentionally deferred; workspace IDs use `wsp_` prefix (no branded `WorkspaceId` type).
- New-project route filesystem config collapses to a single workspace selector (matching sibling input backgrounds); change-folder UI lives only in settings/files, never duplicated per-route.
- Project-load with a missing/unmatched filesystem handle surfaces a workspace-picker UI — never auto-select; avoid IDB schema migrations pre-launch (let the user re-bind).
- Chat provider mount must NOT gate on `chatId` existence — that gate was the project crash-loop class-of-bug smoking gun; remove gates, don't patch with route-level skeletons.
- Editor pane layout uses Dockview v6 (migrated from v4) — composite routing + background-mount activation now go through the v6 API surface; consult dockview migration guides when bumping. The single-image `capture_screenshot` and 6-angle batch capture both rely on background-mount activation in v6 to render without forcing tab focus.
- Settings dialog mounts all panels eagerly on first open via Radix Tabs `forceMount` + `data-[state=inactive]:hidden` (each panel fires its data fetches exactly once when the Dialog opens; tab switching is a pure CSS attribute toggle with zero remounts/refetches and form-local state preserved); `responsive-tabs.tsx` collapses to a single Radix root with `orientation='vertical'` and an outer flex that flips `flex-col → md:flex-row` (mobile horizontal tab strip restored via explicit `max-md:inline-flex max-md:min-h-8` on `TabsList` since the base only adds `inline-flex` for horizontal orientation); `settings-dialog.tsx` adds `forceMount` to every `<TabsContent>` call site.
- Cancel-while-empty-stream restores the user's draft: when the user stops a chat request before any assistant content streams in, `chat-persistence.machine.ts` emits a `restoreCancelledDraft` variant carrying pre-computed `userMessage` + `truncatedMessages` (pure `hasNoAssistantContent()` predicate + three-arm guarded transition: preempt-resume → restoreCancelledDraft → applyStoppedRequest), `chat-session-store.ts` truncates `chat.messages`, dispatches `loadDraftFromMessage` to the draft actor, and `queuePersist`s the trimmed transcript; `chat-history.tsx` subscribes to the emit and rAF-focuses `chatTextareaRef` (mirrors the `handleNewChat` focus pattern) so the user can keep editing without moving the mouse — never poll/diff inside listeners, the machine owns discrimination and pre-shapes the payload.
- `better-auth-ui` multi-session capability is intentionally OFF — the `/v1/auth/multi-session/list-device-sessions` 404 is expected and the UI must not surface or wire any multi-session components; security settings drops the API-keys section (dedicated `/api-keys` page exists).
- `better-auth-ui` mutations swallow errors silently when the global error toaster is bound to a different `QueryClient` instance — the auth components and the toaster MUST share one `QueryClient` so unlink/reauth toasts surface consistently; same rule for any other react-query global side-effect.
- Typechecking uses `tsgo` (Go-based TS compiler); do not add cross-project `references` arrays to `tsconfig.json` (causes TS6305); `tsgo --incremental` writes `out-tsc/` directories with `.tsbuildinfo` even under `--noEmit`, and emitted stale `.js` files can be picked up by vitest's default test discovery — exclude `out-tsc/**` from vitest config (these dirs are gitignored stale build output, safe to delete); avoid `using`/`await using` in shipped code — Rolldown won't downlevel and Safari lacks support; use try/finally with `[Symbol.asyncDispose]()` instead. Node-runtime config files at the app root that are loaded outside the bundler (e.g. `apps/ui/react-router.config.ts` consumed by `@nx/react/router-plugin` and the netlify build via `pnpm nx build ui`) need both runtimes happy: at runtime they execute via `NX_PREFER_NODE_STRIP_TYPES=true` so `await import('./app/lib/static-paths.ts')` works (omitting the `.ts` suffix produces "Cannot find module ..." in netlify CI because Node's strip-types loader requires the explicit extension), but the same `.ts` suffix violates `tsgo`'s default `allowImportingTsExtensions=false` and fails `nx typecheck ui` with TS5097 — resolve by hoisting `react-router.config.ts` (and peer Node-runtime configs) out of `tsconfig.app.json`/`tsconfig.spec.json` into a dedicated `tsconfig.node.json` that enables `allowImportingTsExtensions`, so the netlify-required `.ts` extension stays explicit AND typecheck passes.
