name: CI

on:
  push:
    # Run CI on all branches for early feedback
  pull_request:
    # Run CI on all pull requests

# Cancel in-progress runs when a new workflow with the same group name is triggered
# This can save 10-50% on CI compute costs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  main:
    name: Main
    runs-on: ubuntu-latest
    permissions: # Required for nrwl/nx-set-shas to read workflow runs
      contents: 'read'
      actions: 'read'
      pull-requests: 'read'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
        # filter: tree:0  # Uncomment if using large repos (>200-500MB of files)

      - name: Setup Nx Environment
        uses: ./.github/actions/setup-nx

      # This enables task distribution via Nx Cloud
      # Run this command as early as possible, before dependencies are installed
      # Learn more at https://nx.dev/ci/reference/nx-cloud-cli#npx-nxcloud-startcirun
      # Connect your workspace by running "nx connect" and uncomment this
      # - run: npx nx-cloud start-ci-run --distribute-on="3 linux-medium-js" --stop-agents-after="build"

      # Nx Affected runs only tasks affected by the changes in this PR/commit
      # Learn more: https://nx.dev/ci/features/affected
      - name: Run affected tasks
        run: pnpm nx affected -t lint test build

  # Check what changed to determine what to deploy
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions: # Required for nrwl/nx-set-shas to read workflow runs
      contents: 'read'
      actions: 'read'
      pull-requests: 'read'
    outputs:
      affected_apps: ${{ steps.affected.outputs.apps }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Nx Environment
        uses: ./.github/actions/setup-nx

      - name: Check affected apps
        id: affected
        run: |
          # Get list of affected app projects as JSON
          affected_apps=$(pnpm nx show projects --affected --type=app --json)
          echo "Affected apps: $affected_apps"
          echo "apps=$affected_apps" >> $GITHUB_OUTPUT

  # Call API deployment workflow
  deploy-api:
    name: Deploy API
    needs: [main, changes]
    if: contains(fromJSON(needs.changes.outputs.affected_apps), 'api')
    uses: ./.github/workflows/fly-deploy-api.yml
    secrets: inherit

  # Call UI deployment workflow  
  deploy-ui:
    name: Deploy UI
    needs: [main, changes]
    if: contains(fromJSON(needs.changes.outputs.affected_apps), 'ui')
    uses: ./.github/workflows/fly-deploy-ui.yml
    secrets: inherit 
