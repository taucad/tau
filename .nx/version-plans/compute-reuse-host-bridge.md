---
runtime: minor
host: minor
cli: minor
---

Add private compute-store channels, dedicated Node SQLite workers, and causal scope-settlement telemetry. Compose project-scoped durable reuse and authenticated controls in local daemon hosts and the CLI.
