export const authInstanceKey = 'authInstanceKey';

// Decorator metadata keys for authentication
export const isPublicAuth = 'isPublicAuth';
export const isOptionalAuth = 'isOptionalAuth';

// Hook metadata keys for Better Auth
export const hookKey = 'hook';
export const beforeHookKey = 'beforeHook';
export const afterHookKey = 'afterHook';
