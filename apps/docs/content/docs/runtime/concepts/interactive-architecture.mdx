---
title: Interactive Architecture
description: An interactive, explorable version of the runtime's three-thread architecture diagram.
docType: explanation
icon: lucide:mouse-pointer-click
---

An explorable version of the [architecture diagram](./architecture). Click any node to highlight its connections and see details.

export const architectureNodes = [
  // === Main Thread group ===
  {
    id: 'main-group',
    type: 'group',
    position: { x: 420, y: 0 },
    data: { label: 'Main Thread (display client)' },
    style: { width: 300, height: 280 },
  },
  {
    id: 'editor',
    type: 'diagram',
    position: { x: 60, y: 40 },
    parentId: 'main-group',
    data: {
      label: 'Editor / App',
      description:
        'The user-facing editor (Monaco) and application shell. Sends file edits to the File Manager Worker and render/updateParameters commands to the RuntimeClient.',
    },
  },
  {
    id: 'fm-proxy',
    type: 'diagram',
    position: { x: 60, y: 130 },
    parentId: 'main-group',
    data: {
      label: 'FileManager Proxy',
      description:
        'A bridge proxy (createFileSystemBridgeProxy) that forwards filesystem operations to the File Manager Worker over MessagePort Bridge A.',
    },
  },
  {
    id: 'client',
    type: 'diagram',
    position: { x: 60, y: 220 },
    parentId: 'main-group',
    data: {
      label: 'RuntimeClient',
      description:
        'Consumer-facing API. Lifecycle is construct -> connect -> command -> consume. Sends render / updateParameters / setOptions / export commands to the Kernel Worker; the transport owns abort reservations and the event stream.',
    },
  },

// === File Manager Worker group ===
{
id: 'fs-group',
type: 'group',
position: { x: 0, y: 100 },
data: { label: 'File Manager Worker' },
style: { width: 310, height: 210 },
},
{
id: 'fs-backend',
type: 'diagram',
position: { x: 55, y: 40 },
parentId: 'fs-group',
data: {
label: 'Filesystem Backend',
description:
'Multi-provider filesystem (DirectIdbProvider, OPFSProvider, FileSystemAccessProvider). The single owner of the virtual filesystem — no other thread accesses storage directly.',
},
},
{
id: 'event-bus',
type: 'diagram',
position: { x: 30, y: 140 },
parentId: 'fs-group',
data: {
label: 'EventBus + WatchRegistry',
description:
"When a file is written, the EventBus emits a change event. The WatchRegistry matches it against the Kernel Worker's subscriptions and pushes watch events directly — no main-thread relay.",
},
},

// === Kernel Worker group ===
{
id: 'kernel-group',
type: 'group',
position: { x: 0, y: 400 },
data: { label: 'Kernel Worker (autonomous render service)' },
style: { width: 500, height: 410 },
},
{
id: 'watch-sub',
type: 'diagram',
position: { x: 40, y: 45 },
parentId: 'kernel-group',
data: {
label: 'Watch Subscription',
description:
"Subscribes to file change events scoped to the current file's dependency graph. Watch events flow directly from the File Manager Worker over Bridge B.",
},
},
{
id: 'render-loop',
type: 'diagram',
position: { x: 40, y: 145 },
parentId: 'kernel-group',
data: {
label: 'Render Loop + Debounce',
description:
'The autonomous render scheduler. render renders immediately; file changes debounce at 200ms; parameter changes re-render without added delay. Owns the generation counter for abort detection.',
},
},
{
id: 'krw',
type: 'diagram',
position: { x: 40, y: 240 },
parentId: 'kernel-group',
data: {
label: 'KernelRuntimeWorker',
description:
'The framework layer. Orchestrates kernel selection, middleware chain, bundler, and the autonomous render loop. Pushes geometry, state, and progress events to the main thread.',
},
},
{
id: 'middleware',
type: 'diagram',
position: { x: 40, y: 335 },
parentId: 'kernel-group',
data: {
label: 'Middleware Chain',
description:
'Onion-model wrappers around createGeometry, getParameters, and exportGeometry. Each middleware calls handler(input) to continue the chain. No direct kernel reference.',
},
},
{
id: 'kernels',
type: 'diagram',
position: { x: 250, y: 335 },
parentId: 'kernel-group',
data: {
label: 'Kernel Modules',
description:
'Plain objects from defineKernel(). Implement getDependencies, getParameters, createGeometry, and exportGeometry. State lives in a context returned by initialize().',
},
},
{
id: 'caches',
type: 'diagram',
position: { x: 310, y: 240 },
parentId: 'kernel-group',
data: {
label: 'Caches',
description:
'File hash cache, file content cache, bundle result cache, and kernel selection cache. Invalidated when watched files change.',
},
},

// === Standalone: Three.js / UI (on the main thread, outside groups) ===
{
id: 'scene',
type: 'diagram',
position: { x: 580, y: 550 },
data: {
label: 'Three.js / UI',
description:
'The Three.js renderer and parameter UI on the main thread. Receives geometry, state, and progress events from the Kernel Worker and updates the 3D scene.',
},
},
];

export const architectureEdges = [
  // Main thread internal
  {
    id: 'editor-proxy',
    source: 'editor',
    target: 'fm-proxy',
    type: 'labeled',
    data: { label: 'file edits' },
  },
  {
    id: 'editor-client',
    source: 'editor',
    target: 'client',
    type: 'labeled',
    data: { label: 'render / updateParameters' },
    sourceHandle: 'right-source',
    targetHandle: 'right-target',
    style: { strokeDasharray: '6 3' },
  },

// Main → FS Worker
{
id: 'proxy-fs',
source: 'fm-proxy',
target: 'fs-backend',
type: 'labeled',
data: { label: 'Bridge A (writeFile / readFile)' },
sourceHandle: 'left-source',
targetHandle: 'right-target',
},

// FS Worker internal
{
id: 'fs-eventbus',
source: 'fs-backend',
target: 'event-bus',
type: 'labeled',
},

// FS Worker → Kernel Worker
{
id: 'eventbus-watch',
source: 'event-bus',
target: 'watch-sub',
type: 'labeled',
data: { label: 'Bridge B (watch events)' },
animated: true,
},

// Kernel Worker internal
{
id: 'watch-render',
source: 'watch-sub',
target: 'render-loop',
type: 'labeled',
data: { label: 'invalidate + debounce' },
},
{
id: 'render-krw',
source: 'render-loop',
target: 'krw',
type: 'labeled',
},
{
id: 'krw-middleware',
source: 'krw',
target: 'middleware',
type: 'labeled',
},
{
id: 'middleware-kernels',
source: 'middleware',
target: 'kernels',
type: 'labeled',
},
{
id: 'krw-caches',
source: 'krw',
target: 'caches',
type: 'labeled',
sourceHandle: 'right-source',
targetHandle: 'left-target',
style: { strokeDasharray: '4 2' },
},

// Client → Kernel Worker
{
id: 'client-render',
source: 'client',
target: 'render-loop',
type: 'labeled',
data: { label: 'kernel protocol + abort signal' },
sourceHandle: 'left-source',
targetHandle: 'right-target',
},

// Kernel Worker → Scene
{
id: 'krw-scene',
source: 'krw',
target: 'scene',
type: 'labeled',
data: { label: 'geometry / state / progress events' },
animated: true,
sourceHandle: 'right-source',
targetHandle: 'left-target',
},
];

<InteractiveDiagram
  nodes={architectureNodes}
  edges={architectureEdges}
  title='Runtime Thread Topology'
  description='Click any node to highlight its connections and view details.'
/>

## How It Works

The diagram is built with [React Flow](https://reactflow.dev/) and renders as a custom MDX component (`<InteractiveDiagram>`). Compared to the static Mermaid version on the [Architecture](./architecture) page:

- **Click** a node to highlight its direct connections and dim the rest
- **Hover** for a tooltip describing the component's role
- **Selected node details** appear in a panel below the diagram
- **Dark mode** follows the site theme

Nodes and edges are plain JavaScript objects defined inline -- positions, labels, descriptions -- so the diagram is maintained alongside the text it illustrates.
