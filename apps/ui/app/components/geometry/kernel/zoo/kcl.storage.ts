import FS from '@isomorphic-git/lightning-fs';

// LightningFS singleton for KCL filesystem operations
export const kclLightningFs = new FS('kcl-fs', {
  // FS options for KCL-specific filesystem
});
