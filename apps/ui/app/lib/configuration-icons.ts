import { Box, Layers } from 'lucide-react';

/** The form renderer's selected icons; the runtime's data-only allowlist is generated from these keys. */
export const configurationIcons = Object.freeze({
  box: Box,
  layers: Layers,
});
