/* oxlint-disable no-barrel-files/no-barrel-files -- relocation shim: this module's whole job is to keep the app's import path stable. */
/**
 * The `test_model` result projection, relocated to
 * `@taucad/agent-tools/geospec` so a daemon-placed run returns the same shape.
 *
 * @module
 */

export { runnerResultToTestModelOutput } from '@taucad/agent-tools/geospec';
