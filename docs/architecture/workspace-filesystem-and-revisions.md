# Workspace Filesystem and Revisions

## Status

**Proposal** (2026-09-12) — the target architecture for how files, revisions, branches, and agent working copies relate across every Tau host (browser, desktop, `tau serve`, CLI, cloud). It is the filesystem-and-revisions half of the workspace-anchored topology ruled in [docs/research/client-host-topology-and-filesystem-authority.md](../research/client-host-topology-and-filesystem-authority.md) (rulings N1–N36) and refines, without contradicting, [docs/policy/filesystem-authority-policy.md](../policy/filesystem-authority-policy.md) and [docs/policy/filesystem-policy.md](../policy/filesystem-policy.md). The execution plan is [docs/research/workspace-filesystem-north-star-blueprint.md](../research/workspace-filesystem-north-star-blueprint.md); the file-tree presentation slice is [docs/research/file-tree-mount-provenance-blueprint.md](../research/file-tree-mount-provenance-blueprint.md).

Nothing here is implemented as a unit. Where a rule already holds in code it is marked _(holds today)_; where it does not, the blueprint lists the change. Rulings in this document become normative only by extraction into `docs/policy` (topology OQ-N20).

---

## Why this document exists

Tau's promise is that **the filesystem is the API** ([vision-policy](../policy/vision-policy.md): "Everything is a file… history stay[s] portable and runnable outside Tau"). Files, revisions, and agent working copies were each built well in isolation, but they are presented through three parallel projections — the Files tree reads the authority, the agent reads a composed rooted view with overlays and masks, and revision cards are derived from chat transcripts — and the word "workspace" carries fifteen meanings across the corpus. The visible symptoms: a chat that appears to work in a different filesystem from the Files pane, `run_*` directories that look like project folders, skill links that open "File not found", two panes each claiming "Current", and a Revisions pane whose numbers come from the transcript rather than from stored history.

This document names one canonical representation and derives every surface from it.

## The one sentence

> Every consumer — agent tool, Files tree, editor, viewer, parameters, revisions pane, export, CLI — reads **one composed view of one checkout**. A **checkout** is a working copy of a **project** that tracks a **branch** of that project's **revision graph**. Revisions are the only durable history; checkouts are working state; everything else is a projection.

The rest of the document is the elaboration of that sentence.

---

## Vocabulary

One meaning per word. Terms are ordered from storage upward. Words in the _Retired_ column must not appear in new code, docs, or UI copy with the retired meaning.

| Term                    | Meaning                                                                                                                                                                                                                                                                                                                                                                                       | Retired / avoided spellings                                                                           |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **Storage root**        | One physical store owned by one provider instance: `indexeddb:<db>`, `opfs:origin`, `webaccess:<workspaceId>`, `memory:<scope>`, `node:<abs>`. _(holds today)_                                                                                                                                                                                                                                | "backend" as an instance                                                                              |
| **Authority**           | The one process per host that owns all filesystem I/O and admission for the storage roots it serves (`WorkspaceFileService` in the file-manager worker; `tau serve`; the Electron utility). _(holds today)_                                                                                                                                                                                   | "file manager" as an owner                                                                            |
| **Workspace**           | The directory an authority serves and the unit a client binds to (topology N3/N4, `.tau/binding.json`). Contains projects at its root and a host-private area at `<workspace>/.tau/`. Home is a workspace.                                                                                                                                                                                    | "workspace" for a per-run tree, a materialized view, an XState actor, a chat claim, or a storage root |
| **Project**             | A directory with a `tau.json` inside a workspace. The unit of export, portability, and revision history.                                                                                                                                                                                                                                                                                      | "workspace" for a project                                                                             |
| **Revision**            | One immutable authored tree plus parents, provenance, and summary, addressed by its commit id. Recorded by hosts, never by agents (N26).                                                                                                                                                                                                                                                      | "checkpoint", "snapshot", "turn revision" as distinct things                                          |
| **Revision graph**      | The set of revisions and named refs of one project. One repository per project.                                                                                                                                                                                                                                                                                                               | "revision authority" as a UI concept                                                                  |
| **Branch**              | A named ref that moves. `main` is the project's line; every other branch is a line of work a user or a placement policy named. Branch names are the display names.                                                                                                                                                                                                                            | "lane", "workspace ref", synthetic `explore/…` `history/…` branches                                   |
| **Head**                | The revision a branch names, or the revision a checkout has applied (`detached` when no branch names it).                                                                                                                                                                                                                                                                                     | "current" as a storage term                                                                           |
| **Change id**           | A stable identity for a candidate revision across rewrites (N35). Engine-side only.                                                                                                                                                                                                                                                                                                           | —                                                                                                     |
| **Checkout**            | A working copy of one branch, addressable by id: git's worktree, jj's workspace. `{ id, projectId, root, branch \| detached, baseRevisionId }`. The **live checkout** is the project directory (git's main worktree). **Linked checkouts** are additional working copies in the host-private area. One checkout per branch. Chats, peer agents, and the workbench attach to a checkout by id. | "materialized workspace", "run tree", "isolated view", `run_*`, "candidate checkout"                  |
| **Turn**                | One agent run against one checkout, from prepare to finalize. A turn produces at most one revision.                                                                                                                                                                                                                                                                                           | "job" for the filesystem side                                                                         |
| **Lease**               | The host's record that a chat's run is attached to a checkout. Many leases may hold one checkout at once (peer subagents, the user). Stored under `.tau/runs/`.                                                                                                                                                                                                                               | "claim", "workspace claim", "mode" as the record                                                      |
| **Backend**             | The engine that stores a project's revision graph on this host: browser object store, native git, or jj over git. One per project per host; every backend implements `RevisionPort`. jj's own `Backend` trait is this same seam.                                                                                                                                                              | "adapter" in product copy, "engine" as a user word                                                    |
| **Remote**              | A peer revision graph the project syncs with: a Git remote (GitHub, a bare repo) or the **Tau Hosted Remote** (R2 behind the Tau API). Zero or more per project. Refs and objects are exchanged, never working trees.                                                                                                                                                                         | "backend" for a remote, "sync target", "cloud save"                                                   |
| **Named version**       | A tag (`refs/tags/<name>`) with a note and an actor, pointing at one revision. What users, publications, and external links refer to.                                                                                                                                                                                                                                                         | "release", "milestone", "label"                                                                       |
| **Publication**         | A Tau Cloud record that points a share URL at a named version of a project whose graph is synced to the Tau Hosted Remote. Bytes are served from the synced graph; nothing is uploaded twice.                                                                                                                                                                                                 | "export to cloud", "snapshot upload"                                                                  |
| **Trigger**             | Why a revision was minted: `save` \| `idle` \| `turn` \| `merge` \| `restore` \| `sync` \| `import`. Stored in provenance, drives History grouping.                                                                                                                                                                                                                                           | "autosave" as a distinct object                                                                       |
| **Actor**               | Who a revision is attributed to: `{ kind: 'user' \| 'agent', id, email?, name?, anonymous?, model?, runId?, onBehalfOf? }`. Captured on every revision from day one.                                                                                                                                                                                                                          | "author" as a free string                                                                             |
| **Large object**        | A blob at or above the large-object threshold (default 1 MiB). Same identity as any blob; transported individually rather than in packs.                                                                                                                                                                                                                                                      | "LFS file", "asset"                                                                                   |
| **Composed view**       | The rooted filesystem a consumer actually reads: checkout ∘ overlays ∘ mask. One function, all hosts, all consumers.                                                                                                                                                                                                                                                                          | "agent filesystem", "virtual fs", "RpcFileSystem" as a concept                                        |
| **Overlay**             | A read-only, non-project source composed into a view at a fixed path: built-in skills at `.agents/skills/<slug>`, dependencies at `node_modules`. Never stored in the project, never in revisions.                                                                                                                                                                                            | "mount" for an overlay (mounts are authority routing)                                                 |
| **Source** (provenance) | Where a composed entry comes from: `project` \| `dependencies` \| `system-skills`. Carried per entry by the composed view.                                                                                                                                                                                                                                                                    | "layer", "origin" (reserved for revision provenance), "provider" (a storage object)                   |
| **Records**             | Host-written bytes inside the project (`.tau/chats`, `.tau/runs`, `.tau/artifacts`…). Authored-tree residents that are _not versioned_ and are read-only to agents.                                                                                                                                                                                                                           | "system artifacts", "durable logs"                                                                    |
| **Cache**               | Regenerable derived bytes (`.tau/cache`, `node_modules`). Never versioned, never transferred, excluded from watches.                                                                                                                                                                                                                                                                          | "compute tier" for `.tau/cache` (the compute store is elsewhere)                                      |
| **Project session**     | The set of resources a client holds for one project while it is _live_: the composed views it serves, the kernel runtime client, the agent-host attachment, the revisions actor system, watchers, and durable-compute admission. One actor per project per client. Navigation never creates or destroys one; _Open_ and _Close_ do.                                                           |
| **Live / Closed**       | A project is live while its project session runs and closed otherwise. A chat is live while it holds a run, a lease, or a warm external-agent session. Closed keeps every record; live costs memory, workers, and connections.                                                                                                                                                                |
| **Attention**           | A chat state that needs the user: an approval or operator interrupt, a failed run, or a conflict. Attention is never silent and never inferred from time.                                                                                                                                                                                                                                     |
| **Versioned**           | Whether a path's bytes enter a revision. Derived from the path registry, never from a glob at a call site.                                                                                                                                                                                                                                                                                    | "in revisions", "authored" used as a boolean                                                          |

The operator-facing words are **Revision**, **Branch**, **Current**, **Restore**, **Switch**, **Merge**, **Discard**, **Work in**, **Sync**, **Tau Cloud**, **Built-in**, **Read-only**. Everything else in this table is engineering vocabulary. In particular, users never see "checkout", "lease", "backend", or "worktree": a checkout is presented as _the branch and who is working on it_.

---

## Layers

```text
L5  Porcelain        Files · Editor · Viewer · Parameters · Revisions pane · Chat links · Composer picker · CLI verbs · agent `revisions` tool
                     ─────────────────────────────── one checkout selection per workbench ──────────────────────────
L4  Composed view    composeView(checkout, overlays, mask) → RootedFileSystem + provenance per entry   (all hosts)
L3  Checkouts        live checkout (project dir)  ·  linked checkouts (<workspace>/.tau/checkouts/<project>/<id>)  ·  leases
L2  Revisions        one repository per project · BACKEND (RevisionPort) · branches · change ids · evidence/chat refs
                     REMOTES: git smart HTTP + LFS only · Git remotes · Tau Hosted Remote (git server in Tau API) · atomic receive
L1  Authority        WorkspaceFileService · mount table · PATH REGISTRY (one classifier) · watch planes
L0  Storage roots    DirectIdb · OPFS · WebAccess · Memory · Node fs
```

### L0 — Storage roots _(holds today)_

Unchanged. One provider per storage root; mounts route; no overlay provider exists and none is introduced (the mount-only ruling stands). The composed view (L4) is built _above_ the authority, not as a provider.

### L1 — Authority and the path registry

The authority gains one function that every other layer consults:

```ts
classify(path: RootedPath, ctx: { checkout: Checkout }): {
  class: 'authored' | 'records' | 'cache' | 'control-plane';   // Rule 16 storage class
  versioned: boolean;                                          // enters a revision
  agentAccess: 'read-write' | 'read-only' | 'hidden';          // composed-view mask
  watch: 'ui' | 'kernel' | 'none';                             // which watch plane
}
```

The registry is **the** table. Today the same knowledge is split across `revisionPathPolicy` (capture pruning), `generatedIgnoreContent` (`.gitignore` generation), the agent write mask, nineteen `node_modules` string checks in the UI, and four private copies of the read-only constant. In the target, all of those read `classify`. Rule 16's four storage classes become the registry's `class` column and stop being prose.

The registry is structural, not a glob: it is keyed by the project's reserved `.tau/**` layout (below), by the mount table (dependencies), and by the checkout kind (a linked checkout classifies its own `.tau/**` the same way the live one does).

### L2 — Revisions

- **One repository per project.** It lives in the project's revision control plane (`.tau/revisions/**` in the browser object store; `.jj`/`.git` on disk hosts under N10). It is `control-plane` class: hidden from agents, never in a composed view, never exported.
- **`RevisionPort` is the only interface.** The browser adapter is one implementation; jj and native git are others. Ports are widened, never bypassed (N24). Hosts record; agents edit files and never call the port (N26).
- **Identity.** A revision's id is its commit id. A branch is a ref. A candidate line has a change id so it survives rewrites. `Rev N` is a first-parent ordinal on the _selected branch_, computed at display time, never stored.
- **What enters a revision** is exactly the set of paths for which `classify(path).versioned` is true. Nothing else, on any host.
- **Refs beyond branches.** `refs/tau/evidence/<rev>` (compute evidence, N27) and `refs/tau/chats/<chatId>` (chat history shipped with the graph, N28) are the transport of records; the canonical chat file remains `.tau/chats/<chatId>/events.jsonl`.

#### Backends

A backend is a `RevisionPort` implementation that owns the local object store and refs of one project. Three exist; each host picks one per project at open time from the project's control plane, never per call:

| Backend                                      | Store                                                                                                                  | Hosts                                    | Status                                                                                                                                                                        |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Browser: `isomorphic-git` over the authority | a standard `.git` layout under `.tau/revisions/` in the project's storage root, through an `fs` shim over the provider | browser (Home, OPFS, WebAccess)          | **wired** (W3a, 2026-09-12): `createIsomorphicGitRevisionPort`; the hand-rolled store is deleted; stock `git fsck --strict` reads the store with Tau's headers (OQ-W21 ruled) |
| Native git                                   | `.git/`                                                                                                                | desktop, `tau serve`, CLI, cloud         | adapter implemented (worktrees, `update-ref` transactions, 655-line integration suite), reachable only through a dead wrapper; wrapped as a `RevisionPort` in this program    |
| jj over git                                  | `.jj/` with a colocated `.git/`                                                                                        | disk hosts once the spike blockers close | adapter and wasm algebra exist, constructed nowhere; removed from the tree in this program and re-created from history when the jj wave opens (EQ14)                          |

Plug-and-play means: the port is the seam, backends are chosen by configuration, and no consumer above L2 names one. jj's `Backend` trait is the same seam on the Rust side (GitBackend is its only production backend); Tau does not write a custom jj backend, it uses jj over git and gains jj's algebra without a second store. A cloud object store is **not** a backend: local working state needs local objects. The cloud enters as a git remote. Adopting `isomorphic-git` for the browser (OQ-W21) deletes the custom object writer, pack code, and transport that the review found missing or duplicated, and gives log, diff, merge, tags, and smart HTTP from one maintained dependency; revision identity stays Tau's own (`Tau-Metadata` trailer and change-id derivation in `revision-headers`).

#### Remotes

A remote is a peer revision graph reached over **the git protocol and nothing else**. The operator's ruling (EQ4, 2026-09-12): Tau does not invent a transport; the Tau API _is_ a git server, and every other remote is a git URL.

- **One wire.** Git smart HTTP (`info/refs?service=git-upload-pack`, `git-upload-pack`, `git-receive-pack`) plus the **git LFS batch API** for large objects. Disk hosts use native git (`git fetch`/`git push`, and therefore `jj git push`) with **zero Tau transport code**. The browser uses `isomorphic-git`'s smart-HTTP client, which already negotiates packs and has no LFS: the browser LFS client (`.gitattributes` matching, pointer clean/smudge at the composed-view seam, the batch API over the proxy) is Tau's own, sized in S49. GitHub, GitLab, Gitea, a bare repository on a shared drive, and the Tau Hosted Remote are all the same kind of remote to a client.
- **The Tau Hosted Remote is a git server in the Tau API.** It runs on Fly, where the API already runs as a container, with bare repositories on a Fly volume served by `git upload-pack --stateless-rpc` / `git receive-pack --stateless-rpc` behind better-auth (bearer token or the browser session). Refs move by git's own atomic update (`receive-pack` is all-or-nothing across refs; a rejected push leaves nothing fetchable). A `pre-receive` hook enforces the ref allow-list, entitlement, and quota. `git update-server-info` after every push keeps the dumb-HTTP read layout current for free. LFS objects live in R2 behind the batch endpoint (presigned upload/download URLs). Nightly `git bundle` snapshots of every repository go to R2 for disaster recovery. A pure-Node server over R2 with a Postgres object index remains possible later; the git binary is the standard library here.
- **Ref allow-list on the server**: `refs/heads/*`, `refs/tags/*`, `refs/tau/{chats,evidence,artifacts}/*`. Host-local refs (`refs/tau/workspaces/*`, `refs/tau/owners/*`, `refs/remotes/*`, `sync/*`) are rejected by the hook and never leave a host (N28).
- **Chats sync as refs** (`refs/tau/chats/<chatId>`, N28): connecting a Tau or Git remote is the chat sync carrier; there is no chat-specific sync plane. Clients read chats from the checkout's `.tau/chats/**` projection on every host (files-first ruling 4). Chat content leaves a device only through a remote the user connected; the Sync consent copy says chats are included, with a per-project _Sync chats_ toggle for projects that should stay files-only (files-first ruling 5, inverted from opt-in to opt-out at the moment of connecting a remote).
- **Remote choice, one at a time.** The Sync region offers _No remote_, _Tau Cloud_, or _Git remote_ (a URL, with GitHub sign-in as the credential when the URL is on GitHub, since GitHub is already a Tau identity provider). The data model is git's own remotes list, so several remotes are representable and a later program can expose them; the pane shows one.
- **Browser → Git remote** goes through the Tau API's CORS proxy for git smart HTTP (the standard `isomorphic-git` deployment pattern, a few dozen lines of forwarding), never a Tau-specific relay protocol. Credentials for third-party remotes are host-resolved: the GitHub OAuth token from the Tau session in the browser, the keychain on desktop, a credential helper on the CLI. No secret is written under a project or workspace directory.
- **A sync conflict** (remote ref moved) is handled by fetch into `refs/remotes/<remote>/*` followed by the ordinary branch merge; a conflict lands on a host-local `sync/<remote>/<branch>` and shows on the Sync row as _Needs resolution_ (see Conflicts). There is no server-side merge.
- **Other blob-like stores are not remotes.** S3, Dropbox, and IPFS have no git transport; exposing them as client-side remote kinds would mean re-implementing a git server per store. They enter, if ever, as (a) server-side storage for a user's Tau-hosted repositories (bring-your-own bucket for LFS objects), or (b) a bare repository on a folder a desktop sync client already mirrors (`git push` to `~/Dropbox/designs.git` works today on disk hosts with no Tau code), or (c) read-only exports of the dumb-HTTP layout for sharing. None is in the first program; the design does not preclude any.

```text
Tau Hosted Remote (apps/api, Fly)
  GET  /v1/git/<projectId>.git/info/refs?service=git-upload-pack|git-receive-pack   (auth: session | bearer)
  POST /v1/git/<projectId>.git/git-upload-pack                                        → git upload-pack --stateless-rpc
  POST /v1/git/<projectId>.git/git-receive-pack                                        → git receive-pack --stateless-rpc
       pre-receive: allow-list · entitlement · quota (413 with the shortfall) · never parses chat bytes
       post-receive: update-server-info · publication materialization (below) (the `git bundle` snapshot to R2 is the nightly job, not a hook step)
  POST /v1/git/<projectId>.git/info/lfs/objects/batch                                  → presigned R2 upload/download
  GET  /v1/git/<projectId>.git/{HEAD,objects/**,refs/**}                               dumb-HTTP read-only layout, stock git can clone
  POST /v1/git/proxy?url=<https git remote>                                            CORS proxy for browser clients
```

#### Named versions and publications

- A **named version** is an annotated tag: `refs/tags/<name>` → revision, with `{ note, actor, createdAt }`. Users create one from History (_Name this revision_) or _Where you are_ (_Name…_); hosts create one at every publish. Names are unique per project, renameable, deletable; the revision they point at is immutable.
- A **publication** is a Tau Cloud record `{ projectId, tag, revisionId, visibility, sharedEmails }`. Publishing = ensure the graph up to that revision (and its large objects) is pushed to the Tau Hosted Remote, create the tag if absent, then create or re-point the publication. The API then **materializes the tagged tree server-side** from the pushed pack into the publications service's existing sha256-keyed blob store, so the shipped viewer paths stay exactly as they are: public viewers keep direct CDN URLs (the public cost model depends on them), private viewers keep the grant-rechecking proxy, visibility flips keep moving bytes between tiers. Nothing is uploaded twice; the graph is the source and the blob store is a derived, server-owned projection of one revision. Republishing points the same URL at a new tag; the tag history _is_ the publication history. Today's multipart upload is retired after existing publications are migrated (manifest → revision + tag, no serving change). `tau publish <tag>` is the CLI verb and the dogfood path for `tau-examples`.

#### Checkpoint policy (when edits become revisions)

Prior art converges: Onshape and Google Docs save continuously and mark versions explicitly; Figma and VS Code local history checkpoint on a quiet window and collapse the automatic ones. Tau adopts the same split. Files are always saved to the checkout as they change (files-first); the question is only when a revision is minted.

| Trigger                      | When                                                                                                                            | Gate                                          | Display                                                                         |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------- |
| `save`                       | `Mod+S` anywhere in the workbench (editor buffers flush first), palette _Save revision_                                         | tree differs from head                        | normal row                                                                      |
| `idle`                       | 5 minutes after the last write with no further writes (OQ-W12; the checkpoint window, distinct from EQ15's liveness idle-close) | tree differs from head                        | collapsed: consecutive idle rows fold into one `n autosaves` row until expanded |
| `hidden`                     | the tab becomes hidden (`visibilitychange`) after a write — the real close in the browser (A39); W6                             | tree differs from head                        | folded with idle rows                                                           |
| `turn`                       | a turn finalizes                                                                                                                | always (a turn with no changes mints nothing) | normal row, chat card                                                           |
| `merge`, `restore`, `switch` | before and after the operation as the operation requires                                                                        | tree differs from head                        | normal row                                                                      |
| `close`                      | the tab is being unloaded (`pagehide`) or the desktop app is quitting, after a write                                            | tree differs from head                        | folded with idle rows                                                           |
| `import`                     | project import — `source: import` on the row; not a `checkout.machine` cut trigger until project import lands on the port       | always                                        | normal row                                                                      |

The gate is one comparison: the cut's `treeId` against the head's `treeId`, both content hashes, so "only when something changed" is exact and cheap. It is only as good as the registry: `thumbnail.webp` (rewritten on every render) is evidence, and `.tau/types/**`, `.tau/tsconfig.generated.json`, `.tau/lockfile.json` are generated, so none of them is versioned and none can trip the gate. Two tabs passing the gate at once mint two candidates; the second CAS fails and that tab drops its revision and re-reads (the existing live-tree Web Lock fences the cut). Idle revisions are immutable like every other revision (no amend, no rewrite); the graph stays append-only and dedupes by content, and display does the folding. A `save` with nothing changed shows _Nothing changed since Rev N_ and mints nothing. On a shared checkout the idle timer is per checkout, not per client.

#### Continuous sync and the close flush

The expectation: finish work in a browser tab, close it, sign in to the desktop app on another machine, and continue from exactly where the tab left off, chats included. A browser gives no guarantee of work after a tab closes, so the design never depends on the close moment carrying more than the last few seconds of work.

- **Push on every revision.** While a remote is connected, every minted revision (any trigger) schedules a push within a short debounce (2 s), coalescing bursts. The push set is two sets, not one (review 3 F4): the **history set** (`refs/heads/*`, `refs/tags/*`) pushes atomically on native git (`--atomic --force-with-lease`) and ref-by-ref in the browser with `main` last and the whole set retried as a unit; the **record set** (`refs/tau/{chats,evidence,artifacts}/*`) pushes per ref, and a rejected record ref never blocks the history set — it lands in the durable queue and shows as a chat-specific state. LFS objects upload before the ref push that references them, so a pointer never lands ahead of its object. There is no separate "sync now" state to remember; _Sync now_ only forces the debounce to zero. The Sync row reads `Backed up` or `Backing up… n revisions` or `Not backed up · n revisions` at all times.
- **Close flush.** `visibilitychange: hidden` is the real close in the browser (review 3 F5): through the existing `useFlushOnClose` registry, after the editor and chat flushes it already runs, the host sends `close` to `checkout.machine`, which mints a `close` revision if the tree differs from the head, and to `sync.machine`, which pushes with zero debounce while the page is still alive and `await` works. `pagehide` carries only a **precomputed, already-serialized single POST** under `fetch(..., { keepalive: true })` — the history-set pack built at `hidden` if it was not acknowledged by then, at most 64 KiB, never chats and never LFS — because a smart-HTTP push is two round trips over async object reads that cannot start after the document is gone. Desktop uses `before-quit` as the first step of the existing shutdown promise. On desktop, quit is held while native `git push` runs, with a window that says `Backing up 2 revisions…` and a _Quit anyway_ escape after a bounded wait; the app then quits. _W13-a2 (P32, P35):_ the `pagehide` re-send is browser-only by construction — the native leg pushes by spawning `git` and has no `pagehide`; on desktop the close flush is the quiesce (`awaitSyncSettled`, awaited by the worker's `release()` and the Node host's `flushClose()` and, through the utility, by the quit hold), and the recorder is armed only around the history-set receive-pack POST.
- **Durable queue.** A push not acknowledged by the server stays recorded in the checkout's control plane (`.tau/revisions/sync-pending`) and is retried on the next open of the project on that device, before anything else. The state is visible, never silent: the Sync row and the header chip show `Not backed up` until the acknowledgement arrives.
- **Open pulls first, under a bound.** Opening a project that has a remote fetches before the first render of the tree, with three exits owned by `sync.machine`'s `opening` state (review 3 F16; W13 landed them there and P34 keeps them there — `project-session` never holds a second copy; the page's first tree render waits while the sync facet reads `checking`, i.e. for the 3 s render window or the fetch's completion, whichever first): when `navigator.onLine` is false the pull is skipped and `sync.machine` pulls on the `online` event; after 3 s without an answer the local tree renders and the pull continues in the background; after 10 s total the pull is aborted (a per-request `signal` on `RevisionHttpRequest`, P36 — the client-wide `signal` alone bounded only this host's wait), queued, and the tree renders. Both values are `project-session` policy, revisable like the idle window; the Sync row shows `Checking…` during the first window and the ordinary states after. A clean live checkout fast-forwards to the remote head; a dirty one merges by the ordinary rules and a conflict lands on `sync/<remote>/<branch>` (A22); a leased live checkout is never re-based (A2), so a running turn finishes first and the merge follows. Chats appear because `refs/tau/chats/*` came with the fetch and **the fetch path writes the `.tau/chats/**` projection\*\* through the authority with the host originator (A30, review 3 F1); the chat writer never does.
- **What "where I left off" means in this program**: the same files, the same revision history, the same branches and named versions, the same chats with their cards. Which chat, files, and pane were open is client UI state and is not synced; a later program may store it as a record.

#### Conflicts

- Conflicts arise in exactly two places: merging a branch into another, and syncing when the remote's ref moved. Both funnel into the same three-way merge. A sync conflict is recorded on a host-local branch `sync/<remote>/<branch>` and shown on the Sync row as _Needs resolution_; a conflicted revision is never pushed (I-CONF).
- A conflicted merge mints a **conflicted revision on the source branch** (jj-style: conflicts are recorded, never left as loose files) and never touches `main` or the live checkout. The pane shows _Needs resolution_ on that branch with per-file rows: _Keep mine_, _Keep theirs_, _Open in editor_ (text files, conflict markers), _Ask chat to resolve_ (starts a turn attached to that branch's checkout with the conflict record as context). Binary and parametric files (STEP, meshes, `.tau/parameters/**`) offer choose-one only.
- Resolution mints a normal revision on the branch; the user then merges or syncs again. Nothing is auto-resolved.

#### Large-object policy

- Large objects go through **git LFS**, the standard every git client and host already understands, instead of a Tau-specific path. Tracking rules are generated into `.gitattributes` from the path registry (derived, like the ignore file): binary types always, and any file at or above 1 MiB (the jj `snapshot.max-new-file-size` default) by a size filter. Trees then reference LFS pointers; the bytes live in the LFS store of whichever remote the project syncs to (R2 for Tau Cloud, GitHub's LFS for a GitHub remote, with its quota).
- **What the user sees** (EQ7): nothing about LFS. Identity, restore, compare, and history are unchanged. The Sync region shows storage against the plan (`2.1 GB of 10 GB`) and a push over quota is refused with the list of files that would exceed it. The registry separates large _authored inputs_ (a scanned mesh, an imported STEP) which always sync, from large _generated outputs_ (`exports/**`, render captures), which are evidence and are not synced by default; a project-level _Sync large exports_ toggle exists for people who want them backed up. Derived previews (`thumbnail.webp`) are evidence and never authored bytes.
- Text and parametric sources stay far below the threshold; imported STEP and meshes are the expected LFS objects. A GitHub remote's LFS limits are surfaced on the remote row before the first push rather than discovered as a rejection.

#### Retention and garbage collection

| What                                                   | Rule                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Revisions and tags                                     | immutable; never collected while reachable from any ref                                                                                                                                                                                                                                                          |
| Unreachable objects (after a branch or tag is deleted) | **not collected locally in this program**: Restore-by-id, chat cards, and conflict evidence reach revisions no ref names. If local GC returns, reachability includes every revision named by `refs/tau/chats/*` and `.tau/runs/*`. Blob remotes sweep after repack only what no allow-listed ref reaches         |
| Linked checkouts                                       | removed only by _Discard_, and _Discard_ is gated on the checkout's tree equalling its head (dirty → offer _Save revision_ first); turn end never removes a checkout, even on the Node host; a checkout whose branch was merged more than 30 days ago is offered for removal in the pane, never removed silently |
| Leases                                                 | retired at turn end (the checkout stays); liveness is the existing live-tree Web Lock and the host start sweep, no heartbeat; a lease carries the authority epoch (N3) so a lease from a superseded epoch is _stale_ and retired on the next prepare                                                             |
| Tau Hosted Remote repositories                         | `git gc` after push via the hook; LFS objects unreferenced by any reachable pointer are collected nightly with the bundle snapshot                                                                                                                                                                               |
| Quotas                                                 | Tau Hosted Remote counts packs and large objects against the plan; a push over quota is a 413 with the shortfall, never a partial write                                                                                                                                                                          |
| Evidence refs                                          | owned by the compute-reuse charter's retention rules                                                                                                                                                                                                                                                             |

#### Attribution

- Every revision carries an **actor**. For a signed-in user: `{ kind: 'user', id, email, name }` from the session. For an agent: `{ kind: 'agent', id: <model>, runId, onBehalfOf: <user actor> }`. For a user who chose anonymity or is not signed in: `{ kind: 'user', id: 'anon:<stable per-workspace hash>', anonymous: true }` with no email.
- Mapping to native git: author = the user actor (`Name <email>` or `Anonymous <anon-hash@users.noreply.tau.new>`), committer = `Tau <noreply@tau.new>`, trailers `Tau-Actor: agent <model> run <runId>` and `Tau-Trigger: <trigger>`. Stock git shows sensible authorship; Tau reads the trailers.
- Anonymity is a workspace-level setting and a per-publication choice; switching it never rewrites history.

#### Diffs

_Compare_ opens the existing text diff viewer (the Shiki-based `DiffViewer` used by chat file cards) for revision-vs-revision and revision-vs-checkout, per file, on three surfaces: History hover, the chat card's file rows, and conflict rows. Geometry diff in the viewer is a later, separate capability with the same inputs.

#### Undo

Undo is within a checkout's unsaved state; revisions are across it. The editor's undo stack never crosses a revision boundary, and _Restore_ offers its own one-level _Undo_ toast.

### L3 — Checkouts

A checkout is git's worktree and jj's workspace, made addressable. It answers the question _where does work happen_, separately from _which line of history is it on_ (branch) and _who is doing it_ (lease).

- **The live checkout is the project directory** (git's main worktree). It tracks `main` by default.
- **Linked checkouts** are additional working copies, one per branch, materialized in the host-private area. They are _outside_ the project directory, so nothing about them needs excluding from capture, export, watches, or the agent mask by path spelling — physical separation supplies the exclusion (the jj spike's F22). On native backends they _are_ `git worktree add` / `jj workspace add`; the browser backend implements the same registry with directories. The private area is per host: on browser storage roots it is `<storageRoot>/.tau/checkouts/<projectId>/<checkoutId>/`, reached through a **checkout route** `/checkouts/<checkoutId>` installed from persistent project-root configuration (authority Rule 3, same storage root, class `authored`, invisible to discovery) so `createRootedFileSystem('/checkouts/<id>')` needs no new capability; on disk hosts it is the host data directory the compute-reuse charter already chose for its private store (D9), never any served directory, because on `tau serve` the served directory _is_ the project and native git refuses a worktree inside its own worktree.
- **One checkout per branch.** This is git's rule and it keeps the UI honest: a branch row _is_ its checkout, and "who is working on `fillet-exploration`" is the set of leases on that branch's checkout. (jj permits many workspaces per change; Tau does not expose that.)
- **Chats attach to a checkout by id, not by mode.** A chat record carries `checkoutId`. The first turn of a new chat attaches to the live checkout when no _other_ chat's lease holds it, and otherwise creates a linked checkout on a new branch. A user can re-attach a chat to any checkout between turns from the composer picker. Subagents spawned by a chat inherit its checkout; peer chats deliberately collaborating on one task attach to the same checkout by selecting the same branch. Concurrency inside one checkout is what it is in git: one tree, last write wins per file, one revision per turn capturing the whole tree with every active lease named in its provenance.
- **The workbench attaches too.** The workbench's checkout selection is just another attachment; "Current" in the Revisions pane marks the branch whose checkout the workbench reads.
- **Switch and Restore respect leases and the one-checkout-per-branch rule.** If a lease holds the live checkout, Switch never re-bases it: it creates (or reuses) the branch's linked checkout and re-roots the workbench there. Restore applies a revision to the _selected_ checkout and lets it track the revision's branch only when no other checkout tracks it; otherwise the checkout is `detached`. The registry enforces this at `addCheckout` and at `setHead`.
- **The checkout registry lives in the control plane** (`.tau/revisions/checkouts/<id>.json` on the browser backend; `.git/worktrees/` and `.jj/` natively) and is reached only through `RevisionPort.listCheckouts / addCheckout / removeCheckout`.
- **A checkout never becomes a revision.** Finalizing a turn on a checkout _mints_ a revision from the checkout's versioned paths and advances the checkout's branch by compare-and-swap. A linked checkout persists while its branch has work; after merge it is disposable and "Discard" removes both.
- **Durable state of a project = revision graph + refs + live checkout.** Linked checkouts, leases, composed views, tree caches, `Rev N` numbers, dirty flags, and provenance labels are all regenerable from those three, and linked checkouts with unfinalized edits are the one thing whose loss loses work, which is why a turn always finalizes or discards.

### L4 — Composed view

```ts
composeView(checkout: Checkout, opts: { consumer: 'agent' | 'user' }): ComposedView
// ComposedView = RootedFileSystem + { provenance(path): Provenance }
// Provenance = { source, versioned, agentAccess, identity?, overrides? }  — agentAccess is the agent's access (P10); user gating reads source/versioned
```

- **Same function, all hosts, both consumers.** Browser worker, `tau serve`, the Electron utility, and the CLI call the same `composeView`. The agent's tool filesystem and the Files pane are two callers of one function over the same checkout; they cannot disagree.
- **Overlays** are read-only and are never stored in the project: built-in skill bundles at `.agents/skills/<slug>` (identity `skill:<slug>@<version>#<fingerprint>`), dependencies at `node_modules` (derived; regenerated per host, N18). A project-authored path under an overlay's root _overrides_ the overlay as a whole bundle (never a per-file merge); the composed entry then reports `source: 'project'` with `overrides: <overlay identity>`.
- **The mask differs by consumer, the tree does not.** For the agent, `records` paths are read-only and `control-plane` paths are hidden. For the user, records are visible, dimmed, and editable only through host UI. The set of paths is the same.
- **Provenance is data, labels are catalog.** The view returns `{ source, versioned, agentAccess }` (`agentAccess` is the agent's access; user-facing refusal is `source !== 'project'`, dimming is `versioned === false` — ruling P10, W2 review R2); the UI maps that tuple to copy through one catalog. No presentation string crosses the wire.
- **Events ride the view.** A composed view owns a rooted event port. Writes through the agent's view reach the UI's view of the same checkout through the authority's rooted watch plane, on every host. There is no path on which an agent write is invisible to a UI reading the same checkout.

### L5 — Porcelain

One workbench (one project route, one lane set) has **one checkout selection**. All of Files, editor tabs, viewer, parameters, main-file selector, and the Revisions pane read that selection through `composeView(selected)`. Selecting is one verb, **Switch**, with two implementations the user never distinguishes: if the target branch has no linked checkout, the live checkout applies it (git switch, DT1 as shipped); if it does, the workbench re-roots every consumer at that checkout (`cd` to the worktree). Because every consumer already takes a rooted view, re-rooting is re-issuing views, not new plumbing. The chat's composer chooses which checkout the _next turn_ works in; the workbench follows the chat's checkout by default and can be pinned elsewhere.

| Surface                                | Reads                                         | Verbs it exposes                                                                                                                               |
| -------------------------------------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Workbench header chip                  | `describe(selected)` + active chat's checkout | shows `⎇ main · Rev 12` at all times; click opens Revisions; `Follow chat` when the chat works on another branch; the branch popover is Switch |
| Files tree, editor, viewer, parameters | `composeView(selected, 'user')`               | open, edit, create, rename, delete (refused when `source !== 'project'`, dimmed when `versioned === false`), reveal                            |
| Chat file links, skill rows            | same view                                     | open + reveal; a `system-skills` entry opens read-only                                                                                         |
| Revisions pane · _Where you are_       | `describe(selected)` + default remote status  | Save revision, Name…, Return to latest, Discard changes, Sync                                                                                  |
| Revisions pane · _Branches_            | `listRefs()` + `listCheckouts()` + leases     | Switch, New branch, Merge into `<current>`, Discard; _Needs resolution_ rows: Keep mine, Keep theirs, Open in editor, Ask chat to resolve      |
| Revisions pane · _History_             | `log(selected.branch)` + tags                 | Restore, Compare, Name this revision, Publish; idle rows fold into `n autosaves`                                                               |
| Revisions pane · _Sync_                | the one remote + `listRemoteRefs`             | Sync now, Connect Tau Cloud, Connect Git remote, Disconnect; per-project toggles Sync chats, Sync large exports                                |
| Publications (existing dialog)         | tags + publication records                    | Publish `<named version>`, re-point, visibility                                                                                                |
| Composer picker                        | chat.checkoutId + `listCheckouts()`           | Work in: `<branch rows>` / New branch                                                                                                          |
| Agent tools                            | `composeView(turnCheckout, 'agent')`          | read, write, list, glob, grep; `EROFS` on read-only                                                                                            |
| Agent `revisions` tool                 | same L2–L3 as the CLI                         | log, diff, describe (read-only; branch/merge/sync stay user verbs)                                                                             |
| CLI                                    | same L2–L4                                    | `tau revisions log \| switch \| branch \| merge \| diff \| discard \| remote \| sync`                                                          |

Host-attested revision events (`turn.finalized { turnId, runId, chatId, projectId, checkoutId, revisionId?, branch?, treeId?, changedPaths, trigger, runIds }` — declared once in `/revisions/revision-effects` and built only by `describeTurnSettlement`; its siblings `turn.conflicted` and `turn.failed` carry the same identity fields and are surfaced, never dropped; until the root emits `turnReleased` (W6) both hosts observe a failed turn through the one `watchFailedTurns` compensation — W5 review R12) are the only admitted input to revision cards (R-UX2). The transcript-derived timeline is retired.

#### The Revisions pane

One pane, four regions, each revealed only when it has something to say. Hobbyists see one; software engineers see four.

| Region            | Always shown | Shown when                                   | Content                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------------- | ------------ | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Where you are** | yes          | —                                            | `main · Rev 12 · Current`; `Modified` with `Save revision` (`Mod+S`) when the checkout differs from its head; a sync glyph (`Synced` / `3 to upload` / `Not backed up`); `Return to latest` when detached                                                                                                                                                                                                   |
| **Branches**      | no           | a user has created a branch                  | one row per unmerged branch: name, head `Rev N`, chips for the chats working there, ahead/behind `main`, hover verbs Switch · Merge into `<current>` · Discard; `Needs resolution` state with per-file resolution verbs; merged branches drop out of the list; `New branch` lives in the pane's overflow until the first branch exists                                                                      |
| **History**       | yes          | —                                            | newest-first rows for the selected branch grouped by recency (_Today_, _This week_, _Earlier_), with _Earlier_ collapsed by default; `Rev N`, when, who (you / chat name / merge), named-version tag when present, file summary with `+N −M`; consecutive autosaves fold into one row; a single graph rail that grows a second lane only at a fork or merge; hover verbs Restore · Compare · Name · Publish |
| **Sync**          | no           | a remote exists, or the user opens _Connect_ | one remote row: `Tau Cloud · Backed up · 2.1 GB of 10 GB` (or `Backing up… 1 revision` / `Not backed up · 2 revisions`) or `GitHub · origin · 2 ahead`; `Sync now`; the choice _No remote / Tau Cloud / Git remote_; a _Sync chats_ toggle and a _Sync large exports_ toggle per project                                                                                                                    |

Outside the pane, one compact **header chip** in the viewer's action cluster shows the workbench's checkout at all times (`⎇ main · Rev 12`). It exists because the chat may be working on a different branch than the panes show; the chip carries a `Follow chat` action in that case and nothing otherwise. It generalizes the existing conditional `RevisionStatusAction` rather than adding a surface.

Vocabulary in the pane stays at the level of a document editor: revision, branch, restore, merge, sync. Git words (`worktree`, `ref`, `HEAD`, `remote` as a noun) never appear; `origin` appears only as the name a git remote already has. The agent's card in chat reads the same _Where you are_ line.

---

## How the app drives each layer

### A turn, end to end

```text
prepare(chat)
  ├─ checkout = chat.checkoutId ?? placement policy:
  │     live checkout if no other chat's lease holds it, else addCheckout(branch: new name) from head(main)
  │     (a subagent inherits its parent's checkout; the user may pick any branch in the composer)
  ├─ if the checkout is dirty vs its head → mint base revision (source 'user') first
  └─ write lease .tau/runs/<runId>.json { checkoutId, chatId, baseRevisionId }

run
  └─ agent reads/writes composeView(checkout, 'agent'); every other attachment to the checkout (workbench, peer agents)
     sees each write via the rooted watch plane

finalize
  ├─ capture = versioned paths of the checkout (registry-driven)
  ├─ revision = writeRevision({ parents: [base], tree: capture, provenance: { source: 'agent', runIds: leases on this checkout } })
  ├─ CAS checkout.branch ← revision           (main for the live checkout; the linked checkout's branch otherwise)
  ├─ emit turn.finalized (host-attested) → chat card "Rev N"
  └─ retire lease

merge (user verb "Merge into main")
  ├─ three-way merge(head(main), head(branch), mergeBase) → apply to main's checkout
  ├─ conflict → both branches kept, conflict record surfaced, checkout untouched
  └─ success → merge revision { parents: [main, branch], source: 'merge' }; CAS main; the linked checkout is disposable

switch (user verb "Switch")
  ├─ branch has a linked checkout → workbench re-roots at it
  └─ otherwise → apply head(branch) to the live checkout; live tracks branch

restore (user verbs "Restore", "Return to latest")
  └─ apply revision R to the selected checkout; it tracks R's branch, or 'detached' when none names R

sync (user verb "Sync", default remote)
  ├─ fetch(remote) → for each tracked branch: fast-forward, or merge through the merge path above
  └─ push(remote, tracked refs + refs/tau/*, expected) → 409 → fetch and retry once, then surface
```

### Where the logic lives: machines and effects

Today the revision lifecycle is spread across three shapes: a UI XState machine (`revision.machine.ts`, transcript-derived graph plus the restore flow), a 2,100-line React provider that choreographs prepare/finalize with promises and Web Locks (`chat-workspace-authority-provider.tsx`), and a closure-based decorator with maps of promises on the Node host (`withTurnRevisions` in `packages/host`). The browser and the disk hosts therefore run two different implementations of one lifecycle, and only the smallest of the three is testable without a DOM or a filesystem.

The target puts every **stateful** revision flow in a host-neutral XState machine and every **effect** behind the port:

| Machine (`packages/revisions/src/<name>.machine.ts`, P8)                 | Instance                                     | States                                                                                                                                                                                                                                     | Events in                                                                                                                                                                                 | Emits                                                                                                                                                                                   | Replaces                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------ | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `turn.machine`                                                           | one per turn                                 | `preparing → leased → finalizing → finalized \| conflicted \| released \| failed`                                                                                                                                                          | placement via `input` at spawn, `turnCompleted`, `turnAbandoned`, `release`, `leaseGranted` / `leaseRefused`, and the cut outcomes `revisionMinted`                                       | `nothingToSave`                                                                                                                                                                         | `cutFailed`                                                                                                                                                                                                                                                               | `casLost` | `turnFinalized`, `turnConflicted` (sent to the parent and emitted for cards; the host event on the wire is `turn.finalized`) | the provider's prepare/finalize promise chain and the host's former `withTurnRevisions` maps (retired; the Node seam is `createProjectRevisions(...).record(launcher)`); the same machine runs on both |
| `checkout.machine`                                                       | one per checkout; **the only minter**        | `clean → dirty → minting → clean \| dirty` (a write generation counter decides which), `stale → rereading` for the CAS loser (D24), `failed` non-terminal; `after: idleWindow` in `dirty.quiet`                                            | `changed { paths, generation }` (one per content-change event, never per path), `save`, `hidden`, `close`, `cut { trigger, runIds }` from turns and branch/restore/sync ops               | `revisionMinted { trigger, revisionId }`, `nothingToSave`, `cutFailed`, `casLost` (D24), `checkoutStatusChanged` (sent to the parent only; the root coalesces it into `RevisionStatus`) | the ad-hoc dirty flag and the planned `checkpoint-scheduler`                                                                                                                                                                                                              |
| `sync.machine`                                                           | one per project                              | `noRemote → backedUp → pending (after: debounce) → pushing → backedUp \| queued (retry backoff) \| conflicted \| failed`; on open `fetching → fastForwarding \| merging`                                                                   | `revisionMinted`, `syncNow { pushId? }`, `close`, `remoteConnected/Disconnected`, `online/offline`, `pushAcknowledged/Failed`                                                             | `syncState` (**settled** values only: backedUp / pending / queued / conflicted, never a backoff tick), `pushSettled { pushId, outcome }` sent to the requester, `syncConflict`          | the planned `sync-scheduler`; nothing exists today                                                                                                                                                                                                                        |
| `restore.machine` (the existing `revision.machine` restore states, kept) | one per project                              | `idle → planning → planned → confirming → applying → applied \| failed`                                                                                                                                                                    | `restore`, `returnToLatest`, `undo`, `confirm`, `cancel`                                                                                                                                  | `toast.*`, `checkoutChanged`                                                                                                                                                            | itself, re-rooted on the graph instead of transcripts (S10, S19)                                                                                                                                                                                                          |
| `branch.machine`                                                         | one per project                              | `idle → checking → confirming → applying → applied \| conflicted \| failed`                                                                                                                                                                | `switch`, `merge`, `discard`, `create`, `rename`, `confirm`, `cancel`                                                                                                                     | `checkoutChanged`, `branchMerged`, `mergeConflicted`, `toast.*`                                                                                                                         | the provider's `checkout`/`mergeBranch`/`deleteBranch` promises and the pane's `isBusy` flag; _Switch_ is the one `checkout(revision)` implementation shared with `restore.machine` through the same injected actor (A2, D10); _Discard_ is guarded on a clean tree (A25) |
| `resolution.machine`                                                     | one per conflicted revision, keyed by its id | `open → resolving → finishing → resolved \| abandoned`                                                                                                                                                                                     | `keepMine(path)`, `keepTheirs(path)`, `openInEditor(path)`, `resolvedInEditor(path)`, `askChat`, `finish`, `abandon`                                                                      | `conflictResolved { revisionId }`, `turnRequested`                                                                                                                                      | nothing today (S33); _Ask chat to resolve_ spawns a `turn.machine` seeded with the conflict                                                                                                                                                                               |
| `checkouts.machine`                                                      | one per project                              | `recovering` (invokes `sweepLeases`, the epoch comparison) `→ ready \| failed`, with `adding`/`removing` sub-states                                                                                                                        | `open`, `addCheckout`, `removeCheckout`, `leaseStale { runId }` (sent by `turn.preparing` on a superseded epoch), `turnFinalized`                                                         | `checkoutsChanged`, `leaseRetired`, `removalOffered`                                                                                                                                    | the provider's `reclaim`/`reclaimAll` and the host's `sweepTurnWorkspaces`; owns the checkout registry (S22), stale-lease retirement and removal offers (S36 local)                                                                                                       |
| `remote.machine`                                                         | one per project                              | `reading → none → choosing → authorizing → validating → initialSync → connected \| failed`, `disconnecting` (A40)                                                                                                                          | `connect { kind, url? }`, `authorized`, `validated`, `disconnect`, `cancel`, `quotaRefused { paths }` (A40)                                                                               | `remoteConnected`, `remoteDisconnected`, `toast.*`                                                                                                                                      | nothing today (S34); the sibling of the existing `workspace-connection.machine` one layer down; authorization is an injected actor (browser OAuth popup for GitHub scope, desktop keychain, CLI credential helper)                                                        |
| `publish.machine` (existing, moved)                                      | one per project                              | `idle → choosingVersion → tagging → pushing → publishing → success \| error` (W8: the tag exists before it is offered — P39 folds the order; P38: the tag ref is pushed under its lease, so a moved name is a leased re-point, never `-f`) | `publish { tag? }`, `confirm`, `cancel`, `pushSettled { pushId, outcome }` (the root forwards the sync child's settle verbatim; `publish` decides `error` on a non-success outcome — P39) | `published`, `toast.*`                                                                                                                                                                  | `pushing` sends `syncNow { pushId }` and waits for the correlated `pushSettled` with a bounded `after` → `error`; the UI dialog and `tau publish` wrap the same machine (S32)                                                                                             |

These compose as **one actor tree per project**: a root `project-revisions.machine` is the single actor a host creates. The six always-on children (`checkouts`, `sync`, `remote`, `branch`, `restore`, `publish`) are **invoked** for the root's whole life, so they stop with it; the variable-count children (`checkout` per registered checkout, `turn` per admitted turn) are spawned with a stored `ActorRefFrom` in context and stopped with `stopChild` on removal and on root `exit`. Children get `parentRef` through `input` and address siblings by sending to the parent, which routes (the `project.machine` precedent); **`systemId` is not used**, because the app is one XState system and a second live project would collide. The root also owns the **workbench selection**: `selectedCheckoutId` and `follow: 'chat' | 'pinned'` in context with `switch { branch }`, `followChat`, `pinTo { checkoutId }` events and the D10 apply-to-live-versus-re-root guard, which needs `checkouts`' lease set. `turn.finalized` has three consumers of two kinds: a **send** to the parent (routed to `checkouts`, the checkout, and the chat's session) and an **emit** for the chat card. Where the tree runs is decided, not open: `project-revisions` and its children run **in the file-manager worker** beside the authority, the object store, and the hashing (so tree-hash gates never cross the bridge), and the worker publishes one coalesced `RevisionStatus` projection per project (`{ projectId, checkoutId, branch, dirty, minting, headRevisionId, follow, attention, restore, remote } — no ordinal on the wire, `Rev N` is derived at read from the first-parent walk (I3; W5 review R12)`) that React reads through a stable selector; the UI worker of the agent host and the Electron utility get the same machines with their own actors. `sessions`, `project-session`, and `chat-session` run on the main thread, since they own main-thread resources.

Rules:

- **Machines import no filesystem, git, React, or DOM.** Effects (`cut`, `push`, `fetch`, `computePlan`, `applyPlan`, `writeRecord`, `persistQueue`, `sweepLeases`) are named actors the host supplies through `machine.provide({ actors })`, the pattern `RevisionProvider` already uses for `computePlan`/`applyPlan`. One-shot effects are promise actors (`fromSafeAsync` in the app, `fromPromise` with `signal` in packages); **the lease is a `fromCallback` actor**, because the Web Lock is held for the callback's lifetime across the event-driven `leased` state and is not reentrant; one lease actor per checkout is the lock's only owner. No function lives in context (no `persist` callback): every write is an actor whose inputs the test asserts. Every machine has a `failed` state or a non-terminal failure edge for each invoked effect. A machine test uses fake actors and a fake clock, asserts states, sent and emitted events, and actor inputs, and runs in plain Node with no jsdom.
- **Coalesce at the seam, debounce in the machine.** The content-change stream sends one `changed { paths, generation }` per change event, never one per path; a 50-file batch is one transition. Token deltas never enter a machine: `run.lifecycle: running` alone means generating, and tool-part transitions are batched per event by the transport adapter. The sync push debounce and the idle window are policies the machines own. `syncState` carries settled values only, so a backoff tick never re-renders eight project rows.
- **One machine, every host.** The browser (in the UI worker or provider), the Electron main process, `tau serve`, and the CLI instantiate the same machines with their own actors. Host-attested `turn.finalized` (with `turn.conflicted`/`turn.failed`, A4) is the `turn.machine`'s emitted settlement on every host, so the schema is one by construction, not by convention.
- **UI machines stay in the UI.** `file-manager`, `project`, `editor`, and `chat-persistence` machines keep their ownership; the revision machines connect to them by events, not by shared context: the content-change stream feeds `checkout.machine.fsWrite` (the seam `RevisionProvider` already wires), the editor's `flushNow` completes before `close` is sent, `sync.machine.syncState` drives the pane and chip.
- **Close and quit reuse the hooks that exist, with ordering made real.** The browser's `UnloadProvider` / `useFlushOnClose` registry stays the single listener, but its callback becomes `(reason: 'hidden' | 'pagehide') => void | Promise<void>` and it runs in two phases: flush registrants (the editor's existing `ProjectSessionFlushRegistration.flush()`, which awaits `storing: idle`, and the chat actors) settle first, then the session registrants receive `hidden` (→ the idle trigger) or `pagehide` (→ `close`). Insertion order is never relied on. The desktop's `before-quit` shutdown promise awaits `sessions.quit` → `quiesced` under a bound, with _Quit anyway_ sending `quitAnyway` that resolves it early.
- **Ownership has no cycles.** `chat-session` does not own turns: it sends `prepare` and `turnCompleted` to its project session, which forwards to the worker-side `project-revisions` root; the root spawns the `turn.machine`, which asks its `checkout.machine` to `cut`; `turn.finalized` comes back the same way. The `sessions` singleton is created with `createActor` in a store module and provided through context (the `ChatSessionStore` precedent), never with `useActorRef`, so Strict Mode cannot double-start every project. Per-route views use `useActorRef` as today.
- **Records are the durable form, never machine snapshots.** No machine persists its XState snapshot; `.tau/runs/<runId>.json`, the graph, git's own remote config, and `.tau/revisions/sync-pending` are the truth, and every machine rehydrates from them through `input` at start (I3: derived at read time). A snapshot format would be a second schema to migrate; the records already exist.
- **What is not a machine**: pure functions (`classify`, `composeView`, the revision algebra, hashing, fold rules), the port adapters, the git server, the `Rev N` / dirty / "Current" / `Follow chat` derivations (selectors over the graph and machine snapshots), and `import-github.machine`, which stays as it is; once EQ11 lands its download actors can become a clone through the remote actors with no state change, which the deferred import ruling leaves for later.
- **Placement**: every machine is scaffolded by the workspace machine generator (`create-machine` skill) as one direct package subpath (`@taucad/revisions/turn-machine`, `…/checkout-machine`, …), each exporting exactly one machine value plus its selectors and guards; the package root barrel carries no machine value. `xstate` enters `packages/revisions` as a **peer plus dev dependency**, the `packages/camera` and `packages/three` precedent; the five-machine composition lives at the owner as `createProjectRevisionsActor` in `/revisions/revision-effects` (W3c: `packages/host` names no `xstate` type and holds no `xstate` dependency; `apps/ui` mounts the same composition in the file-manager worker). Fencing: `checkout`'s `fence` serialises mints per checkout **within one process** (browser: the live-tree Web Lock; Node: in-process); across processes the only fence is the port's expected-old ref CAS — `applyTree` writes are unfenced across processes (W3c review R13; a disk host that needs more decides it in W9). Nothing under `libs/filesystem` depends on XState: the authority is an effect, never a consumer of a machine. Events are `camelCase` per the XState policy; the kept restore states are renamed from the current `SCREAMING_SNAKE_CASE` when they move.

### Sessions and liveness: many projects, many ideas at once

Today the mount table in the file-manager worker is profile-wide (every known project is mounted, always), but everything that makes a project _usable_ is route-scoped: the project's content and tree services, its kernel workers, its project and editor machines, and its revision authority are created by the route and destroyed when the user navigates away. Runs survive in the app-wide chat store, yet their settlement authority and host registration die with the route, and durable compute is admitted for exactly one project at a time. Desktop keeps an always-on launcher per workspace root, so the two clients disagree about what "open" means. The design as it stood did not support several live projects, because nothing owned a project's liveness.

The target adds one layer above the per-project actor system and one registry above that:

| Machine                                                                                                                  | Instance                            | States                                                                                                                                                                                                                                                                                                                                                                | Events in                                                                                                                                                                                              | Emits                                                                    | Owns                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `sessions.machine` (`apps/ui`, app singleton created with `createActor` in a store module; desktop and browser share it) | one per client                      | `ready → quitting → quiesced`; the budget is a guard on `open`, not a region                                                                                                                                                                                                                                                                                          | `open { projectId, reason }`, `close { projectId, reason }`, `touch { projectId }` (navigation and focus only, never activity streams), `idleExpired { projectId }` from a child, `quit`, `quitAnyway` | `liveSetChanged`, `budgetRefused { projectId, suggestions }`, `quiesced` | spawns and stops `project-session` children with stored refs; decides every open and close; hands the idle window to children through `input`                                                                                             |
| `project-session.machine`                                                                                                | one per live project                | `opening.pulling` (fetch before render, A32) → `opening.starting` (parallel: `views`, `runtime`, `agentHost`, `compute`) → `live` (`idle` with `after: idleWindow` → `idleExpired` to the parent, guarded on clean + pushed; `busy`) → `closing` (sync flush → ask, cancel or await runs → release leases → stop children) → `closed`; `failed { reason }` per region | `open`, `close { reason: user \| idle \| budget \| quit }`, `childFailed`, `runStarted`, `runSettled`, `activity` (adapter-coalesced)                                                                  | `sessionState`, `attention { count }`                                    | the nested file-manager actor, the project machine and its geometry units, the agent-host attachment (browser worker or desktop launcher), the `project-revisions` root, the project's `chat-session` actors, compute admission, watchers |
| `chat-session.machine` (replaces the store's flag bag; `chat-persistence` and `draft` become its children)               | one per chat that is live or viewed | parallel: `run` (`idle → queued → running { generating \| tool \| waiting { approval \| input } \| reconnecting } → done \| failed \| stopped`) × `read` (`read \| unread`) × `revision` (`onMain \| onBranch`, `clean \| dirty`, `synced \| pending \| conflicted`)                                                                                                  | `run.lifecycle`, `interrupt.recorded`, `text-delta`, tool part transitions, ACP `session/update`, `viewed`, `close`, `turn.finalized`, `syncState`                                                     | `statusChanged`                                                          | the run hold, the durable-run hold, the settlement of its turns (through `turn.machine`), its unread record                                                                                                                               |

Rules that follow:

- **Liveness is orthogonal to navigation.** Opening a project route sends `open` if the project is closed; leaving sends nothing. The user can iterate on several ideas in one project (several chats on `main`, or on branches) and on several projects, and every one keeps running while another is on screen. Returning is instant because nothing was torn down.
- **One session owns everything project-scoped.** The content and tree services, kernel workers, the agent-host attachment, compute admission, watchers, and the revisions actor system are children of the project session and die with it, never with a route. Run settlement and host registration therefore outlive navigation, which closes two defects the trace found. Compute admission becomes per live project instead of single-project.
- **Close is a verb and a policy, never a side effect.** _Close_ on a project: if runs are in progress, ask (_Stop 2 agents and close?_), then flush sync (D28), cancel runs, release leases, stop children. _Close_ on a chat: cancel its run, release its lease, keep its record. Policy closes are visible in the sidebar with their reason: `Closed to save memory` after the idle window, `Closed · memory budget` when a newer open needed the slot. A policy never closes a project with a running agent, a dirty tree, or an unacknowledged push.
- **Budget, stated.** The browser holds at most **8** live projects (operator ruling 2026-09-12, OQ-W25); each costs a kernel worker with a 100 MiB geometry buffer and an agent-host worker per run, so the idle window and the visible policy close carry the memory story rather than a low cap; desktop is bounded by memory only. When a ninth open arrives, the registry closes the least recently touched _idle_ project and says so; if none is idle it refuses with the list to close. Idle closes after 30 minutes with the project hidden (OQ-W26); the window is per workspace, never per project.
- **App quit and tab close** send `quit` to the registry, which runs every live session's `closing` with the flush of D28; on desktop the shutdown promise awaits it.
- **Desktop and browser share the semantics.** The desktop always-on launcher per workspace root becomes the `agentHost` child of the project session; a closed project on desktop has no launcher. The same machines, different injected actors.
- **The sidebar is a selector.** Every glyph is derived from `sessions`, `project-session`, `chat-session`, and `project-revisions` snapshots at read time. The only persisted status is `unread`, a per-client record, because it is about what this person has seen on this device.

#### Agent states, mapped to what the sidebar says

| Source signal                                                                                                               | `chat-session` state       | Sidebar glyph                 | Row text (when the row has room) |
| --------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ----------------------------- | -------------------------------- |
| no session, no run                                                                                                          | `run.idle`                 | none                          | —                                |
| `run.lifecycle: admitted`                                                                                                   | `queued`                   | hollow dot                    | `Queued`                         |
| `run.lifecycle: running` with no tool part in flight (deltas never enter the machine)                                       | `running.generating`       | spinner                       | `Working…`                       |
| tool part `input-streaming` / `input-available`; ACP `tool_call` `pending` / `in_progress`                                  | `running.tool`             | spinner                       | `Running <tool>`                 |
| `interrupt.recorded` `requested` (`approval`, `operator`, `safeguard`); ACP `request_permission`; part `approval-requested` | `running.waiting.approval` | amber dot + count             | `Needs your approval`            |
| model asked a question and stopped (`run.lifecycle: paused`)                                                                | `running.waiting.input`    | amber dot                     | `Waiting for you`                |
| store `durableRunState: reattaching`; request lifecycle `retrying`                                                          | `running.reconnecting`     | spinner, dim                  | `Reconnecting…`                  |
| `run.lifecycle: completed` and the chat was not focused                                                                     | `done` + `read.unread`     | blue dot                      | `Done`                           |
| `run.lifecycle: completed`, focused                                                                                         | `done` + `read.read`       | none                          | —                                |
| `run.lifecycle: failed` (`RunFailureDetail`)                                                                                | `failed`                   | red glyph                     | `Failed · <reason>`              |
| `run.lifecycle: cancelled`; request lifecycle `stopping`                                                                    | `stopped`                  | none (muted row)              | `Stopped`                        |
| `turn.finalized` on a branch                                                                                                | `revision.onBranch`        | `⎇ <branch>` chip             | —                                |
| `checkout.machine` dirty                                                                                                    | `revision.dirty`           | small ring on the branch chip | `Unsaved`                        |
| `sync.machine` pending / queued                                                                                             | `revision.pending`         | cloud-up glyph                | `Backing up` / `Not backed up`   |
| `sync.machine` conflicted or `mergeConflicted`                                                                              | `revision.conflicted`      | alert glyph                   | `Needs resolution`               |

Project rows aggregate their chats: a spinner while any chat runs, an amber count while any needs attention, a red glyph if a session child failed to open or a run failed and is unread, a solid dot when live and idle, nothing when closed. A project row also carries the branch chip only when the workbench is off `main` and the sync glyph only when a remote exists, per A29.

### The relations, stated plainly

- A **project** has one revision graph, one live checkout, and zero or more remotes.
- A **branch** is a named line in that graph. `main` is what the live checkout tracks by default; other branches are created by users or by placement policy when chats need their own line.
- A **checkout** is where edits happen: the live one is the project directory, linked ones are private working copies, one per branch. A checkout is never _in_ the graph. Chats, subagents, peers, and the workbench attach to a checkout by id; sharing one is how peers collaborate.
- A **turn** takes a lease on a checkout, edits it, and ends by minting **one revision** onto the checkout's branch.
- **Merging** a branch produces a merge revision on `main` and makes the linked checkout disposable. The branch ref may be deleted then (user verb "Discard") or kept.
- **Restore** does not fork history; it applies an older revision to the selected checkout. The next turn from there mints a revision whose parent is the restored one, which _is_ a fork — and the graph records it as such without any synthetic `history/…` branch.
- **Sync** exchanges refs and objects with a remote and never touches a working tree except through the same merge path a local branch uses.

### What each surface must never do

- Panes must not read different checkouts from each other. (Fixes the "chat works elsewhere" symptom.)
- The Files tree must not show a linked checkout as a directory of the project. (Physical separation makes this automatic.)
- Revision numbers, dirty state, and "Current" must not be derived from the transcript.
- No caller may test a path prefix to decide access, versioning, or visibility. It asks the registry.
- No agent may read or write the revision control plane or the lease records.

---

## Project layout

Inside a project directory (all hosts). `class` is the Rule 16 storage class; `versioned` and `agent` are the registry's answers.

| Path                                                                                                                                                                                     | class                                  | versioned                                                                       | agent                   | owner              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------- | ----------------------- | ------------------ |
| `tau.json`, sources, `.tau/config.*`, `.tau/parameters/**`, `.tau/AGENTS.md`, `.tau/skills/**`, `.tau/parts`, `.tau/machines`, `.tau/export/**`                                          | authored                               | yes                                                                             | read-write              | user / agent       |
| `.tau/types/**`, `.tau/tsconfig.generated.json`, `.tau/lockfile.json`, `.gitignore`, `.gitattributes`                                                                                    | authored (generated from the registry) | `.gitattributes` and `.gitignore` yes (git needs them in the tree); the rest no | read-write              | tooling            |
| `.tau/cache/**`, `node_modules/**`                                                                                                                                                       | cache                                  | no                                                                              | read-write, not watched | host               |
| `.tau/chats/<chatId>/events.jsonl`                                                                                                                                                       | records                                | no (shipped as `refs/tau/chats`)                                                | read-only               | host               |
| `.tau/runs/<runId>.json` (leases, publications, conflicts)                                                                                                                               | records                                | no                                                                              | read-only               | host               |
| `.tau/artifacts/**`, `.tau/tool-results/**`, `.tau/offloaded-tool-results/**`, `exports/**`, `thumbnail.webp`                                                                            | records / evidence                     | no (shipped as `refs/tau/evidence` when _Sync large exports_ is on)             | read-only               | host               |
| `.tau/revisions/**` (browser store: objects, refs incl. `refs/tags` and `refs/remotes`, `checkouts/`, `config` with remotes, threshold, anonymity), `.jj/`, `.git/`, `.tau/binding.json` | control-plane                          | —                                                                               | hidden                  | revision authority |

Outside the project, inside the workspace's host-private area:

| Path                                                                               | Purpose                                                                                           |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `<workspace>/.tau/checkouts/<projectId>/<checkoutId>/`                             | linked checkouts (working copies of a branch; `git worktree` / `jj workspace` on native backends) |
| `<workspace>/.tau/store/`                                                          | compute reuse store (compute-reuse charter D9); action/artifact evidence                          |
| `<workspace>/.tau/workspace.json`, `builds/`, `backups/`, `imports/`, `unclaimed/` | workspace records (flat-layout draft)                                                             |

Generated ignore content (`.gitignore` on disk hosts) is _derived from the registry_, never hand-maintained, and lists every row whose `versioned` answer is false (cache, records, control-plane, and the generated-but-unversioned files) — the one rule that keeps `.tau/types/**` and the control plane out of a disk-host revision (W1 review R4). Composed overlays (`.agents/skills`, `node_modules`) never exist in the project directory and need no ignore entry.

---

## Hosts

The same five layers, placed per host. "Anchor" is the host that runs the authority for a workspace (topology).

| Layer      | Browser (Home / OPFS / WebAccess)                                                                                   | Desktop (Electron)                                            | `tau serve` / CLI                       | Cloud                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | --------------------------------------- | ----------------------- |
| L0/L1      | file-manager worker; `.tau/binding.json` absent (Home is self-anchored)                                             | Electron utility over `node:<abs>`                            | `tau serve` over `node:<abs>`           | container authority     |
| L2         | `isomorphic-git` store at `.tau/revisions` (standard `.git` layout over the provider)                               | jj / native git adapter                                       | jj / native git adapter                 | jj / native git adapter |
| L2 remotes | `isomorphic-git` smart HTTP + LFS to the Tau Hosted Remote directly and to Git remotes through the API's CORS proxy | native git (`git push`/`jj git push`) to Tau or Git remotes   | native git                              | native git              |
| L3         | live = `/projects/<id>`; linked checkouts at `/checkouts/<id>` routes over the same storage root                    | `git worktree` / `jj workspace` under the host data directory | same; never inside the served directory | same                    |
| L4         | `composeView` in the worker                                                                                         | `composeView` in the utility                                  | `composeView` in the serve process      | same                    |
| L5         | SPA                                                                                                                 | SPA (identical)                                               | `tau revisions …`, TUI                  | via serve               |

Rules that follow: a client bound to a remote anchor reads composed views over the fs-bridge, never a mirror (N5); `node_modules` is derived on each host and never crosses the bridge (N18); revision graphs move between hosts by bundle/fetch/push through the port, never by copying working trees (topology "revision-shipped"). The Tau Hosted Remote is how a project moves between a user's devices: the browser on a second device fetches the graph and materializes the live checkout from `main`.

---

## Rulings

Each ruling names the invariant it protects. Invariant ids continue the topology's `E-`/`I-` series where one already exists.

| Id      | Ruling                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Protects                                                                                                                |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **A1**  | One workbench reads one checkout. Every file-bearing pane and every chat link resolves through the same `composeView(selected)`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | I-FS, "chat works elsewhere"                                                                                            |
| **A2**  | The workbench reads one selected checkout. Switch applies a branch to the live checkout when that branch has no linked checkout and no lease holds the live checkout (git switch, DT1 as shipped); otherwise it re-roots the workbench at the branch's linked checkout. A running turn is never re-based. Two checkouts never track one branch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | E-A, I-ONEAUTH                                                                                                          |
| **A3**  | Chats attach to checkouts by id. **Non-branching is the default**: every chat attaches to the live checkout on `main` unless the user chooses a branch; a second concurrent chat shares the live checkout rather than forking. A subagent inherits its parent's checkout. Attachment never refuses a turn. Branches exist only because a user created one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | operator ruling EQ6 (amends N26), DT2                                                                                   |
| **A4**  | A revision is minted only by a host at turn finalize, restore-fork, or merge; agents never call the port. Revision cards admit only host-attested `turn.finalized`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | N26, R-UX2, I-EVIDENCE                                                                                                  |
| **A5**  | Versioning, access, visibility, watch plane, and ignore generation are answers from one path registry. No prefix test at a call site.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | I-MASK, Rule 16                                                                                                         |
| **A6**  | Linked checkouts live outside the project directory, one per branch, registered in the control plane through the port: on browser roots at a persistent `/checkouts/<id>` route over the same storage root, on disk hosts under the host data directory, never inside a served directory. Exclusion by physical separation, never by ignore rule.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | F22, I-PROV, Rule 3                                                                                                     |
| **A7**  | Overlays are composed, never stored. A project-authored path under an overlay root overrides the whole bundle; the entry carries `overrides`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | I-DERIVED, skill-shadowing rule                                                                                         |
| **A8**  | Provenance crosses the wire as `{ source, versioned, agentAccess, identity?, overrides? }`; `agentAccess` is the agent's access, user gating derives from `source`/`versioned` (P10); labels live in one UI catalog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | I-VOCAB                                                                                                                 |
| **A9**  | Records are read-only to agents and dimmed for users; the control plane is hidden from both. Nothing in either class enters a revision or an export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | I-MASK, Rule 16                                                                                                         |
| **A10** | Every host exposes the same L2–L4; the CLI exposes the same verbs headlessly. Browser, desktop, and CLI produce identical revisions for identical edits.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | vision "hosts are peers", I-HASH                                                                                        |
| **A11** | Events ride the composed view. An agent write to a checkout is observable by any UI view of that checkout on every host, through the authority's rooted watch plane.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | I-WATCH                                                                                                                 |
| **A12** | `Rev N`, dirty, "Current", and branch display names are derived at read time from the graph and the selected checkout; none is stored.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | "display is derived, never authority"                                                                                   |
| **A13** | One repository per project. The workspace is the binding and authority unit; the project is the history and portability unit. _(proposed; see OQ-W1)_                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | vision "history stays portable"                                                                                         |
| **A14** | Backends are chosen per project per host from the control plane and reached only through `RevisionPort`; no layer above L2 names one. A cloud store is never a backend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | N24, N10                                                                                                                |
| **A15** | Remotes are git URLs and speak git smart HTTP plus LFS; nothing else. Ref updates are git's own atomic receive; there is no server-side merge; the server enforces a ref allow-list. Disk hosts use native git, the browser uses `isomorphic-git`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | I-XFER, I-HASH, EQ4                                                                                                     |
| **A16** | The Tau Hosted Remote is a git server inside the Tau API (bare repositories on a Fly volume, LFS in R2, nightly bundle snapshots to R2), so `git clone`, `git push`, and `jj git push` work with stock tools and no Tau client. This amends topology N9/N16 (see Amendments).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | vision "history stays portable"                                                                                         |
| **A17** | Remote credentials are host-resolved references; no secret is ever written under a project or workspace directory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | security floor                                                                                                          |
| **A18** | Users see revision, branch, restore, merge, sync. Checkout, lease, backend, worktree, ref, and HEAD are engineering words and never UI copy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | I-VOCAB, "Users need not know Git"                                                                                      |
| **A19** | The workbench's checkout is always visible in one header chip; the chip is the only place outside the Revisions pane that names a branch, and it exposes `Follow chat` only when the chat and the panes diverge.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A1, "chat works elsewhere"                                                                                              |
| **A20** | Revisions are minted by trigger (`save`, `idle`, `turn`, `merge`, `restore`, `sync`, `import`, `close`) and only when the cut's tree differs from the head's tree. Idle revisions are immutable and folded in display, never amended.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | I-MERKLE, "only when changed"                                                                                           |
| **A21** | A named version is an annotated tag. Publications point at named versions of a synced graph; the API materializes the tagged tree server-side into the publications blob store, so nothing is uploaded separately and the shipped viewer, CDN, and private-proxy paths are unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | vision "never access to your own files", one identity                                                                   |
| **A22** | A conflict is a conflicted revision on the source branch, never on `main` and never loose files in a checkout. Resolution is explicit per file; an agent may be asked, never assumed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | E-C, I-CONF                                                                                                             |
| **A23** | Blob-like stores (S3, Dropbox, IPFS) are never client-side remote kinds. They may enter as server-side storage for Tau-hosted repositories, as a bare repository on a synced folder, or as read-only exports; none is in the first program. One remote is exposed per project for now; the git remotes list is the data model, so multi-remote is not precluded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A15                                                                                                                     |
| **A24** | Large objects are git LFS objects, tracked by `.gitattributes` generated from the registry (binary types, ≥ 1 MiB); large generated outputs are evidence and not synced by default; storage against the plan is visible in Sync and a push over quota is refused with the file list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | I-BIG, I-HASH, EQ7                                                                                                      |
| **A25** | Nothing reachable is ever collected, and reachability includes revisions named by chats and run records; no local GC in this program; no checkout is removed except by a Discard gated on a clean tree; no lease removal is silent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | data-loss floor                                                                                                         |
| **A26** | Every revision carries an actor from day one: signed-in email when available, a stable anonymous id when chosen or absent, and the agent's model, run, and on-behalf-of user for agent turns. Anonymity never rewrites history.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | I-PROV                                                                                                                  |
| **A27** | _Compare_ is a text diff on History, chat cards, and conflict rows; geometry diff is a later capability over the same inputs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                                                                                                                       |
| **A28** | Undo is within a checkout's unsaved state; revisions are across it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                                                                                                       |
| **A29** | Progressive disclosure and temporal hiding are the UX mantra: a surface shows what is relevant now and hides what is old or unused until asked. History groups by recency and collapses older revisions and autosaves; the Branches region and the composer picker do not exist until a second branch does; merged branches leave the list; Sync appears only when a remote does.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | operator ruling EQ3, DESIGN.md calm density                                                                             |
| **A30** | Chats sync only as `refs/tau/chats/*` through the project's remote; every client reads chats from the checkout's `.tau/chats/**` projection; no chat-specific sync plane exists or is built. Connecting a remote includes chats unless the project's _Sync chats_ toggle is off.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | files-first rulings 1, 4, 5; operator ruling EQ10                                                                       |
| **A35** | Liveness is owned by machines, not routes: an app-singleton `sessions.machine` spawns one `project-session.machine` per live project, which owns every project-scoped resource (views, kernel, agent-host attachment, compute admission, watchers, the revisions actor system, the project's `chat-session` actors); navigation opens but never closes; _Close_ is a user verb or a visible policy (idle window, live-project budget) that never closes a running, dirty, or unpushed project; quit runs every session's closing with the sync flush.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | operator direction 2026-09-12 (concurrent projects, explicit close), A32, A33                                           |
| **A36** | Every agent, revision, and sync state the user sees is a selector over `sessions`, `project-session`, `chat-session`, and `project-revisions` snapshots; the sidebar is such a selector; the only persisted status is the per-client `unread` record. The state table above is the complete vocabulary; a new signal is added to the table before it is shown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A33, I3                                                                                                                 |
| **A37** | Browser and desktop share liveness semantics through the same machines with different injected actors: the desktop always-on launcher is the `agentHost` child of a project session and exists only while the project is live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | A35, topology N3                                                                                                        |
| **A38** | Composition rules are binding: no `systemId`; always-on children are invoked, variable-count children are spawned with stored refs and stopped on removal and root exit; siblings talk through the parent; one minter per checkout (`checkout.machine`) with a write generation; the lease is a `fromCallback` actor; every effect has a failure edge; no function in context; coalesce at the seam (one `changed` per change event, no token deltas), debounce in the machine, settled values out; `project-revisions` runs in the file-manager worker and publishes one `RevisionStatus` projection per project; the `sessions` singleton is created with `createActor`; opening pulls before it renders; unload runs flush registrants before session registrants and distinguishes `hidden` from `pagehide`; quit completes with `quiesced` under a bound.                                                                                                                                                                                                                                                                                           | adversarial review 2 (F1–F23), XState policy                                                                            |
| **A39** | Review 3 corrections are binding: `visibilitychange: hidden` is the real close and `pagehide` carries only a precomputed ≤64 KiB history-set POST; the push set is a history set (atomic on native, ref-by-ref with `main` last in the browser) and a record set (per ref, never blocking history); `refs/tau/chats/<chatId>` is an orphan-parented chain written by object plumbing whose tree holds `chat.json` plus per-device `events/<deviceId>.jsonl` segments, and the fetch path writes the `.tau/chats/**` projection; the chat record leaves IndexedDB for `.tau/chats/<id>/chat.json` with `messages` derived from the log; opening pulls under a 3 s render / 10 s abort bound and skips the pull offline; the browser LFS client is Tau's (S49); the native adapter's managed-ref guard narrows to the host-local set so `refs/tau/{chats,evidence,artifacts}` transport; the GitHub credential is browser-held and proxy-forwarded in a header with incremental `public_repo`→`repo` consent; the two-client e2e is node-side Vitest driving Playwright as a library; the wasm algebra is deleted in W3 with the browser codec, not in W1. | adversarial review 3 (F1–F16)                                                                                           |
| **A40** | Execution corrections from the W11 lanes are binding: `remote.machine` starts in `reading` (rehydrating from git's own remotes list, D29, the `checkouts.machine` pattern) and `connect` carries `{ kind, url? }`; `remote.machine` is the single owner of the over-quota file list — `quotaRefused` is sent to it by its own `initialSync` failure edge and, via the parent, by `sync.machine`'s pushes (P19); the push lease stays: `RevisionPushInput.expected` per ref, native `--force-with-lease=<ref>:<expected>`, browser advertisement check → per-ref `rejected` (`leaseLost`), never plain `--force` (P18); clean/smudge live inside `RevisionPort` on both legs, never in the composed view (W11b review Q4); the proxy is git-endpoint-only and Tau LFS objects transfer browser→R2 directly (P17).; the `Tau-Metadata` trailer carries `provenance.turnId` (it enters `deriveChangeId`'s preimage) so a card survives reload from `log(branch)` alone — W5's ratified deviation.                                                                                                                                                           | W11a review, W11b review, W5 review, queue P17–P19                                                                      |
| **A34** | The git substrate is reused where it is well designed and cut where it is unreachable: the port contract and conformance suite, the bounded command runner, the `Tau-Metadata` trailer and change-id derivation, the digest helpers, the three-way merge, the in-memory graph authority, and the native git adapter's worktree and `update-ref` transaction code stay; the dead native git persistence wrapper, the hand-rolled browser object codec, the bundle members, the jj adapter and wasm algebra loader, and the transcript-derived revision state are deleted in the wave that makes them unreachable; a package is clean when every module is reachable from a barrel and every barrel export has a consumer.                                                                                                                                                                                                                                                                                                                                                                                                                                 | operator direction 2026-09-12 (reuse well-designed parts, leave a clean state), A31                                     |
| **A33** | Every stateful revision flow (turn lease, checkpoint, sync, restore, branch verbs, conflict resolution, checkout registry and lease recovery, remote connection, publish) is a host-neutral XState machine in `packages/revisions`, composed as one actor system per project under a root `project-revisions.machine`, with effects injected as actors over the `RevisionPort`; the same machines drive browser, Electron, `tau serve`, and CLI; UI machines connect by events; records, not snapshots, are the durable form; close and quit reuse the existing unload registry (moved to `pagehide`) and the desktop `before-quit` shutdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | operator direction 2026-09-12 (XState for complex UI logic, testable in isolation, portable across hosts), A4, A20, A32 |
| **A32** | Sync is continuous, not an event: every revision pushes within a debounce while a remote is connected; closing a tab or quitting the app flushes a `close` revision and pushes with a request that outlives the document; unacknowledged pushes are queued durably, retried on next open, and shown as _Not backed up_; opening a project fetches before render.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | operator requirement 2026-09-12 (close-and-continue), A2, A20, A22, A30                                                 |
| **A31** | No backwards compatibility: every change in this program is a clean cut with the old code deleted in the same wave; no migration of `.tau/workspaces` records, transcript-derived revisions, or upload-based publications.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | operator ruling EQ5                                                                                                     |

---

## Open questions for ratification

| Id     | Question                                                                                                                                                                                                                       | Recommendation                                                                                                                                                                                                                                                                                                                                                  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OQ-W1  | Repository per project (today, A13) or per workspace (topology OQ-N10 "every workspace a repo lazily")?                                                                                                                        | Per project: matches `tau.json` as the export unit and keeps `git clone` of one project meaningful. A workspace-level repo would entangle unrelated projects' history.                                                                                                                                                                                          |
| OQ-W2  | Do linked checkouts move out of `.tau/workspaces/<run>` into the workspace private area (A6) in the browser too, given rooted views today capture an exact mount entry?                                                        | Yes; extend `createRootedFileSystem` to accept a sub-path of a mount. The alternative (ignore rules inside the project) is the collision the corpus already documents.                                                                                                                                                                                          |
| OQ-W3  | Does `.tau/workspaces/**` disappear entirely, with leases and publications under `.tau/runs/`?                                                                                                                                 | Yes. Splitting the writable tree (now external) from the records resolves the three-way path collision in filesystem-authority-policy.md:196.                                                                                                                                                                                                                   |
| OQ-W4  | Revision granularity: per turn (today) or per tool call behind a flag (topology OQ-N12)?                                                                                                                                       | Per turn on every host; per-call stays behind a flag and never changes what the Revisions pane shows by default.                                                                                                                                                                                                                                                |
| OQ-W5  | Engine order for disk hosts: keep the browser adapter over `NodeFsProvider` (today) until jj passes its blockers, or wire native git first?                                                                                    | Native git first for disk hosts (already implemented, unwired); jj when the `jj run` >64 KB drop and the response-cap failure are closed.                                                                                                                                                                                                                       |
| OQ-W6  | Is Home a workspace with an id, or the one id-less workspace?                                                                                                                                                                  | Home is a workspace whose `binding.json` is implicit; it needs no `wsp_*` id because it can never be bound remotely.                                                                                                                                                                                                                                            |
| OQ-W7  | Ratify Rule 16 (four storage classes) as the registry's `class` column, closing its "draft" status.                                                                                                                            | Ratify as written, with `.tau/workspaces` removed from the host-records row per OQ-W3.                                                                                                                                                                                                                                                                          |
| OQ-W8  | One checkout per branch (git rule) or many per branch (jj)?                                                                                                                                                                    | One. It keeps "branch row = who is working there" true and matches every native backend's default. Reopen only with a concrete need.                                                                                                                                                                                                                            |
| OQ-W9  | _(ruled 2026-09-12, EQ11; see OQ-W19)_ How does the browser reach a Git remote?                                                                                                                                                | `isomorphic-git` smart HTTP through the Tau API's CORS proxy; disk hosts use native git.                                                                                                                                                                                                                                                                        |
| OQ-W10 | _(ruled 2026-09-12, EQ4)_ Tau Hosted Remote protocol.                                                                                                                                                                          | Git smart HTTP plus LFS, served by the git binary inside the Tau API; bundles are not a wire.                                                                                                                                                                                                                                                                   |
| OQ-W11 | _(ruled 2026-09-12, EQ6)_ Placement for a second concurrent chat.                                                                                                                                                              | Attach to the live checkout on `main`; non-branching is the default.                                                                                                                                                                                                                                                                                            |
| OQ-W12 | Idle window: 5 minutes (proposed), 2, or 10?                                                                                                                                                                                   | 5; VS Code local history and Figma sit between 30 s and 30 min, and a hardware edit loop is minutes, not seconds. Configurable per workspace, never per project.                                                                                                                                                                                                |
| OQ-W13 | Do idle revisions fold only in display (proposed) or also squash on the backend (jj-style amend)?                                                                                                                              | Display only. Append-only keeps every backend and every remote simple; the cost is object count, which content dedupe bounds.                                                                                                                                                                                                                                   |
| OQ-W14 | _(ruled 2026-09-12, EQ5)_ Existing multipart-upload publications.                                                                                                                                                              | No migration (A31): the upload path is deleted, existing publication rows and their blobs are removed, and users re-publish from a revision.                                                                                                                                                                                                                    |
| OQ-W15 | _(ruled 2026-09-12, EQ7)_ Large objects and Git remotes.                                                                                                                                                                       | LFS everywhere; a GitHub remote's LFS limits are shown before the first push.                                                                                                                                                                                                                                                                                   |
| OQ-W16 | _(closed)_ IPFS is not a remote kind.                                                                                                                                                                                          | See A23.                                                                                                                                                                                                                                                                                                                                                        |
| OQ-W17 | Anonymous id scope: per workspace (proposed) or per project?                                                                                                                                                                   | Per workspace; one pseudonym across a person's projects keeps merges attributable without revealing identity.                                                                                                                                                                                                                                                   |
| OQ-W18 | _(closed)_ Blob-store drivers.                                                                                                                                                                                                 | Not remote kinds (A23).                                                                                                                                                                                                                                                                                                                                         |
| OQ-W19 | _(ruled 2026-09-12, EQ11)_ Browser → Git remote.                                                                                                                                                                               | Through the API's CORS proxy for git smart HTTP, the standard `isomorphic-git` pattern; GitHub sign-in supplies the credential.                                                                                                                                                                                                                                 |
| OQ-W20 | _(ruled 2026-09-12, EQ10)_ The Tau Hosted Remote holds `refs/tau/chats/*`.                                                                                                                                                     | Yes; this is the chat sync mechanism (A30). Obligations in Amendments.                                                                                                                                                                                                                                                                                          |
| OQ-W21 | **Ruled and wired (W3a).** Replace the hand-rolled browser object store with `isomorphic-git` over an `fs` shim, or keep the store and add only its transport?                                                                 | Replace. One maintained dependency supplies objects, packs, smart HTTP, LFS (via plugin), log, diff, merge, and tags; the custom store's only advantage was existing. The jj algebra wasm stays for candidate identity.                                                                                                                                         |
| OQ-W25 | _(ruled 2026-09-12: 8)_ Live-project budget in the browser.                                                                                                                                                                    | 8. Each live project costs a kernel worker with a 100 MiB geometry buffer plus a worker per running agent; the idle close (OQ-W26) and the visible budget close keep a full tab honest. Desktop is memory-bounded only. Memory telemetry (`performance.measureUserAgentSpecificMemory`) in W19's acceptance decides whether 8 needs lowering on 16 GB machines. |
| OQ-W26 | Idle close window: 30 minutes hidden (proposed), never, or 10?                                                                                                                                                                 | 30 minutes, per workspace, and only when the project is idle, clean, and pushed; the sidebar names the reason. "Never" leaks workers across a working day; 10 minutes fights a lunch break.                                                                                                                                                                     |
| OQ-W24 | Delete the jj adapter and the wasm algebra loader now (neither is constructed anywhere; jj is blocked on F13) and re-create them from history in the jj wave, or keep them in the tree behind the port with their tests in CI? | Delete (charter EQ14). Unreachable code is the thing A31 forbids; the spike blueprint keeps the design, digests, and pinned revision, and git history keeps the bytes.                                                                                                                                                                                          |
| OQ-W23 | Browser close flush beyond `keepalive`: add a service-worker Background Sync path (Chromium only) so a large close-time push survives the tab, or accept the durable queue plus next-open retry on every browser?              | Accept the queue for now. Continuous push makes the close-time pack small on every browser; Background Sync helps only Chromium and only when the pack exceeds the keepalive limit, which the storage row already warns about (_Not backed up_). Add it when telemetry shows queued pushes older than a session.                                                |
| OQ-W22 | Tau git server on a Fly volume with the git binary (proposed) or a pure-Node server over R2 with a Postgres object index?                                                                                                      | Volume plus binary now: it is the standard library and it makes dumb-HTTP, atomic receive, hooks, and `git bundle` backups free. Revisit if multi-region residency is required.                                                                                                                                                                                 |

---

## Amendments to the topology and the filesystem policies

This document does not only refine. Where an operator ruling after the topology north star reversed a settled point, the reversal is recorded here rather than implied.

| Prior ruling                                                                                                                                                         | What changes                                                                                                                                                                                                                                               | Why                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| N9, OQ-N8 ("the API object remote is deferred; no cloud escrow is built now")                                                                                        | The Tau Hosted Remote is built in this program as a git server.                                                                                                                                                                                            | Operator instruction 2026-09-12: hosted file support via Tau API and R2, the sync capability slated in billing. |
| N16 ("never holds a working tree or chat content")                                                                                                                   | The remote still never holds a working tree; it holds `refs/tau/chats/*` in the private tier so a second device can open a project with its chats. Obligations: entitlement checked on push, deletion on account closure, the API never parses chat bytes. | OQ-W20                                                                                                          |
| N26 (`direct \| candidate` toggle, candidate default when concurrent)                                                                                                | Placement replaces the toggle, and the concurrent default flips: a second chat shares the live checkout on `main`; a branch exists only when a user creates one.                                                                                           | A3, operator ruling EQ6                                                                                         |
| N16, files-first ruling 5 (chat content sync opt-in)                                                                                                                 | Chats sync as refs through the project's remote; connecting a remote includes them unless a per-project toggle is off.                                                                                                                                     | A30, EQ10                                                                                                       |
| N28 (chats as refs)                                                                                                                                                  | Kept, with an explicit push allow-list; host-local `refs/tau/workspaces/*` and `refs/tau/owners/*` never leave a host.                                                                                                                                     | F8                                                                                                              |
| Authority Rule 15 ("one rooted view per selected project… exact project mount") and the `.tau/workspaces/<run>/tree` carve-out at filesystem-authority-policy.md:196 | Rooted views are issued per checkout: the project mount for the live checkout, a persistent `/checkouts/<id>` route for a linked one. The carve-out is deleted.                                                                                            | A6, F1                                                                                                          |
| Rule 16 host-records row                                                                                                                                             | `.tau/workspaces` leaves the row; `.tau/runs` enters it.                                                                                                                                                                                                   | OQ-W3, OQ-W7                                                                                                    |
| I-DERIVED's `.tau/workspaces` entry                                                                                                                                  | Deleted with the directory.                                                                                                                                                                                                                                | OQ-W3                                                                                                           |

Everything else in N1–N36, Rules 1–14, and filesystem-policy Rules 0a–0c and 18–30 is refined without conflict.

## Relationship to existing documents

- **Supersedes in spirit** the mount-provenance blueprint's `Branch working copy · <chat>` row (linked checkouts are not rows of the project tree at all) and the local-default blueprint's `local | branch` _mode_ on a chat: a chat attaches to a checkout, and "Locally" is simply the live checkout.
- **Keeps** every filesystem authority rule (1–15), the two watch planes, skip-originator, the performance budgets, the rooted-path spelling, and the no-mirroring, no-overlay-provider rulings.
- **Depends on** Rule 16 ratification, topology N24–N29, N35, and the compute-reuse store location (D9).
- **Feeds** `docs/policy` by extraction once ratified: the vocabulary table, A1–A28, the remote API, the lifecycle policies, and the project layout table are the candidate policy content.
