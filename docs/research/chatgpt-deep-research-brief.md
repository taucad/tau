---
title: 'ChatGPT Deep Research Brief — Tau Repository'
description: 'Self-contained directory pointer and prompt for pasting into ChatGPT deep research to investigate the Tau monorepo.'
status: active
created: '2026-04-21'
updated: '2026-04-21'
category: reference
related:
  - docs/architecture/runtime-topology.md
  - docs/architecture/runtime-editor.md
---

# ChatGPT Deep Research Brief — Tau Repository

A pre-built deep-research prompt that orients an external researcher (ChatGPT in deep-research mode, or any reader unfamiliar with the codebase) to the Tau monorepo. The block below is **self-contained**: copy everything between the fences and paste it into ChatGPT.

## How to use

1. Copy the entire contents of the fenced block under "Pasteable Brief" below.
2. Paste it into ChatGPT (deep-research / web-enabled mode).
3. Append your specific research question at the end (e.g. _"Compare Tau's filesystem architecture to VS Code's"_ or _"Audit the AI agent middleware stack for context-window robustness"_).
4. ChatGPT will then have a complete map of the Tau repo plus pointers to read external sources (docs, GitHub, blogs, papers).

## Methodology

This brief was assembled by mining the live Tau repo in `/Users/rifont/git/tau` with six parallel readonly exploration subagents covering:

1. Multi-kernel CAD runtime (`packages/runtime`, kernels, transcoders, transport).
2. AI agent system (`apps/api/app/api/chat`, `apps/api/app/api/tools`, `libs/chat`, chat UI).
3. Web UI (`apps/ui`, React Router routes, XState machines, Dockview, Three.js viewer).
4. Filesystem + shared memory (`packages/filesystem`, `packages/memory`, runtime FS bridge, COEP/SAB).
5. API backend (NestJS, Fastify, Drizzle, Better Auth, Socket.IO, OTEL telemetry, Fly/Netlify deploy).
6. Meta layer (`docs/policy`, `docs/research`, `.agent/skills`, `.cursor/rules`, `tools/`, `scripts/`, `repos.yaml`, `infra/`).

Findings were merged, de-duplicated, and reformatted into a single navigable directory pointer with one-paragraph summaries and 1-3 anticipated questions per area.

## Pasteable Brief

```markdown
# Tau Repository — Deep Research Brief

You are doing a deep research dive into **Tau**, an AI-native CAD platform for the web. This brief gives you the canonical map of the repository so you can navigate it efficiently. Use the directory pointers below to decide which files to read, then formulate cross-references against external sources (Web APIs, CAD literature, LLM agent papers, browser standards, etc.) for whatever question I ask at the end.

---

## 0. What Tau is

- **Product**: Open-source, MIT-licensed (GPL when bundling OpenSCAD), browser-based CAD platform at https://tau.new.
- **Pitch**: Code-based, parametric, multi-kernel CAD with an AI agent that can read/edit project files, render geometry, and validate output.
- **Repo style**: **Nx monorepo** with **pnpm workspaces**. Apps under `apps/`, runtime libraries under `packages/`, internal libraries under `libs/`, docs and policy under `docs/`.
- **Stack**:
  - **Frontend**: React Router v7, React 19, TypeScript, Tailwind CSS v4, Fumadocs (docs site), Dockview (panels), Allotment (resizable splits), Monaco (editor), Three.js + React Three Fiber (3D viewer), TipTap (chat composer), XState v5 (state machines).
  - **Backend**: NestJS on Fastify, PostgreSQL via Drizzle ORM, Redis (Socket.IO Redis Streams adapter), Better Auth.
  - **AI**: LangChain `createAgent` with LangGraph-compatible Postgres checkpointing; multi-provider (OpenAI, Anthropic, Vertex AI, Ollama, Together AI, Cerebras); Tavily web search; Morph for context compaction.
  - **CAD kernels**: Replicad/OpenCASCADE.js, JSCAD, Manifold, OpenSCAD, Zoo (KCL), and a small "Tau" conversion kernel — all behind a unified `@taucad/runtime` plugin system.
  - **3D processing**: glTF-Transform, Assimp (forked as `@taucad/assimpjs`), lib3mf, tinyusdz (forked).
  - **Observability**: OpenTelemetry (Prometheus + OTLP), Grafana dashboards-as-code, local LGTM stack via `grafana/otel-lgtm` Docker image.
  - **Deploy**: API → Fly.io (`tau-api`); UI → Netlify with branch previews.
- **Cross-cutting design constraints**:
  - **Cross-Origin Isolation (COEP `require-corp` + COOP `same-origin`)** is mandatory in production so that `SharedArrayBuffer` and high-precision timers work for the CAD pipeline. All third-party assets are same-origin proxied (`/api/ph` for PostHog, `/api/github-avatar` for GitHub).
  - **Source-only consumption** of `@taucad/runtime` from the UI — no build artifact between the UI and the runtime kernels.
  - **No extension-based binary detection** — content is sniffed via BOM + NUL heuristic on the first 512 bytes.
  - **Two filesystem watch planes** — kernel fast path (dependency-scoped, drives re-render) vs UI tree path (directory-scoped, drives the file tree). They are never merged.

---

## 1. Top-level layout
```

/ # Repo root
├── apps/ # Deployable apps
│ ├── api/ # NestJS API + LangChain agent + WebSocket gateway
│ ├── api-e2e/ # ky-based HTTP smoke tests
│ ├── ui/ # React Router v7 web app (editor, docs, chat)
│ └── ui-e2e/ # Playwright tests for the UI
├── packages/ # Public @taucad/\* runtime libraries (consumed externally)
│ ├── cli/ # @taucad/cli — headless CAD CLI (`taucad export`)
│ ├── converter/ # @taucad/converter — Assimp/WASM file conversion
│ ├── filesystem/ # @taucad/filesystem — multi-backend browser FS
│ ├── js/ # @taucad/js — small JS helpers
│ ├── json-schema/ # @taucad/json-schema — Draft-07 inference and types
│ ├── memory/ # @taucad/memory — SharedPool / SharedMemoryArena
│ ├── react/ # @taucad/react — useRender hook
│ ├── runtime/ # @taucad/runtime — multi-kernel CAD runtime CORE
│ ├── telemetry/ # @taucad/telemetry — canonical metric definitions
│ ├── testing/ # @taucad/testing — geometry analysis & grading
│ └── three/ # Three.js viewer presentation layer (UI-embedded)
├── libs/ # Internal-only libraries (not published)
│ ├── api-extractor/ # Bundled .d.ts for kernel IntelliSense in Monaco
│ ├── chat/ # Chat RPC, message, and tool schemas (Zod)
│ ├── oxlint/ # Custom oxlint plugins + ESLint MDX parser
│ ├── tau-examples/ # Example projects shipped with the editor
│ ├── types/ # Shared TS types and constants (idPrefix, KernelId, …)
│ ├── units/ # Units of measurement parser/converter
│ ├── utils/ # generatePrefixedId, dispose, path, file utilities
│ └── vite/ # @taucad/vite — Vite plugins (COEP, oxc-runtime-esm, …)
├── docs/
│ ├── architecture/ # Binding architecture contracts (runtime-topology, runtime-editor)
│ ├── policy/ # 39 binding policy documents
│ ├── research/ # ~183 investigation/audit/comparison documents
│ └── assets/ # Images for README/docs
├── infra/
│ ├── docker-compose.yml # Postgres + Redis + grafana/otel-lgtm
│ ├── grafana/dashboards/ # Dashboards-as-code (10+ JSON dashboards)
│ ├── grafana/provisioning/ # Datasources, dashboards, alerts
│ └── search/ # Optional SearXNG stack
├── tools/ # Nx plugins (tsdown, copy-files, generate-cjs-dts, pkgcheck, tsgo, workspace-plugin)
├── scripts/ # Workspace automation (repos CLI, validators, WASM experiments)
├── repos/ # Gitignored clones of external dependency forks (managed by repos.yaml)
├── repos.yaml # External repo manifest (clone, sync, pin commits)
├── patches/ # pnpm patches (replicad, langchain, kcl-wasm, …)
├── tarballs/ # Local tarballs of forked WASM packages
├── .cursor/ # Agent skills, rules, MCP config, plans
│ ├── skills/ # Workflow skills (adding-tools, new-kernel, package-release, …)
│ └── rules/ # .mdc rule files Cursor injects
├── AGENTS.md # Primary onboarding doc for AI agents (commands, project map, skills)
├── nx.json # Nx target defaults + custom plugin registration
├── pnpm-workspace.yaml # Workspaces + dependency catalog
├── tsconfig.base.json # Shared strict TS config (NodeNext, verbatim modules)
├── .oxlintrc.json # Primary linter (oxlint runs first, ESLint handles residual)
├── eslint.config.mjs # Residual ESLint rules + MDX parser
├── knip.config.ts # Dead-code analysis
└── .oxfmtrc.json # oxfmt formatter (replaces Prettier)

```

---

## 2. Multi-kernel CAD runtime — `packages/runtime/`

The heart of "many CAD engines, one runtime API". Every kernel exposes a `defineKernel`-based plugin and runs inside a single shared worker entry that dynamically imports the chosen kernel.

| Path                                          | What's there                                                                                                                                                                                                                                                                                                            | Anticipated questions                                                                                                                       |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/runtime/src/kernels/<name>/`        | One folder per kernel: `<name>.kernel.ts`, `<name>.plugin.ts` (`createKernelPlugin({ id, moduleUrl, extensions, exportSchemas, … })`), `<name>.schemas.ts` (Zod render/export options). Kernels: `replicad/`, `opencascade/`, `jscad/`, `manifold/`, `openscad/`, `zoo/` (KCL), `tau/`.                                 | Where is a kernel defined vs registered? Where are tessellation/export options? How do kernels share OCCT vs ship pure JS?                  |
| `packages/runtime/src/transcoders/converter/` | `defineTranscoder` plugin wrapping `@taucad/converter`. Edges declare `from: 'glb'` → 3MF/STL/STEP/USDZ etc. with per-edge Zod schemas.                                                                                                                                                                                 | How does GLB from a kernel become another format? How do transcoder edges merge with kernel-native exports for type-safe `client.export()`? |
| `packages/runtime/src/framework/`             | Worker spine: `kernel-runtime-worker.ts` (single worker entry, dynamic kernel load), `kernel-worker.ts` (render/export pipeline + middleware chain), `runtime-worker-client.ts` / `runtime-worker-dispatcher.ts` (main ↔ worker), `runtime-filesystem-bridge.ts`, `error-enrichment.ts`.                                | Where is the render loop? Where is abort/render-superseded handled?                                                                         |
| `packages/runtime/src/client/`                | Main-thread `RuntimeClient`: connection lifecycle, `createGeometry` / `export` orchestration, `createRuntimeClientOptions` (deepmerge with plugin-by-id replacement).                                                                                                                                                   | How are options merged across plugins?                                                                                                      |
| `packages/runtime/src/middleware/`            | `geometry-cache.middleware.ts` (MessagePack + `.tau/cache` + LruMap L1), `parameter-cache.middleware.ts`, `gltf-coordinate-transform.middleware.ts`, `gltf-edge-detection.middleware.ts`, `runtime-middleware.ts` composition. Middleware can declare `getDependencies()` to extend the dependency hash and watch list. | How is `dependencyHash` built? What invalidates L1 vs L2 cache?                                                                             |
| `packages/runtime/src/transport/`             | Pluggable client↔worker transport: `worker-transport.ts` (MessagePort), `in-process-transport.ts` (tests), `runtime-transport.ts` interface.                                                                                                                                                                            | How does SSR/testing run without a Worker?                                                                                                  |
| `packages/runtime/src/types/`                 | Central contracts: `runtime-kernel.types.ts` (`defineKernel`), `runtime-transcoder.types.ts` (`defineTranscoder`, `TranscoderEdge`), `runtime-protocol.types.ts` (incl. `GltfContentDelivery: inline \| pooled`), `runtime-middleware.types.ts`, `export-option-schemas.ts`.                                            | What is the type-level contract for a kernel vs transcoder? How is `exportSchemas` serialized to JSON Schema for the manifest?              |
| `packages/runtime/src/bundler/`               | esbuild-wasm bundling for user code resolution before kernel execution.                                                                                                                                                                                                                                                 | How does a `.ts` entry become executable in the worker?                                                                                     |
| `packages/runtime/src/filesystem/`            | `filesystem-bridge.ts`, `from-fs-like.ts`, `from-node-fs.ts`, `create-runtime-filesystem.ts` — adapters so the kernel can read project files in browser or Node.                                                                                                                                                        | How does the kernel read project files inside the worker?                                                                                   |
| `packages/runtime/src/utils/`                 | Shared GLB helpers: `export-glb.ts`, `glb-writer.ts`, `off-to-gltf.ts`, `edge-detection.ts`, `color-space.ts`, `package-info.ts`.                                                                                                                                                                                       | Where is GLB assembly shared? Where is version metadata embedded?                                                                           |
| `packages/runtime/src/testing/`               | `kernel-testing.utils.ts`, `kernel-geometry-testing.utils.ts`, `smoke-esm.test.ts`, `browser-compat.test.ts`.                                                                                                                                                                                                           | How do API tests spin up a runtime without a browser?                                                                                       |

**Architecture authority:** `docs/architecture/runtime-topology.md` is **the proposal/target** (autonomous reactive render service, SAB + abort, simplified main-thread machines). Compare against current implementation in `framework/` to gauge drift.

---

## 3. AI agent — `apps/api/app/api/chat/` + `apps/api/app/api/tools/`

LangChain `createAgent` orchestrates a tool-using CAD agent with provider-agnostic middleware and LangGraph-compatible Postgres checkpointing.

| Path                                                                                                                                                                        | What's there                                                                                                                                                                                                                                                                                                                                                                                                                            | Anticipated questions                                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `apps/api/app/api/chat/chat.service.ts`                                                                                                                                     | Composition root: builds `createAgent({ model, tools, systemPrompt, checkpointer, middleware })`.                                                                                                                                                                                                                                                                                                                                       | What is the exact middleware order? How does static vs dynamic system prompt split map to provider caching? |
| `apps/api/app/api/chat/checkpointer.service.ts`                                                                                                                             | `PostgresSaver` from `@langchain/langgraph-checkpoint-postgres`.                                                                                                                                                                                                                                                                                                                                                                        | Where is conversation state persisted in production?                                                        |
| `apps/api/app/api/chat/prompts/cad-agent.prompt.ts`                                                                                                                         | Authors the CAD system prompt; produces `{ static, dynamic }` for Anthropic-style prompt caching; per-section telemetry via `onSectionResolved`.                                                                                                                                                                                                                                                                                        | How do agent/plan modes differ? Where is kernel-specific phrasing injected?                                 |
| `apps/api/app/api/chat/prompts/prompt-section-registry.ts`                                                                                                                  | `createSectionRegistry()` with per-section `cacheBreak`.                                                                                                                                                                                                                                                                                                                                                                                | Which sections break the cache?                                                                             |
| `apps/api/app/api/chat/prompts/kernel-prompt-configs/`                                                                                                                      | One prompt config per kernel (`replicad`, `openscad`, `jscad`, `manifold`, `opencascadejs`, `zoo`) plus type definitions + multi-shape examples.                                                                                                                                                                                                                                                                                        | How does Tau avoid OpenSCAD-only instructions when running on Replicad?                                     |
| `apps/api/app/api/chat/middleware/`                                                                                                                                         | LangChain `AgentMiddleware` chain: `agent-safeguards.middleware.ts` (doom-loop detector — appends one-shot guidance after 3 identical tool failures, never invalidates Anthropic prompt cache), `token-usage-context.middleware.ts` (deterministic `<system-reminder>` for context window), `client-context.middleware.ts` (skills + `.tau/AGENTS.md` memory), `compaction.middleware.ts` (Morph-backed context compression).           | How are identical tool failures detected? What happens when context is nearly full?                         |
| `apps/api/app/api/chat/compaction.service.ts`                                                                                                                               | Morph API client; throws `CompactSummaryValidationError` on malformed responses.                                                                                                                                                                                                                                                                                                                                                        | What guarantees does compaction make about preserving tool-pair invariants?                                 |
| `apps/api/app/api/tools/tool.service.ts`                                                                                                                                    | Tool registry: returns LangChain `StructuredTool` for a `(ToolSelection, KernelProvider)` pair; caches per-kernel factories for `test_model`/`edit_tests`.                                                                                                                                                                                                                                                                              | How does partial tool selection work?                                                                       |
| `apps/api/app/api/tools/tools/tool-*.ts`                                                                                                                                    | One per tool: `read-file`, `edit-file`, `glob-search`, `grep`, `get-kernel-result`, `screenshot`, `test-model`, `edit-tests`, `web-search` (Tavily), `web-browser`, `list-directory`, `create-file`, `delete-file`. Pattern: schema in `libs/chat`, handler uses `tool()` from `@langchain/core/tools` and routes filesystem/GPU/kernel work via `chatRpcService.sendRpcRequest({ chatId, toolCallId, rpcName, args })` to the browser. | Where is each tool's handler implemented in the UI? How does every tool get `chatId`/`toolCallId`?          |
| `apps/api/app/api/models/model.service.ts`                                                                                                                                  | Model catalog from `model.constants.ts`; `ProviderService.createModelClass` instantiates the right LangChain client; normalizes usage tokens (cache read/write quirks per provider).                                                                                                                                                                                                                                                    | Where is the model catalog defined? How is usage normalized?                                                |
| `apps/api/app/api/providers/provider.service.ts`                                                                                                                            | Maps `ProviderId` → constructor: `ChatOpenAI` (also Together via OpenAI-compat), `ChatAnthropic`, `ChatVertexAI`, `ChatOllama`, `ChatCerebras` + per-provider flags and OTEL names.                                                                                                                                                                                                                                                     | Which env vars per provider? How does Together differ from OpenAI?                                          |
| `apps/api/app/benchmarks/`                                                                                                                                                  | Headless model benchmark suite: `model-benchmark-runner.ts`, `model-benchmark-suite.ts`, `model-benchmark-geometry.ts`, `model-benchmark-report.ts` (self-contained HTML). CLI at `apps/api/scripts/benchmark-models.mts`.                                                                                                                                                                                                              | How is a benchmark case graded? Where does geometry validation hook in?                                     |
| `libs/chat/`                                                                                                                                                                | The contract package: tool input/output Zod schemas (`src/schemas/tools/`), `MyUIMessage` schema with `providerMetadata.common.reasoningStartedAtMs`, RPC envelope (`schemas/rpc.schema.ts`), `createRpcDispatcher` + per-handler modules (`src/rpc/handlers/*.ts`) that implement the **browser side** of every RPC the API initiates.                                                                                                 | What is the canonical streamed tool part shape? How are RPC args validated end-to-end?                      |
| `apps/ui/app/components/chat/`                                                                                                                                              | Composer (TipTap, desktop/mobile split), tool cards (`chat-tool-label.tsx` standardizes verb + description styling), activity grouping (`chat-activity-group.tsx`, `chat-activity-section.tsx`, `chat-activity-fold-context.tsx`) — Cursor-style folding of streamed tool/reasoning parts.                                                                                                                                              | How does folding interact with streaming? How do tool labels stay consistent across tools?                  |
| `apps/ui/app/utils/assistant-message-activity.ts`                                                                                                                           | Builds the "activity groups" from raw message parts (consumed by `chat-message.tsx`).                                                                                                                                                                                                                                                                                                                                                   | What's the rule for grouping vs not?                                                                        |
| `apps/ui/app/routes/projects_.$id/chat-message-tool-*.tsx`                                                                                                                  | One renderer per tool result (edit-file, edit-tests, read-file, test-model, get-kernel-result, screenshot, web-browser, web-search, etc.).                                                                                                                                                                                                                                                                                              | Where is each tool's UI implemented?                                                                        |
| `apps/ui/app/hooks/use-chat.tsx`, `chat-instance.tsx`, `chat-instance-registry.ts`, `chat-registry-provider.tsx`, `active-chat-provider.tsx`, `chat-persistence.machine.ts` | Concurrent chat instances; `ActiveChatProvider` separates **draft persistence** from streaming so background chats can run in parallel.                                                                                                                                                                                                                                                                                                 | How are multiple chats keyed? How is draft persistence separated from streaming?                            |
| `.agent/skills/adding-tools/SKILL.md`                                                                                                                                      | The end-to-end "add a new chat tool" workflow: schema in `libs/chat`, API handler, RPC dispatcher, UI part renderer.                                                                                                                                                                                                                                                                                                                    | Full checklist from schema to UI?                                                                           |

---

## 4. Web UI — `apps/ui/`

React Router v7 (`flatRoutes()` filesystem routing) + React 19 + XState v5 + Dockview + Allotment + Monaco + Three.js.

| Path                                                                                                         | What's there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Anticipated questions                                                                                                    |
| ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `apps/ui/app/root.tsx`                                                                                       | App shell with provider stack: auth, React Query, file manager, project manager, theme, analytics, keyboard, chat registry, flush guard. Loader fetches theme cookie, validates subdomain redirects, loads env + model catalog.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Which providers are global vs route-scoped?                                                                              |
| `apps/ui/app/entry.client.tsx`, `apps/ui/app/sessions.server.tsx`, `apps/ui/app/environment.config.ts`       | Hydration entry, session cookie storage (`remix-themes`), Zod-validated server env.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Where is `ENV` consumed safely on the client?                                                                            |
| `apps/ui/app/routes/_index/`                                                                                 | Marketing landing: hero, CTAs, deferred CAD preview gating (`HeroViewerGate` defers Three.js behind `requestIdleCallback`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | How does the landing host chat without a full project?                                                                   |
| `apps/ui/app/routes/projects_.$id/`                                                                          | Primary editor. `route.tsx` nests `SharedWorkerGate`, `FileManagerProvider`, `ChatRpcSocketProvider`, `WebglContextTrackerProvider`, `ProjectProvider`, Monaco model service, `ActiveChatProvider`, `ChatInstance`. `chat-interface.tsx` → `chat-interface-desktop.tsx` / `chat-interface-mobile.tsx` use **Allotment** for resizable regions and panel ID maps for chat/files/explorer/kernel/viewer/parameters/editor/converter/details.                                                                                                                                                                                                                                                             | Where is serialized Dockview layout persisted? How do "geometry units" (`Map` of `cadMachine`) bind to viewer panel IDs? |
| `apps/ui/app/routes/projects_.$id/chat-editor-dockview.tsx` and `chat-viewer-dockview.tsx`                   | Dockview hosts Monaco **per panel** with file path params, kernel diagnostics, `useFileContent` discriminated gate (binary/too-large/error/text).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | How does `useMonacoModelService` dedupe models across panels?                                                            |
| `apps/ui/app/routes/projects_.library/`, `projects_.community/`, `projects_.$id_.preview/`, `projects_.new/` | Project library, community feed, public/share preview, new-project flow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | How does preview differ from full editor in terms of machines/mounts?                                                    |
| `apps/ui/app/routes/docs.$/`                                                                                 | Fumadocs-backed product docs UI — sidebar, MDX, code blocks, actions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | How is MDX compiled (Vite plugin path)?                                                                                  |
| `apps/ui/app/routes/api.*/`                                                                                  | BFF-style server routes: `api.search`, `api.ph.$` (PostHog same-origin proxy for COEP), `api.import`, `api.github-avatar` (GitHub avatar same-origin proxy).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Which headers enable cross-origin isolation?                                                                             |
| `apps/ui/app/components/`                                                                                    | `cad-preview.tsx`, `model-viewer.tsx` (bridges geometry to `CadViewer`, optionally reusing an external `graphicsMachine`), `chat/`, `files/` (file/viewer link, `file-selector`, `export-selector`), `panes/` (Dockview wrappers), `geometry/` (R3F scene, `GltfMesh`, grids, gizmos, section view, measure, materials, RJSF-themed parameter widgets), `code/` (Monaco integration with `useMonacoModelService`), `magicui/`, `animate-ui/`, `ui/` (shadcn primitives).                                                                                                                                                                                                                               | Single design system source of truth? When to pass `graphicsRef` vs let `ModelViewer` own it?                            |
| `apps/ui/app/hooks/`                                                                                         | `use-project.tsx` wires **projectMachine**, **editorMachine**, **gitMachine**, **logMachine**, per-panel **graphicsMachine** map, **geometryUnits** (a `cadMachine` per entry file), parameter group APIs, Comlink **object store** worker. `rpc-handlers.ts` builds `RpcDependencies` for `@taucad/chat/rpc` from the file manager, CAD/graphics actors, screenshot machines.                                                                                                                                                                                                                                                                                                                         | How does `waitFor` synchronize load vs editor? Where does `mainEntryFile` come from?                                     |
| `apps/ui/app/machines/`                                                                                      | XState v5 machines: `editor.machine.ts` (open files, focusedChatId, panelState, serialized Dockview layouts, persistence/`flushNow`), `project.machine.ts`, `cad.machine.ts`, `cad-preview.machine.ts`, `file-manager.machine.ts` + `file-manager.worker.ts` (two-phase init: `connectingWorker` → `initializingServices`), `graphics.machine.ts`, `parameter.machine.ts`, `chat-mode.machine.ts`, `git.machine.ts`, `screenshot-*.machine.ts`, `webgl-context.machine.ts`, `controls-listener.machine.ts`, `logs.machine.ts`. Pattern: `setup(...)` machines with `fromSafeAsync` (`#lib/xstate.lib.js`) for Strict-Mode-safe async actors, provided actors via `machine.provide({ actors: { … } })`. | Which machines invoke workers vs Comlink? How is `SerializedDockview` restored without layout jank?                      |
| `apps/ui/app/lib/xstate.lib.ts`                                                                              | `fromSafeAsync` (preferred over `fromPromise`), `fromEventObservable`, Strict Mode subscription patterns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Why `fromSafeAsync` everywhere?                                                                                          |
| `apps/ui/app/lib/seems-binary.ts`, `file-content-service.ts`, `file-content-errors.ts`                       | BOM + NUL heuristic on first 512 bytes (VS Code style); discriminated `FileContentResult` (`loading \| text \| binary \| too-large \| orphaned \| error`); `BoundedFileCache.set()` returns boolean and never silently drops oversized files.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Why content-sniff instead of routing on extension?                                                                       |
| `apps/ui/app/lib/monaco.lib.ts`, `lib/javascript-import-parser.ts`, `lib/type-acquisition-service.ts`        | Monaco integration; `TypeAcquisitionService` registers per-kernel bundled `.d.ts` from `libs/api-extractor` via `addExtraLib` for kernel-aware IntelliSense.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | How are bundled typings updated when a kernel dependency changes?                                                        |
| `apps/ui/content/docs/`                                                                                      | Fumadocs site with two route groups: `(runtime)/` (kernels, middleware, filesystem, bundler, testing, concepts, API) and `(editor)/` (small surface, currently a stub).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | How does `docs.$/route.tsx` choose between groups?                                                                       |
| `apps/ui/vite.config.ts`                                                                                     | Vite 8 with `@tailwindcss/vite`, React Router plugin, Fumadocs MDX, **`cross-origin-isolation.vite-plugin.ts`** for COEP/COOP in dev.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Why a Vite middleware vs static config?                                                                                  |
| `apps/ui/app/styles/global.css`                                                                              | Tailwind v4 + Fumadocs UI preset + `tw-animate-css`; extensive CSS variables for OKLCH design tokens, scroll-shadow utilities (`mask-image` + `animation-timeline: scroll()` + `@property initial-value`), Dockview overrides (often need `!important` because Dockview rules sit outside `@layer`).                                                                                                                                                                                                                                                                                                                                                                                                   | Why `@source` directives pulling class candidates from node_modules?                                                     |
| `packages/react/`                                                                                            | `@taucad/react` exports `useRender` — creates a `RuntimeClient`, runs the multi-kernel render pipeline from a `code` map + optional `file` + `parameters`, exposes geometries, status, errors, default parameters, JSON Schema, and typed export.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | When to use `useRender` directly vs `cadMachine` in the editor?                                                          |
| `packages/three/`                                                                                            | Three.js viewer presentation layer (built outputs visible: `cad-viewer`, `gltf-mesh`, controls, materials, screenshot helpers). Note: most active R3F components live **embedded in the UI** at `apps/ui/app/components/geometry/graphics/three/`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Where is the GLB → Three.js scene path?                                                                                  |

---

## 5. Filesystem, shared memory, transport

Browser filesystem with multi-backend providers, cross-tab coordination, `SharedArrayBuffer` pools, and worker-based RPC.

| Path                                                                                                                                                                      | What's there                                                                                                                                                                                                                                                                                                                                            | Anticipated questions                                                                        |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `packages/filesystem/src/file-service.ts`                                                                                                                                 | Top-level API: mount, stat, read/write, streams, watches, kernel vs UI coalescing windows, `setFilePool`.                                                                                                                                                                                                                                               | When does the in-memory tree sync with provider state? How is `filePool` used on read paths? |
| `packages/filesystem/src/provider-registry.ts`, `mount-table.ts`                                                                                                          | Pure factory for backend instances; prefix → provider + `MountConfig` resolution. `FileService.mount(prefix, backend)` is domain-agnostic — callers compose domain paths (e.g. `/projects/${id}`); FileService never knows about projects.                                                                                                              | What happens on overlapping prefixes?                                                        |
| `packages/filesystem/src/providers/`                                                                                                                                      | Concrete backends: `direct-idb-provider.ts` (IndexedDB project storage), `fs-access-provider.ts` (File System Access API), `opfs-provider.ts` (OPFS with sync access where possible), `memory-provider.ts` (ephemeral), `filesystem-observer-bridge.ts` (native `FileSystemObserver` API wrapper).                                                      | Which providers require a dedicated worker for sync handles?                                 |
| `packages/filesystem/src/cross-tab-coordinator.ts`                                                                                                                        | `navigator.locks` + `BroadcastChannel` for write ordering and visibility across tabs.                                                                                                                                                                                                                                                                   | What failure modes if locks unsupported?                                                     |
| `packages/filesystem/src/throttled-worker.ts` (+ `EventCoalescer`)                                                                                                        | Batches/delimits watch traffic in chunks (100/chunk, 200ms delay, 10k buffer) between `EventCoalescer` and port emission.                                                                                                                                                                                                                               | Default chunk sizes and backpressure?                                                        |
| `packages/filesystem/src/in-memory-file-tree.ts`                                                                                                                          | O(1) stat/readdir tree without full provider walks.                                                                                                                                                                                                                                                                                                     | When is the tree invalidated vs lazily filled?                                               |
| `packages/filesystem/src/resource-write-queue.ts`, `write-coordinator.ts`, `bounded-file-cache.ts`, `directory-tree-cache.ts`, `watch-registry.ts`, `change-event-bus.ts` | Per-resource write serialization (VS Code-style parallelism across files), bounded caches, watch registries.                                                                                                                                                                                                                                            |                                                                                              |
| `packages/memory/src/shared-pool.ts`                                                                                                                                      | LRU-ish keyed byte storage over a single `SharedArrayBuffer` with Atomics.                                                                                                                                                                                                                                                                              | Max size, key collision, thread safety model?                                                |
| `packages/memory/src/shared-memory-arena.ts`                                                                                                                              | Fixed-layout arena headers/entries for structured shared slots.                                                                                                                                                                                                                                                                                         | When to prefer arena vs pool?                                                                |
| `packages/runtime/src/filesystem/` + `framework/runtime-filesystem-bridge.ts`                                                                                             | `exposeFileSystem` / `createFileSystemBridge` / `createBridgeProxy` build on a generic RPC over `MessagePort`, with `extractTransferables` and optional `FilePool`; `fromMemoryFS`, `fromFsLike`, `fromNodeFs` adapt host environments.                                                                                                                 | What happens on `DataCloneError`?                                                            |
| `packages/runtime/src/types/runtime-protocol.types.ts`                                                                                                                    | `GltfContentDelivery: inline (bytes) \| pooled (key into SharedPool)` — when not `crossOriginIsolated`, the dispatcher falls back to inline. **Domain-driven SAB ownership**: FM machine allocates file pool SAB (`filePoolBuffer`); `RuntimeClient` allocates geometry pool SAB. `client.geometryPool` is the only pool on the public `RuntimeClient`. | Fallback path when not COI?                                                                  |
| `apps/ui/app/filesystem/handle-store.ts`                                                                                                                                  | IndexedDB persistence for `FileSystemDirectoryHandle` (root key) + per-project FS backend config; ref-counted DB open; documents permission re-validation across sessions.                                                                                                                                                                              | What happens when permission is revoked?                                                     |
| `apps/ui/app/machines/file-manager.machine.ts` + `file-manager.worker.ts`                                                                                                 | XState machine spawns the FS worker, allocates `SharedArrayBuffer` for the file pool, `createFileSystemBridge` + `createBridgeProxy`, wires `FileContentService` / `FileTreeService`, teardown via `safeDispose`. Worker mounts `/` to IndexedDB and optional OPFS `/node_modules`.                                                                     | Two-phase init — what gates each phase?                                                      |
| `apps/ui/app/hooks/object-store.worker.ts`                                                                                                                                | Comlink-exposed IndexedDB API for **project metadata** (projects, chats, editor state, atomic `createProjectWithResources`). **Distinct** from file-manager FS (workspace files).                                                                                                                                                                       | Transaction boundaries and rollback paths?                                                   |
| `apps/ui/app/workers/import.worker.ts`                                                                                                                                    | Off-main-thread large-repo import: download ZIP/stream, extract via JSZip, optional write via `DirectIdbProvider`.                                                                                                                                                                                                                                      | Cancellation and partial failure recovery?                                                   |
| **COEP / SAB chain**                                                                                                                                                      | `apps/ui/netlify.toml` (production: COOP `same-origin` + COEP `require-corp` + CORP `same-origin`) → `libs/vite/src/cross-origin-isolation.vite-plugin.ts` (dev/preview headers) → `packages/runtime/src/framework/environment.ts` (asserts COI for SAB).                                                                                               | How are third-party assets loaded under `require-corp`?                                      |

---

## 6. API backend — `apps/api/`

NestJS on Fastify, PostgreSQL via Drizzle, Redis (Socket.IO Streams adapter), Better Auth, OpenTelemetry.

| Path                                                  | What's there                                                                                                                                                                                                                                                                                                                                                              | Anticipated questions                                                                                    |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `apps/api/app/main.ts`                                | Bootstraps `NestFactory` with **FastifyAdapter** (body limit, Pino-backed request IDs, CORS, URI versioning, Helmet, `FastifyOtelInstrumentation`). Imports `#telemetry/otel.js` **before** other app code. **Production only**: enables shutdown hooks, attaches `RedisIoAdapter`, then `listen`.                                                                        | Why is OTEL imported as a side-effect first? How does dev mode serve HTTP if `listen` only runs in PROD? |
| `apps/api/app/app.module.ts`                          | Root: `ApiModule`, `DatabaseModule`, **global** `TelemetryModule`, **global** `RedisModule`, `AuthModule.forRootAsync()`, `ConfigModule.forRoot({ validate: getEnvironment })`, `LoggerModule`. Global `ZodValidationPipe`, `ZodSerializerInterceptor`, `HttpExceptionFilter`. `RequestIdMiddleware` applied to `*`.                                                      | Why must `TelemetryModule` precede `RedisModule`?                                                        |
| `apps/api/app/api/api.module.ts`                      | Aggregates feature modules: `analysis`, `chat`, `code-completion`, `file-edit`, `health`, `kernels`, `models`, `privacy`, `providers`, `telemetry` (ingest), `test-api` (DEV-only), `tools`, `websocket`.                                                                                                                                                                 | Where is the test API gated from prod?                                                                   |
| `apps/api/app/api/chat/chat-rpc.gateway.ts`           | Socket.IO gateway with `better-auth` `getSession` middleware (uses `fromNodeHeaders(socket.handshake.headers)`); `socket.data.userId` set for room joins; uses `emitWithAck` pattern.                                                                                                                                                                                     | How does session auth bind to a socket?                                                                  |
| `apps/api/app/api/websocket/redis-io.adapter.ts`      | Production WebSocket adapter wiring `@socket.io/redis-streams-adapter` after `RedisService.createDuplicateClient()`; WebSocket-only transport, large buffers, connection recovery.                                                                                                                                                                                        | Why duplicate Redis clients vs sharing?                                                                  |
| `apps/api/app/api/websocket/dev-websocket.service.ts` | Dev-only: separate HTTP server on **PORT+1** for Socket.IO and raw WS (Vite dev cannot upgrade the main server).                                                                                                                                                                                                                                                          | How does dev wire the gateway to a separate port?                                                        |
| `apps/api/app/api/health/health.controller.ts`        | Terminus health: `GET /health/live` (heap), `GET /health/ready` (Redis + DB + heap), `GET /health/startup` (uptime). Fly's HTTP check targets `/health/ready`.                                                                                                                                                                                                            | Fail-Slow vs Fail-Fast tradeoffs between live and ready?                                                 |
| `apps/api/app/auth/`                                  | Better Auth: dynamic `betterAuth()` from `getBetterAuthConfig` with `DatabaseService`; mounts auth routes on Fastify; discovers hook metadata. Exports `authInstanceKey` and `BetterAuthService`.                                                                                                                                                                         | Where is `AuthGuard` defined?                                                                            |
| `apps/api/app/database/`                              | `schema.ts` (Drizzle pg tables matching Better Auth: `user`, `session`, `account`, `project`, …), `database.service.ts` (postgres client + drizzle + run migrations on init + `SqlLogger`), `database.module.ts`.                                                                                                                                                         | Where are migration files? Is pgvector used beyond the Docker image?                                     |
| `apps/api/app/redis/`                                 | Global `RedisModule`. ioredis primary client, metrics on connect/error, `createDuplicateClient()` for the Socket.IO adapter; strict `onModuleInit` (startup fails if Redis down).                                                                                                                                                                                         | TLS/`rediss` conventions?                                                                                |
| `apps/api/app/telemetry/otel.ts`                      | `@opentelemetry/sdk-node` bootstrap: resource attrs (service `tau-api`, Fly env), **PrometheusExporter** on `OTEL_METRICS_PORT` (default 9464), optional OTLP traces+logs when endpoint set, auto-instrumentations + LangChain instrumentation, fs/DNS/net/Fastify HTTP toggles.                                                                                          | Which metrics are scrape-only vs OTLP?                                                                   |
| `apps/api/app/telemetry/telemetry.module.ts`          | Global `TracerService` + `MetricsService`. `onApplicationShutdown` races `sdk.shutdown()`.                                                                                                                                                                                                                                                                                | Shutdown ordering with Fastify?                                                                          |
| `apps/api/app/telemetry/tracer.service.ts`            | `injectTraceContext`, `@Span()` method decorator wrapping OTEL spans (the only approved tracing approach — `withSpan` removed).                                                                                                                                                                                                                                           |                                                                                                          |
| `apps/api/app/telemetry/metrics.ts`                   | Instruments derived from canonical `TauMetrics` in `@taucad/telemetry` so API + UI metric names/buckets match.                                                                                                                                                                                                                                                            | How are new metrics versioned across packages?                                                           |
| `apps/api/app/api/telemetry/telemetry.controller.ts`  | `POST /v1/telemetry/ingest` (AuthGuard) maps client worker payloads to `MetricsService` OTEL instruments.                                                                                                                                                                                                                                                                 | Ingest vs Prometheus scrape?                                                                             |
| `apps/api/app/middlewares/request-id.middleware.ts`   | Echoes/sets `request-id` response header from Fastify's id for correlation.                                                                                                                                                                                                                                                                                               |                                                                                                          |
| `apps/api/app/filters/http-exception.filter.ts`       | Global catch-all: Zod validation/serialization, `HttpException`, OTEL span status, consistent `HttpErrorResponse` with request id.                                                                                                                                                                                                                                        | Which paths skip this (e.g. WebSocket)?                                                                  |
| `apps/api/app/logger/`                                | `nestjs-pino` async factory; Fastify logger integration in `main.ts` via `getFastifyLoggingConfig`.                                                                                                                                                                                                                                                                       | `LOG_SERVICE=fly` vs `console`?                                                                          |
| `apps/api/app/config/environment.config.ts`           | Zod-validated env (DB, auth, Redis, LLM keys, OTEL, CORS).                                                                                                                                                                                                                                                                                                                | Which vars required in test vs prod?                                                                     |
| `apps/api/app/testing/`                               | `create-test-app.ts` — pared-down Nest test module for chat pipeline with in-memory checkpointer, mocks for auth, headless RPC/FS/runtime helpers; `middleware-integration.test.ts`, `headless-*.ts`, `stream-*.ts`.                                                                                                                                                      | When use `create-test-app` vs full `AppModule`?                                                          |
| `apps/api-e2e/`                                       | Nx project; `src/api/api.spec.ts` is a minimal ky HTTP check.                                                                                                                                                                                                                                                                                                             | What `BASE_URL`/proxy starts the API?                                                                    |
| `apps/ui-e2e/`                                        | Playwright via `@nx/playwright`; `playwright.config.ts` serves `ui:serve-static`, Chromium project.                                                                                                                                                                                                                                                                       | How does CI pass `BASE_URL` for deployed previews?                                                       |
| `apps/api/fly.prod.toml`                              | App `tau-api`, `internal_port` 3000, HTTPS, `/health/ready` checks, Prometheus metrics on 9464 `/metrics`, env for frontend URL, LangSmith, OTEL, `AUTH_URL`.                                                                                                                                                                                                             | How does `auto_stop_machines` interact with WebSockets?                                                  |
| `apps/ui/netlify.toml`                                | Build `pnpm nx build ui`, publish `apps/ui/build/client`. Production/branch contexts set `TAU_API_URL`, `TAU_WEBSOCKET_URL`, `TAU_FRONTEND_URL`; notes `NX_PREFER_NODE_STRIP_TYPES` for Nx on Netlify. **Branch UI deploys to Netlify for every branch off `main`**; production UI is triggered manually from `.github/workflows/ci.yml` alongside the Fly.io API deploy. | How does preview UI target staging vs production API?                                                    |
| `.github/workflows/ci.yml`                            | Formatting (oxfmt), `docs:validate`, project names, `license-deps:validate`, Knip, `nx affected` `lint test build typecheck size`, concurrency for PRs.                                                                                                                                                                                                                   |                                                                                                          |
| `.github/workflows/deploy.yml`                        | Reusable Fly deploy choosing `fly.staging.toml` vs `fly.prod.toml` for `apps/{api,ui}`.                                                                                                                                                                                                                                                                                   | What triggers `deploy` vs `ci` on `main`?                                                                |

---

## 7. Shared libraries — `libs/`

| Path                  | What's there                                                                                                                                                                                                                                                            | Anticipated questions                                             |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `libs/chat/`          | RPC envelope (`schemas/rpc.schema.ts`, `constants/rpc.constants.ts`), tool input/output Zod schemas (`src/schemas/tools/`), `MyUIMessage` schema, `context-payload.schema.ts`, `createRpcDispatcher` + browser-side handler modules.                                    | What is the canonical tool part shape for streaming UI?           |
| `libs/types/`         | Cross-app constants and types: `constants/` (idPrefix, mime types, CAD constants), `types/` (`cad.types.ts`, `file.types.ts`, `graphics.types.ts`, `api.types.ts`).                                                                                                     | Where is `KernelId` or file-extension truth defined?              |
| `libs/utils/`         | `id.utils.ts` (`generatePrefixedId` — nanoid + typed prefixes from `@taucad/types/constants`), `path.utils.ts`, `file.utils.ts`, `schema.utils.ts`, `dispose.utils.ts` (`safeDispose` swallows errors so teardown chains complete), `cache.utils.ts`, `error.utils.ts`. | Why nanoid + typed prefixes vs UUID?                              |
| `libs/units/`         | Physical units: parser, converter, constants, types for magnitudes used across CAD/export UX.                                                                                                                                                                           | How are mm vs m resolved consistently with converter metadata?    |
| `libs/api-extractor/` | Generated bundled `.d.ts` maps as JSON (`src/generated/*/...bundled.json`) plus extractors for replicad, JSCAD, OpenCASCADE, Manifold, KCL. `src/index.ts` exports `kernelTypeMaps`. UI's `TypeAcquisitionService` registers them via Monaco `addExtraLib`.             | How are bundled typings updated when a kernel dependency changes? |
| `libs/oxlint/`        | Custom oxlint JS plugins + ESLint MDX parser (`@taucad/oxlint/mdx-parser`); tsgolint-based type-aware JSDoc codeblock checking.                                                                                                                                         | Why oxlint + ESLint hybrid?                                       |
| `libs/vite/`          | `@taucad/vite` Vite 8 plugins with `*.vite-plugin.ts` suffix and `vite:` prefix: `cross-origin-isolation.vite-plugin.ts`, `oxc-runtime-esm.vite-plugin.ts` (helps `@oxc-project/runtime` resolve in SSR ESM runner).                                                    | When should I add a Vite plugin here?                             |
| `libs/tau-examples/`  | Example projects shipped with the editor.                                                                                                                                                                                                                               |                                                                   |

---

## 8. Other packages

| Path                    | What's there                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Anticipated questions                                                          |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `packages/converter/`   | Standalone Assimp/WASM pipeline: `import.ts`/`export.ts`, `conversion.ts`, `loaders/`, `exporters/`, `formats.ts` (lightweight metadata exposed at `@taucad/converter/formats`). Three WASM variants: mini (import-only), all (full import + lib3mf), exporter (export + lib3mf). lib3mf v2.4.1 integrated via `Lib3MFBridge` for 3MF. **Architecture rule**: exporters carry NO per-source-format mapping — importers declare units/coordinate-system as scene metadata; each exporter applies transformations to its own spec (GLB Y-up/meter, 3MF Z-up/mm). | How does `exportFromGlb` apply axis/unit metadata? Where are round-trip tests? |
| `packages/memory/`      | (covered above in §5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |                                                                                |
| `packages/testing/`     | Geometry analysis for benchmarks and CI: `geometry/analyze-glb.ts`, `connected-components.ts`, `evaluate-requirement.ts`, `watertight.ts`, `schemas.ts` (requirement specs). Used by the agent's `test_model` tool and by benchmarks.                                                                                                                                                                                                                                                                                                                          | How are GLB requirements evaluated consistently?                               |
| `packages/cli/`         | `bin.ts`, `commands/export.ts` — headless CAD CLI (`taucad export <file> --ext=glb`) using runtime + Node FS.                                                                                                                                                                                                                                                                                                                                                                                                                                                  | How is the same kernel stack used without a browser worker?                    |
| `packages/json-schema/` | `@taucad/json-schema` owns Draft-07 JSON Schema TS typings — fully self-contained, no external `json-schema` npm types dep.                                                                                                                                                                                                                                                                                                                                                                                                                                    |                                                                                |
| `packages/telemetry/`   | Canonical metric definitions + ingest schemas + Prometheus naming + reporter; consumed by API and UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |                                                                                |

---

## 9. Meta layer — policies, research, skills

### Policies (`docs/policy/`, 39 docs)

Binding rules. Examples:

- **`testing-policy.md`** — Vitest patterns; no brittle Radix mocks.
- **`library-api-policy.md`** — `defineX` shape, options objects, plugin patterns.
- **`xstate-policy.md`** — State machines own lifecycle; UI clients send events only.
- **`filesystem-policy.md`** — Single writer, transferable binary payloads, kernel vs UI watch planes.
- **`runtime-architecture-policy.md`** — Worker stack, plugins, transport, lifecycle.
- **`runtime-telemetry-policy.md`** — Kernel/worker telemetry contracts.
- **`context-engineering-policy.md`** — System prompts and tool descriptions for LLM agents.
- **`filesystem-context-policy.md`** — `.tau/` context files, agent middleware ordering, transcript/skill storage.
- **`commit-policy.md`**, **`jsdoc-policy.md`**, **`react-policy.md`**, **`react-testing-policy.md`**, **`accessibility-policy.md`**, **`color-policy.md`**, **`color-space-policy.md`**, **`gltf-construction-policy.md`**, **`rendering-pipeline-policy.md`**, **`worker-policy.md`**, **`rpc-policy.md`**, **`api-error-policy.md`**, **`api-evolution-policy.md`**, **`storage-policy.md`**, **`engineering-forms-policy.md`**, etc.

### Architecture (`docs/architecture/`)

- **`runtime-topology.md`** — Proposal: autonomous reactive render service (worker watches dependencies, debounces, renders, pushes geometry; LSP-like kernel).
- **`runtime-editor.md`** — Reference companion: file-manager worker vs kernel worker vs main thread; two watch planes.

### Research (`docs/research/`, ~183 docs)

Snapshot investigations. Themed clusters:

- **Kernel / WASM / OCCT** — `occt-v8-migration.md`, `ocjs-wasm-build-comparison.md`, `bindings.py` analyses, `ocjs-typescript-codegen-gap-analysis.md`, `ncollection-auto-discovery-build-validation.md`, `embind-smart-pointer-stale-ptr.md`, `wasm-heap-view-detachment.md`.
- **Converter / formats** — `assimp-unified-axis-unit-contract.md`, `unified-export-pipeline-architecture.md`, `export-pipeline-v2.md` … `v6-implementation-audit.md`, `3mf-export-scale-orientation-manifold.md`, `tinyusdz-fork-fixes-vs-upstream.md`.
- **Runtime / schemas** — `define-transcoder-type-safety.md`, `export-option-schema-architecture.md`, `nativehandle-serialization-and-pipeline-architecture.md`, `geometry-data-transfer-architecture.md`.
- **Filesystem / shared memory** — `filesystem-architecture.md`, `filesystem-mount-only-architecture.md`, `shared-worker-fs-architecture.md`, `shared-memory-geometry-pipeline.md`, `shared-pool-stale-parameter-reads.md`, `safari-cross-origin-isolation.md`.
- **Agent / AI / LangGraph** — `system-prompt-audit.md`, `agent-loop-safeguards.md`, `context-summarization-compaction.md`, `agent-skill-patterns.md`, `agent-skill-patterns-v2.md`, `claude-code-architecture-mining.md`, `claude-code-prompting-techniques.md`, `claude-code-subagent-architecture.md`, `multimodal-agent-image-storage-patterns.md`, `concurrent-chat-streams-agents-panel.md`.
- **Observability** — `observability-architecture.md`, `grafana-observability-gaps.md`, `observability-metrics-audit.md`.
- **UI / perf** — `ui-startup-performance-gap-analysis.md`, `netlify-ui-deployment-strategy.md`.

### Cursor skills (`.agent/skills/`)

Workflow scripts agents follow for repeatable tasks: **adding-tools** (new chat tool end-to-end), **create-package**, **create-policy**, **create-research**, **create-vite-plugin**, **langgraph** (LangGraph docs MCP), **mine** (deep external mining), **new-kernel**, **occt-wasm-build**, **package-release**, **pr-review-coordinator**, **repos**, **submit-pr**, **typescript-overloads**.

### `.cursor/rules/`

Small `.mdc` rule files Cursor injects: `agents-md.mdc`, `context-engineering.mdc`, `documentation.mdc`, `eslint.mdc`, `package-manager.mdc`, `parameter-design.mdc`, `tailwind.mdc`, `testing.mdc`, `ui-design.mdc`, `xstate.mdc`.

### `AGENTS.md`

Primary agent onboarding doc: commands (`pnpm nx …`, infra, DB, `ci:affected`, `docs:validate`), one-line architecture, project map table, skills table, conventions, **Learned User Preferences** and **Learned Workspace Facts** (continual-learning-mined facts, not hand-edited).

---

## 10. Build / dev tooling

- **`tools/tsdown.plugin.ts`** — infers tsdown build targets from `tsdown.config.ts`.
- **`tools/copy-files-from-to.plugin.ts`** — Nx plugin to copy assets (e.g. WASM binaries) from `copy-files-from-to.cjson`; wires `build`/`dev`/`test` to depend on it.
- **`tools/generate-cjs-dts.ts`** — post-build copies ESM `.d.ts` → `.d.cts` with `.cjs` specifiers for Node CJS resolution.
- **`tools/pkgcheck.ts`** — pre-publish checks (publint, attw, madge).
- **`tools/size-build.mjs`** — merges per-project `.size-limit.json` for CI size-limit.
- **`tools/tsgo.plugin.ts`** — replaces default typecheck with **tsgo** (Go-based TS compiler, `--noEmit`, no project refs).
- **`tools/workspace-plugin/`** — Nx generator `package` for new `@taucad/`\* libs.
- **`scripts/`** — workspace automation: `src/repos/` (clone/sync external forks per `repos.yaml`), `src/wasm-experiment.sh` + `experiments/*.yml` (WASM build matrix), `validate-project-names.ts`, `validate-frontmatter.ts`, `generate-opencascade-changelog.mts`, `update-license-deps.mts`.
- *`*repos/`+`repos.yaml`** — gitignored clones of external dependency forks (OCCT, assimp, replicad, tinyusdz, lib3mf, vscode reference, emscripten, ForgeCAD). `pnpm repos add <owner/repo> -g <group> --clone`. Used to investigate dependency source instead of `node_modules`.
- **`patches/`** — pnpm patches for `replicad`, `@langchain/openai@1.4.0`, `@taucad/kcl-wasm-lib`, `@kittycad/lib`, `@zenfs/core`.

---

## 11. Cross-cutting design themes

1. **`defineX` author APIs accept Zod only** — `defineKernel`, `defineTranscoder`, `defineMiddleware`. JSON Schema and defaults are resolved internally so authors don't repeat themselves; `z.input` (not `z.infer`) is used for consumer-facing types so `.default()` optionality is preserved.
2. **Schema-driven runtime APIs use generic inference end-to-end** — kernel formats, transcoder edges, `client.export` options. No `Record<string, unknown>` escape hatches when a schema can drive the type.
3. **No backwards compatibility for unreleased/internal APIs** — roll forward with breaking changes; do not add shims for consumers that don't exist yet.
4. **Kernels are self-contained** — no shared cross-kernel schemas; each kernel duplicates schemas to remain independently extractable.
5. **Agent surface is explicit** — RPCs, tool inputs, system prompts require explicit parameters with no silent `?? mainEntryFile` fallbacks. The LLM must commit to the file/target it intends.
6. **`generatePrefixedId(idPrefix.*)` everywhere** — never `crypto.randomUUID()`. Typed prefixes from `@taucad/types/constants`.
7. **Geometry transfer is two-layer** — byte array (`Uint8Array`) and shared memory (`SharedPool`-backed `SharedArrayBuffer`). The dispatcher's `toTransportGeometry()` auto-stores geometry before checking for pooled delivery (store-then-check with inline fallback). `RuntimeClient.resolveGeometry()` encapsulates SAB → ArrayBuffer resolution so consumers are transfer-mode agnostic.
8. **Browser `TextDecoder` rejects `SharedArrayBuffer`-backed views** — copy at consumer boundary required.
9. *`*fromSafeAsync`(not`fromPromise`)\*\* for all UI XState async actors — Strict Mode and abort safety.
10. **Activity grouping in chat** — assistant tool/reasoning parts are folded into Cursor-style collapsible groups; in-flight group remains visually ungrouped until the next part signals end-of-group; binary file/CAD mutations are never grouped.
11. **Avoid type assertion escape hatches** — `as never`, `as unknown as`, unnecessary `as const` on returns are banned.
12. **No regex-based assertions in tests bound for upstream PRs** — they are hacky and indefensible in review; prefer principled assertions on parsed structures.

---

## 12. Suggested research questions

If I haven't given you a specific question yet, useful angles include:

- How does Tau's multi-kernel runtime compare to OpenSCAD Playground, KittyCAD ModelingApp, or build123d? Where would Tau need to evolve to add a new kernel category (sim, electrical, firmware)?
- Audit the LangChain agent middleware stack against Claude Code, Cursor Composer, and Aider for context-window robustness.
- Compare Tau's filesystem layer (`packages/filesystem` + `@taucad/runtime` bridge + `apps/ui` machines) to VS Code's filesystem service. What are the gaps for a future POSIX-style FS in the browser?
- Evaluate Tau's geometry transport (byte vs SAB pool) against patterns in Three.js, Filament, Bevy WASM, and tursodatabase for high-throughput cross-thread data.
- Audit the `defineKernel` / `defineTranscoder` plugin APIs for DX gaps, type safety holes, and missing capability lifecycle hooks.
- Trace cross-origin isolation strategy end-to-end (Netlify headers → Vite plugin → runtime assertion → SAB fallback) and compare against Excalidraw, Figma, Photopea, Stable Diffusion WebUI.
- Compare the policy/research/skill meta-layer to Claude Code's slash-command + AGENTS.md model.

---

When ready, append your specific research question. Treat the directory pointers above as the source of truth for what exists in the repo; cross-reference external sources for anything not covered.

```

## Notes for the doc author

- The pasteable block uses a four-backtick fence so the inner triple-backtick code blocks (the directory tree) survive intact.
- Anticipated questions are kept inline per directory so the LLM doing the deep research can use them as starting prompts when it pulls a file open.
- Cross-cutting themes (§11) duplicate facts that live in `AGENTS.md` "Learned Workspace Facts" — kept here so the brief is self-contained when shared outside the repo.
- If the repo gains/removes major packages, update §1 (top-level layout), §3-§8 (per-area tables), and §9 (research/skill counts) and bump `updated:` in frontmatter.

```

```
