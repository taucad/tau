---
title: 'Tau Cloud Activation Status: Remaining Manual Steps to Complete the Terraform Migration'
description: 'Audit of the `repos/tau-cloud` (HCP Terraform) capability — current state in IaC and every remaining manual operator step to fully activate the migration across staging and prod-us. Supersedes the IaC strategy, Netlify UI deployment, and prod+staging deployment-status audits.'
status: draft
created: '2026-05-14'
updated: '2026-05-14'
category: audit
related:
  - docs/architecture/production-gitops-runbook.md
  - docs/architecture/ui-deployment-topology.md
  - docs/architecture/netlify-multi-region-functions.md
  - docs/research/netlify-production-performance-audit.md
  - docs/research/staging-cors-coep-safari-rendering-audit.md
  - docs/research/production-observability-readiness.md
  - docs/research/observability-implementation-status.md
---

# Tau Cloud Activation Status: Remaining Manual Steps to Complete the Terraform Migration

Single-source-of-truth audit of the `repos/tau-cloud` HCP Terraform capability — what is already in IaC, what is still being applied by hand, and the operator checklist that must be executed to declare the migration "fully active" for both `staging` (`taucad.dev`) and `prod-us` (`tau.new`).

## Executive Summary

The tau-cloud capability is **functionally code-complete** for the V1 slice (Cloudflare DNS + Netlify site build settings + GitHub repo + Netlify env vars + a presence-only Fly secret audit shape). The Netlify staging site (`taucad-staging`, `id=a006a282-6254-499d-a38c-d089b47359c7`) and production site (`taucad-prod-us`, `id=7077c956-6b41-4056-b589-2b3c837435eb`) both deploy from Terraform-managed configuration, DNS authority for `taucad.dev` has migrated from Namecheap to Cloudflare, and the GitHub repo settings (rulesets, environments, App credentials) are reconciled from the `tau-repo` stack. Two production-blocking remediations landed alongside this audit:

1. `modules/netlify-site` now ships a `terracurl_request.repo_link` resource (devops-rob/terracurl provider) that PATCHes the Netlify site with a `repo` block so `prevent_non_git_prod_deploys = true` no longer 422s on first apply.
2. Both stacks declare `netlify_github_installation_id = 96259086` + `repo_path = "taucad/tau"` + `repo_branch` (`main` for staging, `production` for prod-us) so the GitHub App linkage is part of the reviewable plan rather than dashboard click-ops.

**What remains is exclusively operator work** — vendor APIs that intentionally have no programmatic surface (Netlify PAT minting, OAuth redirect URIs, registrar NS edits, Let's Encrypt issuance windows) plus a small set of one-time HCP variable/secret seeds that must precede the first apply on each stack. **The migration is not "blocked on code"; it is sequenced by human gates.** The checklist in [Remaining Manual Steps](#remaining-manual-steps) below is the canonical list — every prior research doc that catalogued a subset of these steps is now superseded by this audit.

## Validation Snapshot (2026-05-14)

Live CLI probes against HCP Terraform, GitHub, Cloudflare, Netlify, Fly, and the registrar resolvers. ✅ verified done, 🟡 partial / mid-cutover, ⏳ outstanding, ⚠️ unverifiable from CLI (vendor-only surface).

| Gate                                                                              | Status | Evidence                                                                                                                                                                                                                                                            |
| --------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1 — Netlify GitHub App on `taucad` org                                           | ✅     | `gh api /orgs/taucad/installations` → `{"id":96259086,"name":"netlify"}`                                                                                                                                                                                            |
| A2 — `netlify_github_installation_id` committed to both stacks                    | ✅     | `stacks/cloud/staging/terraform.auto.tfvars:26` + `stacks/cloud/prod-us/terraform.auto.tfvars:39` both = `96259086`                                                                                                                                                 |
| A3 — `terracurl` in workspace lockfiles                                           | ✅     | Implied by successful 2026-05-11 applies; `versions.tf` pins `devops-rob/terracurl >= 1.2.0, < 2.0.0`                                                                                                                                                               |
| A4 — HCP Sensitive variables seeded                                               | ✅     | `tau-cloud-staging` + `tau-cloud-prod-us`: `cloudflare_api_token`, `netlify_api_token`, `posthog_cli_token`/`_env_id`; `tau-repo`: `github_app_*`, `release_bot_app_*`, `fly_api_token_{staging,production,review}`, `nx_cloud_access_token_{read_write,read_only}` |
| A5 — Re-run apply with `terracurl_request.repo_link`                              | ✅     | Latest applied runs: `tau-cloud-staging` @ 2026-05-11T20:57:59Z, `tau-cloud-prod-us` @ 2026-05-11T20:57:59Z (`feat(netlify): add GitHub repository linking via terracurl for git-mode sites`)                                                                       |
| B1 — Netlify PAT placed in HCP + GH                                               | ✅     | `netlify_api_token` Sensitive var present on both stack workspaces; `npx netlify status` authenticates as Richard Fontein under TauCAD team                                                                                                                         |
| B2 — OAuth redirect URIs verified (GitHub + Google)                               | ⚠️     | Vendor consoles only — `GITHUB_CLIENT_ID`/`GOOGLE_CLIENT_ID` present on both Fly apps; auth flows live on `taucad.dev` (no reported regressions). Recommend a one-shot login smoke test on `tau.new` after the prod cutover.                                        |
| B3 — Cloudflare API token (Zone:DNS:Edit)                                         | ✅     | `cloudflare_api_token` Sensitive vars present; applies succeed against `taucad.dev` zone records                                                                                                                                                                    |
| B4 — Cloudflare zones bootstrapped clean                                          | 🟡     | `taucad.dev` → Cloudflare-authoritative (NS `nora.ns.cloudflare.com`, `major.ns.cloudflare.com`); `tau.new` zone may exist in Cloudflare but NS still delegate to Namecheap                                                                                         |
| C1 — Registrar zone inventory reflected in tfvars                                 | ✅     | `mx_records`/`txt_records`/`caa_records` populated in `terraform.auto.tfvars` for both stacks                                                                                                                                                                       |
| C2 — Registrar NS delegation to Cloudflare                                        | 🟡     | **`taucad.dev` cut over** (`dig NS taucad.dev` returns Cloudflare NS pair); **`tau.new` NOT cut over** (`dig NS tau.new` still returns `dns1.registrar-servers.com` + `dns2.registrar-servers.com`)                                                                 |
| C3 — Let's Encrypt cert issuance                                                  | ✅     | `taucad.dev` cert valid `Apr 21 — Jul 20 2026` (Let's Encrypt E8); `tau.new` cert valid `Apr 4 — Jul 3 2026` (Netlify pre-provisioned ahead of cutover)                                                                                                             |
| C4 — Registrar zone locked post-cutover                                           | ⚠️     | Cannot probe registrar-side lock; safe to assume locked for `taucad.dev`, N/A for `tau.new` until C2 lands                                                                                                                                                          |
| D1 — Production trail-PR bootstrap                                                | ✅     | `gh api /repos/taucad/tau/branches/production` → `protected=true`; rulesets active on `main` + `production` (`id=16120800`, `id=16120678`); environments `production`/`staging`/`review-api`/`nx-cloud-write` exist with their expected secrets                     |
| D2 — Production smoke test                                                        | ⏳     | Subjective per-release gate (by design)                                                                                                                                                                                                                             |
| D3 — `OTEL_EXPORTER_OTLP_HEADERS` + `_ENDPOINT` on both Fly apps                  | ⏳     | `flyctl secrets list -a tau-api` + `-a tau-api-staging` shows neither key present (and `OTEL_EXPORTER_OTLP_ENDPOINT` not in `fly.*.toml`) — traces/logs still not reaching Grafana Cloud                                                                            |
| D4 — Grafana Cloud datasources (`prometheus`/`loki`/`tempo`/`postgresql`/`redis`) | ⚠️     | Not probeable without Grafana admin token; tracked separately by `production-observability-readiness.md`                                                                                                                                                            |
| D5 — `GRAFANA_URL` + `GRAFANA_API_KEY` env secrets                                | ⏳     | Not present on `staging`/`production` GH environments (env secrets enumerated → `FLY_API_TOKEN` only)                                                                                                                                                               |
| D6 — Fly Log Shipper app                                                          | ⏳     | `flyctl apps list` shows no log-shipper app under the personal org                                                                                                                                                                                                  |
| D7 — Stale Netlify dashboard env var sweep                                        | ✅     | Pulled live env from both sites: no `wss://api.tau.new` mismatch on staging; `taucad-prod-us` has `production`-scoped TAU\_\* with the correct prod endpoints                                                                                                       |
| E2 — Secret rotation cadence                                                      | n/a    | Steady-state operation; no current rotation due                                                                                                                                                                                                                     |

**Net cutover gap.** The biggest residual is **C2 for `tau.new`**: NS delegation still points at Namecheap, so the live apex (`tau.new`) and `www.tau.new` continue to be served by the legacy Fly UI app (`taucad`), even though `taucad-prod-us` is fully provisioned in Netlify with a `tau.new` custom-domain attachment and a valid Let's Encrypt cert pre-issued. The follow-up checklist below is reduced accordingly.

## Scope and Non-Goals

**In scope.** Manual operator gates required to activate `repos/tau-cloud` end-to-end across `staging` + `prod-us`: HCP workspace seeding, Netlify GitHub App install, Cloudflare bootstrap, registrar nameserver delegation, Netlify PAT placement, OAuth redirect-URI verification, TLS provisioning wait windows, observability secret placement, and the human smoke gates that precede a production promotion.

**Out of scope.** Application code changes (already merged); the `prod-eu` regional rollout (a follow-up that re-runs the same activation playbook with `enabled = true` on the existing stack template); OpenTofu fallback (documented in the now-superseded `iac-terraform-strategy.md` Appendix D); the observability instrumentation gaps catalogued in `prod-staging-ui-deployment-status.md` (orthogonal to tau-cloud activation — owned by `production-observability-readiness.md`).

## Methodology

1. **Transcript replay.** Walked the [Netlify 422 Terracurl fix](fb0b5c83-9110-41c0-9855-35566d52f8a1) transcript (the in-flight Cloud Infra V1 remediation) to enumerate every operator gate the plan deliberately marked `pending`.
2. **Prior research extraction.** Mined the now-superseded `iac-terraform-strategy.md`, `netlify-ui-deployment-strategy.md`, and `prod-staging-ui-deployment-status.md` for every documented manual step; deduplicated against the runbook content already authored in `repos/tau-cloud/docs/{netlify-iac-runbook.md,dns-migration-plan.md}` and `docs/architecture/production-gitops-runbook.md`.
3. **State verification.** Cross-checked the current state of the Terraform stacks (`repos/tau-cloud/stacks/cloud/{staging,prod-us}`), the modules (`modules/{cloudflare-dns,netlify-site}`), the bootstrap script (`scripts/netlify-bootstrap-site.sh`), and the canonical runbook (`docs/netlify-iac-runbook.md`) to confirm each catalogued "manual step" is still required (vs already absorbed into a Terraform resource).
4. **Categorisation.** Classified residuals into four classes — **first-apply gates** (must complete before the first `terraform apply` succeeds on a given stack), **DNS cutover gates** (registrar NS delegation + TLS wait), **vendor-API-bound gates** (intentionally interactive: PAT mint, OAuth URI), and **promotion gates** (smoke + human judgement).

## Current State

| Surface                                                  | Owner                                                                                                      | Status                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cloudflare DNS (`taucad.dev` + `tau.new`)                | `modules/cloudflare-dns` + zone-record rows in `stacks/cloud/{staging,prod-us}/terraform.auto.tfvars`      | **In IaC.** Zones added to Cloudflare; NS delegation already flipped at registrar; every `api.*`, `cdn.*`, apex, `www`, MX, SPF/DKIM/DMARC, CAA record reviewable in plan diff.                                                                                                                                                                          |
| Netlify staging site `taucad` (Git-mode)                 | `modules/netlify-site` (staging stack)                                                                     | **In IaC.** `prevent_non_git_prod_deploys = true` enforced via `netlify_site_build_settings`; GitHub App linkage owned by `terracurl_request.repo_link` (PATCH on create, PUT `/unlink_repo` on destroy).                                                                                                                                                |
| Netlify production site `taucad-prod-us` (CI-mode)       | `modules/netlify-site` (prod-us stack)                                                                     | **In IaC.** `stop_builds = true`; UI deploys via `netlify deploy --prod` from `prod-deploy-ui.yml` (merged on the `production` trail-PR flow); `functions_region = us-east-2` single-region per Netlify Terraform reality (see `netlify-multi-region-functions.md`).                                                                                     |
| Netlify env vars (non-secret + secret)                   | `modules/netlify-site` `plain_environment_variables` + `netlify_environment_variable { is_secret = true }` | **In IaC.** Secret rows enumerate explicit `contexts = ['production', 'deploy-preview', 'branch-deploy']` (Netlify rejects `'all'` for secrets); `apps/ui/netlify.toml` remains source of truth for non-sensitive runtime values inside the site bundle.                                                                                                 |
| Fly app + machine config                                 | `apps/api/fly.{staging,prod}.toml` + `flyctl deploy`                                                       | **Not in IaC** (intentional — `fly-apps/terraform-provider-fly` archived; deploy stays on `flyctl` per accepted V1 trade-off). The `tau-secrets` presence-only module is the documented future bridge; not landed in V1.                                                                                                                                 |
| GitHub repo settings (rulesets, environments, App creds) | `stacks/repo/tau` (HCP workspace `tau-repo`) using `integrations/github` provider                          | **In IaC.** Branch rulesets on `main` + `production` + `release-managers` team; per-environment `FLY_API_TOKEN` secrets (`staging`, `production`, `review-api`); split Nx Cloud tokens (`NX_CLOUD_ACCESS_TOKEN_READ_WRITE` on `nx-cloud-write` env, `_READ_ONLY` at repo scope).                                                                         |
| HCP Terraform org + workspaces                           | HCP Terraform Free tier, org `TauCAD`, workspaces `tau-cloud-staging` / `tau-cloud-prod-us` / `tau-repo`   | **Bootstrapped.** VCS-driven from `taucad/tau-cloud`; speculative plan on PR; apply gated by manual approval (auto-apply never enabled on prod-us).                                                                                                                                                                                                    |
| Cloudflare R2 (V1.5+)                                    | `modules/cloudflare-r2` + `modules/fly-secrets`                                                            | **In IaC** — single bucket per env (`tau-staging-content` / `tau-prod-content`) attached to `cdn.<zone>` natively via `cloudflare_r2_custom_domain` (no Worker). Lifecycle, CORS, zone cache rules, scoped API token, and Fly R2 secrets all owned by `module.cloudflare_r2` + `module.fly_api_secrets`. Operator gates F1–F7 remain before first apply. |
| `tau-secrets` presence audit                             | `modules/tau-secrets` (planned)                                                                            | **Deferred** per V1 scope. Closes the loop on the [Finding 11 cert↔app pairing](staging-cors-coep-safari-rendering-audit.md) defense by diffing `flyctl secrets list` against an expected name set.                                                                                                                                                      |

### V1 Trade-offs Already Accepted

| Decision                                                                                               | Captured in                                        | Why                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------ | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **HCP Terraform (BSL 1.1) over OpenTofu**                                                              | (superseded) `iac-terraform-strategy.md` Finding 1 | Stacks + Variable Sets + HCP-managed state + Run UI on PR outweigh the BSL license cost for a private tau-cloud repo. Reversal path documented but not pursued.                                           |
| **Single stack per environment** (`stacks/cloud/{staging,prod-us}`), not Stacks-block multi-deployment | This audit                                         | Stacks GA's 1-deployment-per-group constraint plus the V1-narrower scope made plain root-modules per env the pragmatic landing pad; the architecture blueprint's full Stacks layout remains the V2+ target. |
| **`functions_region` single string** (not a list)                                                      | `netlify-multi-region-functions.md`                | Netlify Terraform exposes one region; multi-region SSR requires per-region sites + geo DNS (deferred).                                                                                                      |
| **No `is_prod` boolean** anywhere                                                                      | (superseded) `iac-terraform-strategy.md` Finding 7 | Stack/site/workspace names already carry `prod-us` (not `prod`) so adding `prod-eu` later is mechanical.                                                                                                    |
| **Fly provider skipped**                                                                               | (superseded) `iac-terraform-strategy.md` Finding 4 | Official provider archived 2024; community fork single-maintainer. Runbook + `flyctl secrets set` is the accepted V1 path.                                                                                  |

## Remaining Manual Steps

Numbered globally so each step can be referenced in PRs and HCP run notes. Steps are grouped by gate type and ordered by sequence dependency.

### A. First-apply gates (must precede the next `terraform apply` on each stack)

All A-class gates are **✅ DONE** as of 2026-05-14 — see the [Validation Snapshot](#validation-snapshot-2026-05-14). Retained for future regional stacks (`prod-eu`, `prod-ap`).

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                                                    | Why it cannot be scripted                                                                 | Frequency                                   |
| --- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------- |
| A1  | ✅     | **Install the Netlify GitHub App on the `taucad` org** at <https://github.com/apps/netlify>; grant access to `taucad/tau` only (avoid "all repositories").                                                                                                                                                                                                                | Vendor consent flow — GitHub App installs are intentionally interactive.                  | One-time per org.                           |
| A2  | ✅     | **Capture the installation ID** from <https://github.com/settings/installations/<id>> and commit it to **both** stacks' `terraform.auto.tfvars` as `netlify_github_installation_id = <int>`. Non-secret; same trust level as `netlify_site_id`.                                                                                                                           | The integer is only visible after A1 lands and is unique per GitHub org.                  | One-time per org.                           |
| A3  | ✅     | **Run `terraform init -upgrade`** (or click HCP "Start new run" with init re-resolution) on each stack workspace so the lockfile pulls `devops-rob/terracurl >= 1.2.0, < 2.0.0`.                                                                                                                                                                                          | Lockfile is per-workspace; HCP never auto-upgrades providers without an explicit init.    | One-time after every new provider addition. |
| A4  | ✅     | **Seed HCP Sensitive workspace variables** on `tau-cloud-staging`, `tau-cloud-prod-us`, and `tau-repo`: `cloudflare_api_token`, `netlify_api_token`, `posthog_cli_token`, `posthog_cli_env_id`, `nx_cloud_access_token_read_write`/`_read_only`, `fly_api_token_staging`/`_production`/`_review`, `github_app_pem`/`_id`/`_installation_id`, `release_bot_app_pem`/`_id`. | HCP-encrypted variable storage is the audit boundary; secret material is never committed. | One-time per workspace + on rotation.       |
| A5  | ✅     | **Re-run `terraform apply`**: prod-us first (smoke that the `prevent_non_git_prod_deploys` gate flips cleanly with the new `terracurl_request.repo_link`), then staging for parity.                                                                                                                                                                                       | HCP apply gate; required after A1–A4 land.                                                | Per release that touches stack inputs.      |

### B. Vendor-API-bound gates (no programmatic surface exists)

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                          | Why it cannot be scripted                                                                                    | Frequency                                   |
| --- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------- |
| B1  | ✅     | **Mint a Netlify Personal Access Token** at <https://app.netlify.com/user/applications#personal-access-tokens> and paste it into the HCP Sensitive variable `netlify_api_token` (A4) plus, where still needed, the GitHub `production` environment secret `NETLIFY_AUTH_TOKEN` for `prod-deploy-ui.yml`.                                        | Netlify does not expose a programmatic PAT-mint endpoint (the PAT is the root credential for `netlify-cli`). | One-time per Netlify account + on rotation. |
| B2  | ⚠️     | **Verify OAuth redirect URIs** in the Google Cloud Console (OAuth 2.0 client) and the GitHub OAuth App settings page: `https://{taucad.dev,tau.new}/api/auth/callback/{github,google}` must all be listed. Recommended: one-shot login smoke test on `tau.new` after the C2 cutover lands — auth callbacks fail at runtime if a URI is missing. | Neither GCP nor GitHub exposes an API to mutate an OAuth client's redirect URI list — web console only.      | One-time per environment per provider.      |
| B3  | ✅     | **Mint per-zone Cloudflare API tokens** (Zone:DNS:Edit + Zone:Zone:Read on the relevant zone) and place in `cloudflare_api_token` per A4. **Never** mint with broader "Zone:Edit" — DNS-record CRUD will 403 with `code:10000 Authentication error` after the data-source read succeeds.                                                        | Cloudflare's API token UI is interactive; the scopes matter and per-zone tokens contain blast radius.        | One-time per zone + on rotation.            |
| B4  | 🟡     | **Skip Cloudflare's auto-scan** when adding `taucad.dev` and `tau.new` as zones (or delete every record it auto-imported). Terraform must own a clean zone — collisions surface as apply errors `81053` / `81058` ("record already exists"). `taucad.dev` confirmed clean (apply succeeded); confirm zone hygiene for `tau.new` before C2.      | Cloudflare's Quick Scan is an opinionated dashboard flow that conflicts with declarative ownership.          | One-time per zone.                          |

### C. DNS cutover gates

**Net-new actionable work** lives here — `tau.new` is mid-cutover (Netlify pre-provisioned, NS still on Namecheap).

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Why it cannot be scripted                                                                                  | Frequency          |
| --- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------ |
| C1  | ✅     | **Inventory each Namecheap zone export** (`Advanced DNS → Export`) and ensure every TXT/DKIM/DMARC row is reflected in `mx_records` / `txt_records` / `caa_records` of the relevant stack's `terraform.auto.tfvars`. The `tau.new` TXT block was historically cropped — scroll the full export.                                                                                                                                                                                                 | Registrar exports must be reconciled by a human; no API gives an authoritative "you have all rows" signal. | One-time per zone. |
| C2  | 🟡     | **Cut nameservers at Namecheap** to the Cloudflare-assigned pair (captured during B4). Verify with `dig @1.1.1.1 NS <zone> +short` until the Cloudflare pair returns. **`taucad.dev` cut over** (`nora.ns.cloudflare.com` + `major.ns.cloudflare.com`). **`tau.new` not yet** — `dig NS tau.new` still returns `dns1.registrar-servers.com` + `dns2.registrar-servers.com`, so the live apex + `www` continue to serve from Fly `taucad` app. **This is the principal remaining cutover step.** | Registrar APIs vary and NS-glue edits require owner credentials + judgement about propagation windows.     | One-time per zone. |
| C3  | ✅     | **Wait for Netlify-managed Let's Encrypt TLS issuance** after attaching a custom domain (`tau.new` / `taucad.dev` / future regional hostnames). Poll `netlify api showSiteTLSCertificate` until `state == "issued"`. Certs already issued: `taucad.dev` (E8, Apr 21 → Jul 20 2026); `tau.new` (E8, Apr 4 → Jul 3 2026, pre-provisioned).                                                                                                                                                        | Netlify exposes cert state read-only — there is no force-provision endpoint or deterministic SLA.          | Per cutover.       |
| C4  | ⚠️     | **Lock the zone at Namecheap** after C2 so subsequent record changes flow exclusively through `tau-cloud` PRs. NS records remain at the registrar (locked); every other record lives in IaC. Assume done for `taucad.dev`; deferred for `tau.new` until C2 lands.                                                                                                                                                                                                                             | Registrar-level "DNS lock" is an account-side toggle; no API surface.                                      | One-time per zone. |

### D. Promotion + observability seed gates

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Why it cannot be scripted                                                                               | Frequency                 |
| --- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------- |
| D1  | ✅     | **Bootstrap the production trail-PR flow**: one-time `git push origin main:production` to create the `production` branch; merge the `tau` PR that ships `prepare-prod-release.yml` + `.github/CODEOWNERS`; verify `RELEASE_BOT_APP_ID` / `RELEASE_BOT_APP_PRIVATE_KEY` exist before the next `prepare-prod-release` run. Confirmed: `production` branch present + protected; rulesets active for both `main` and `production`; `release_bot_app_id`/`_pem` seeded in `tau-repo`. | Git history mutation + GitHub App key placement are inherently one-time human acts.                     | One-time at activation.   |
| D2  | ⏳     | **Production smoke test** before approving the trail PR: visual correctness (homepage + viewer + docs render), behavioural correctness (CAD viewer, file manager, chat turn), end-to-end auth (sign in via GitHub + Google on `tau.new`, cookies set, callbacks land on `api.tau.new`). E2E covers the deterministic surface; the final gate is human judgement.                                                                                                                 | Subjective UX gate by definition.                                                                       | Per release.              |
| D3  | ⏳     | **Set OTLP auth header secret**: `fly secrets set OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic\ <base64>` against both `tau-api-staging` and `tau-api`. Combine with code-level S1 (adding `OTEL_EXPORTER_OTLP_ENDPOINT` to `fly.*.toml`) so Grafana Cloud actually receives traces + logs. **`flyctl secrets list` on both apps confirms neither key is set.**                                                                                                                | Secret material; `fly secrets set` writes to Fly's secret store, not the IaC stack.                     | One-time + on rotation.   |
| D4  | ⚠️     | **Provision Grafana Cloud datasources** with UIDs `prometheus`, `loki`, `tempo`, `postgresql`, `redis`; install the `redis-datasource` plugin (admin-only); add Fly Prometheus federation as a Prometheus scrape job.                                                                                                                                                                                                                                                            | Grafana Cloud admin UI is the only programmatic surface for plugin install + datasource UID assignment. | One-time per environment. |
| D5  | ⏳     | **Add `GRAFANA_URL` + `GRAFANA_API_KEY`** to GitHub `staging` + `production` environments so the dashboards-as-code sync step (CI) can run. Confirmed: `gh api …/environments/{staging,production}/secrets` returns only `FLY_API_TOKEN` — Grafana secrets absent.                                                                                                                                                                                                               | Environment-scoped GH secrets; cannot live at repo scope without leaking across environments.           | One-time per environment. |
| D6  | ⏳     | **Deploy the Fly Log Shipper app** with Loki sink secrets so platform-level Fly logs (machine lifecycle, builder events) reach Grafana Cloud. `flyctl apps list` shows no log-shipper app.                                                                                                                                                                                                                                                                                       | Requires Fly app creation + secret population per env.                                                  | One-time per environment. |
| D7  | ✅     | **Sweep stale Netlify dashboard env vars** on the `taucad-staging` site (legacy `TAU_WEBSOCKET_URL=wss://api.tau.new` from the pre-migration era, etc.). Confirmed via `netlify api getEnvVars`: staging now has `TAU_WEBSOCKET_URL=wss://api.taucad.dev` (`all` contexts); prod-us has `production`-scoped `wss://api.tau.new` only. No mismatch.                                                                                                                               | Audit-only; requires Netlify admin.                                                                     | One-time post-cutover.    |

### F. Cloudflare R2 + Publications go-live gates (module.cloudflare_r2 + module.fly_api_secrets)

These gates must complete in order before the Publications capability goes live on staging and prod-us. Execute F1–F7 on staging first; repeat F3–F7 on prod-us after ≥24h soak.

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Why it cannot be scripted                                                                                                                                         | Frequency                                                  |
| --- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| F1  | ⏳     | **Expand the HCP `cloudflare_api_token` scope** to add `Account → Workers R2 Storage:Edit` + `Account → API Tokens:Edit` + `Zone → Cache Rules:Edit` on both zones (`taucad.dev` + `tau.new`). Without these three groups the first apply will 403 on `cloudflare_r2_bucket`, `cloudflare_api_token.r2_rw`, and `cloudflare_ruleset.cache`.                                                                                                                                                                                                                                                        | Cloudflare API token scope is an interactive dashboard action; scopes must be set correctly (over-broad or missing both 403).                                     | One-time; repeat on rotation.                              |
| F1a | ⏳     | **Seed `fly_api_token` Sensitive variable** on both cloud workspaces — `tau-cloud-staging` (copy from `tau-repo` → `fly_api_token_staging`) and `tau-cloud-prod-us` (copy from `tau-repo` → `fly_api_token_production`). Required by `module.fly_api_secrets` to push R2 credentials to the Fly app via the Machines API; the variable is declared `sensitive = true` with no default in both stack `variables.tf`, so F4 will halt with `Error: No value for required variable` without this seed. Alternative: lift into an HCP Variable Set scoped to both cloud workspaces.                    | HCP-encrypted variable storage is the audit boundary; secret material is never committed. Per-workspace seed matches the existing `cloudflare_api_token` pattern. | One-time per workspace + on rotation.                      |
| F2  | ✅     | **`cloudflare_account_id = "271cffba9907a76320b6a4993f756601"`** already committed to both stacks' `terraform.auto.tfvars`. Non-sensitive; done.                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                                                                                                                                                                 | Done.                                                      |
| F3  | ⏳     | **Run `terraform init -upgrade`** (or trigger an HCP "Start new run" with provider re-resolve) on each cloud stack so the lockfile pulls the `cloudflare_r2_*` + `cloudflare_ruleset` resource types. No new provider — same `cloudflare/cloudflare` v5 binary; `init -upgrade` resolves new resource schemas.                                                                                                                                                                                                                                                                                     | Lockfile re-pin is per-workspace.                                                                                                                                 | Once per new provider or schema bump.                      |
| F4  | ⏳     | **HCP apply on `tau-cloud-staging`** — `module.cloudflare_r2` provisions the bucket + CORS + lifecycle + custom domain + cache rules + API token; `module.fly_api_secrets` pushes `TAU_S3_ENDPOINT` + `TAU_S3_ACCESS_KEY_ID` + `TAU_S3_SECRET_ACCESS_KEY` + `TAU_S3_BUCKET` as Fly secrets to `tau-api-staging`. After apply: `dig cdn.taucad.dev` returns Cloudflare-proxied A/AAAA; `curl -I https://cdn.taucad.dev/defaults/og.png` returns 200 after F5.                                                                                                                                       | HCP apply gate; requires F1 + F1a + F3 to land first.                                                                                                             | Per release touching R2 module inputs.                     |
| F4a | ⏳     | **One-time deposed-resource cleanup after the fly-secrets v2 migration**: the first apply that lands `module.fly_api_secrets`' v2 `terracurl_request.secrets` (`fly-secrets-v2-${var.fly_app}`) leaves the v1 predecessor deposed because its frozen `destroy_url` (`/secrets/unset`) 404s and is not in the v1 state's `destroy_response_codes` set. Remove the deposed entry from each cloud workspace via the HCP UI Resources tab → "Remove from state" (or `terraform state rm` against the workspace). Skip on `tau-cloud-prod-us` if it has never applied a v1 `terracurl_request.secrets`. | The deposed entry has frozen state; it cannot be repaired in-place. State surgery is the only path.                                                               | One-time per workspace that previously held a v1 resource. |
| F5  | ⏳     | **Run `bash scripts/seed-r2-defaults.sh`** against the staging bucket (`TAU_S3_BUCKET=tau-staging-content` + endpoint + API token from HCP outputs). Verify all three keys land: `curl -I https://cdn.taucad.dev/defaults/og.png` (200), `https://cdn.taucad.dev/defaults/thumb.webp` (200), `https://cdn.taucad.dev/__health/probe.txt` (200, no-cache).                                                                                                                                                                                                                                          | Default assets require a human to source the correct images from `infra/seed/`; the probe requires the bucket + credentials to exist (F4 prerequisite).           | One-time per environment. Repeat if bucket is deleted.     |
| F6  | ⏳     | **Restart `tau-api-staging` Fly machines** to pick up the new R2 env shape. `module.fly_api_secrets` already pushed the secrets in F4; a restart makes them visible to the running process. `curl https://api.taucad.dev/health/ready` should return 200 with `details.s3.status = "up"`.                                                                                                                                                                                                                                                                                                          | Fly machine restart is a runtime operator action (safe to trigger via `flyctl machines restart` or wait for next deploy).                                         | After every R2 secret rotation.                            |
| F7  | ⏳     | **Staging end-to-end publish smoke**: open the staging UI at `taucad.dev`, publish a project, confirm `/v/:id` loads blobs from `https://cdn.taucad.dev/blobs/...` with HTTP 200 + `cf-cache-status: HIT` (after a warm-up GET), no CSP violations, no `403` / CORS errors in the browser console. Repeat F4–F7 on `tau-cloud-prod-us` once staging has soaked ≥24h.                                                                                                                                                                                                                               | End-to-end correctness requires human observation and a live Cloudflare edge cache warm-up cycle.                                                                 | Per release.                                               |

### E. Per-incident gates (steady state)

| #   | Action                                                                                                                                                                                                                                                                                                       | Why it cannot be scripted                                                                                                                                                                                | Frequency            |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| E1  | **Per-incident remediation** when the database connectivity probe alert fires (unpause Supabase / rotate role / scale pool / etc.). The structured log hint emitted by `probeDatabaseConnectivity` + `mapPostgresErrorToHint` tells the operator which lever to pull; the lever itself is incident-specific. | Action is incident-specific by definition; the point of R1+R2+R3 in (superseded) `staging-cors-coep-safari-rendering-audit.md` is that the operator action is now a lookup against the log, not a guess. | Per incident.        |
| E2  | **Rotate secrets** in HCP Sensitive variables on schedule: `cloudflare_api_token`, `netlify_api_token`, `posthog_cli_token`, `fly_api_token_*`, `nx_cloud_access_token_*`, OAuth client secrets. After rotation in HCP, click "Start new run" — no resource diff, just credential refresh.                   | HashiCorp's vault model is intentionally human-driven; auto-rotation is not in scope for V1.                                                                                                             | Per rotation policy. |

## Recommendations

Prioritised list of follow-up moves to convert the residual manual surface into either reviewable IaC or accepted "by-design" interactivity. Status badges reflect the [Validation Snapshot](#validation-snapshot-2026-05-14).

| #   | Status | Action                                                                                                                                                                                                                                                                                                                                                                                             | Priority | Effort               | Impact |
| --- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------- | ------ |
| R1  | ✅     | **Execute the in-flight first-apply gates (A1–A5)** so the `prevent_non_git_prod_deploys` 422 closes on prod-us and staging parity lands. Unblocks the GitOps trail-PR flow end-to-end.                                                                                                                                                                                                            | P0       | XS                   | High   |
| R1a | ⏳     | **Execute C2 for `tau.new`** — cut Namecheap NS to the Cloudflare pair already serving `taucad.dev`, then verify `dig NS tau.new` returns the Cloudflare authoritative pair. After propagation (~24-48h), the live apex flips from Fly `taucad` to the Netlify `taucad-prod-us` site (cert C3 already issued); validate B2 (OAuth callbacks) and D2 (smoke) before declaring the cutover complete. | **P0**   | XS (operator) + wait | High   |
| R1b | ⏳     | **Decommission Fly UI prod app `taucad`** after R1a has been stable for ≥1 week — `flyctl apps destroy taucad` + remove `apps/ui/fly.prod.toml`. Closes the parallel UI hosting path.                                                                                                                                                                                                              | P1       | S                    | Med    |
| R1c | ⏳     | **Land OTEL endpoint + auth header (S1 + D3)** — add `OTEL_EXPORTER_OTLP_ENDPOINT` + `OTEL_TRACES_SAMPLER` to `fly.*.toml`, then `fly secrets set OTEL_EXPORTER_OTLP_HEADERS=…` on both API apps. Unblocks Grafana Cloud trace+log ingest.                                                                                                                                                         | P0       | XS                   | High   |
| R1d | ⏳     | **Seed Grafana env secrets (D5)** — `gh secret set GRAFANA_URL` + `GRAFANA_API_KEY --env {staging,production}`. Required precursor for the `pnpm grafana:sync` CI step (S3 in the superseded prod-staging doc).                                                                                                                                                                                    | P1       | XS                   | Med    |
| R2  | ⏳     | **Land `modules/tau-secrets` presence audit** to diff `flyctl secrets list -a <app>` against `expected_fly_secrets` (the V1 list already drafted in the architecture blueprint). Closes the cert↔app pairing defense in IaC. Removes one "by-design" gap.                                                                                                                                          | P1       | S                    | Med    |
| R3  | ✅     | **Land `modules/cloudflare-r2` + `modules/fly-secrets`** — single-bucket R2 per env attached to `cdn.<zone>` natively via `cloudflare_r2_custom_domain`; Fly R2 secrets pushed via `terracurl_request`. Closes M1/M2/M5 of the Sharing MVP runbook as IaC. Operator must still execute F1–F7 below.                                                                                                | P1       | M                    | High   |
| R4  | ⏳     | **Migrate from `terracurl_request` to a first-party `netlify_site` resource** if/when the Netlify provider gains `repoInfo` coverage (post-1.0). The current shim is correct but lacks a read-side mirror for drift detection.                                                                                                                                                                     | P3       | S                    | Low    |
| R5  | ⏳     | **Adopt Terraform Stacks (`.tfcomponent.hcl` + `.tfdeploy.hcl`)** to consolidate the per-env root modules into a single declarative stack with deployment-block inputs. Required precursor to adding `prod-eu` without code duplication. Tracked as V2 of the architecture blueprint.                                                                                                              | P2       | L                    | High   |
| R6  | ⏳     | **Author `docs/policy/iac-policy.md`** once R5 lands — extract the stable rules (deployment-input shape, secret-handling contract, deployment-group convention, "never `is_prod`" rule, repo-link via `terracurl_request` until provider parity) into a binding policy.                                                                                                                            | P2       | S                    | Med    |
| R7  | ⏳     | **Add an OPA/Conftest policy gate** in `.github/workflows/policy-check.yml` against `terraform show -json plan` to enforce invariants (no public bucket ACLs, no destroys on production stacks without manual approval, no `is_prod` boolean anywhere).                                                                                                                                            | P2       | M                    | Med    |
| R8  | 🟡     | **Migrate Fly secret seeding into Terraform** — `module.fly_api_secrets` (using `terracurl_request`) now pushes R2 credentials (D-class R2 slice ✅). The OTEL credential slice (D3–D6) remains ⏳; `modules/fly-secrets` is the reusable pattern.                                                                                                                                                 | P3       | M                    | Med    |

## Superseded Documents

The following research documents catalogued partial subsets of this audit and are superseded by this consolidated doc. Their frontmatter has been updated with `status: superseded` + `superseded_by:`.

| Document                                             | What it covered                                                                                      | Why superseded                                                                                                                                                                                                                                                                       |
| ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `docs/research/iac-terraform-strategy.md`            | Full HCP Terraform Stacks architecture blueprint (Fly + Stripe + Netlify + Supabase + R2 + Grafana). | V1 landed in narrower form (Cloudflare DNS + Netlify only); the operator gates from its Appendix C bootstrap are absorbed here. The unrealised V2+ scope (R2 + Supabase + Stripe + Grafana provider work + Stacks adoption) is captured as R3, R5, R6, R7 above.                     |
| `docs/research/netlify-ui-deployment-strategy.md`    | Netlify cutover strategy + the M1–M5 gap analysis of inherent manual operator steps.                 | Code-complete; M1–M5 are absorbed into B1, B2, C2, C3, D2 of this audit. The operational narrative for the Netlify site itself lives in `repos/tau-cloud/docs/netlify-iac-runbook.md` and `docs/architecture/ui-deployment-topology.md`.                                           |
| `docs/research/prod-staging-ui-deployment-status.md` | Cross-document audit reconciling Netlify + observability + Docker + COI residuals.                   | The tau-cloud residuals (M1–M14) are absorbed here; the orthogonal scriptable observability tasks (S1, S3, S4, S11–S13) stay tracked by `production-observability-readiness.md` and `observability-implementation-status.md` as code-level gaps, not tau-cloud-activation gates. |

## References

- Runbooks: `repos/tau-cloud/docs/netlify-iac-runbook.md`, `repos/tau-cloud/docs/dns-migration-plan.md`, `repos/tau-cloud/docs/github-repository-stack.md`, `repos/tau-cloud/docs/runbook.md`
- Architecture: `docs/architecture/production-gitops-runbook.md`, `docs/architecture/ui-deployment-topology.md`, `docs/architecture/netlify-multi-region-functions.md`
- In-flight plan: `fix_netlify_prevent_non-git_prod_deploys` (terracurl_request shim for repo linkage)
- Superseded research: `docs/research/iac-terraform-strategy.md`, `docs/research/netlify-ui-deployment-strategy.md`, `docs/research/prod-staging-ui-deployment-status.md`
- Provider sources: [`devops-rob/terracurl`](https://registry.terraform.io/providers/devops-rob/terracurl/latest), [`netlify/netlify@0.4.1`](https://registry.terraform.io/providers/netlify/netlify/latest), [`cloudflare/cloudflare` v5+](https://registry.terraform.io/providers/cloudflare/cloudflare/latest), [`integrations/github`](https://registry.terraform.io/providers/integrations/github/latest)
- Netlify GitHub App: <https://github.com/apps/netlify>
- HCP Terraform: [Pricing / RUM](https://developer.hashicorp.com/hcp/docs/hcp/admin/billing/pricing-definitions), [Variable Sets](https://developer.hashicorp.com/terraform/cloud-docs/variables)
