import type { ReactElement } from 'react';
import { RuntimeDemoClientEntry } from './runtime-demo-client-entry';

export default function Page(): ReactElement {
  return <RuntimeDemoClientEntry />;
}
