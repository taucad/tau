/**
 * Full assembly export (spec Section 4): exactly the 650 T-CENSUS
 * occurrences at the modeled phase; no root occurrence node.
 */
import { buildEngine } from '../lib/assembly.js';

export default function main() {
  return buildEngine();
}
