{
  "name": "agent-host",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/agent-host",
  "projectType": "library",
  "tags": ["scope:shared", "type:package"],
  "targets": {
    "test:browser": {
      "command": "vitest run --config vitest.browser.config.ts",
      "options": {
        "cwd": "{projectRoot}"
      },
      "cache": false
    }
  }
}
