{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tau.new/schema/geospec-engine-provenance.schema.json",
  "title": "GeoSpec engine release provenance",
  "description": "Provenance record published with every @taucad/geospec-engine release. Records the release date, the license the release ships under, and the date that release converts to its Apache-2.0 future license (charter DL4).",
  "type": "object",
  "additionalProperties": false,
  "required": ["package", "version", "releaseDate", "license", "futureLicense", "apacheConversionDate", "artifacts"],
  "properties": {
    "package": {
      "const": "@taucad/geospec-engine"
    },
    "version": {
      "description": "SemVer version of the release this record describes.",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)*$"
    },
    "releaseDate": {
      "description": "UTC date the release was made available (ISO 8601 date).",
      "type": "string",
      "format": "date"
    },
    "license": {
      "const": "FSL-1.1-Apache-2.0"
    },
    "futureLicense": {
      "const": "Apache-2.0"
    },
    "apacheConversionDate": {
      "description": "releaseDate + 2 years. On and after this date the release may also be used under the Apache-2.0 future license.",
      "type": "string",
      "format": "date"
    },
    "artifacts": {
      "description": "Every published artifact in the release, with its digest.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "sha256"],
        "properties": {
          "path": {
            "description": "Path of the artifact within the published package.",
            "type": "string",
            "minLength": 1
          },
          "sha256": {
            "description": "Lowercase hex SHA-256 digest of the artifact bytes.",
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          },
          "bytes": {
            "type": "integer",
            "minimum": 0
          }
        }
      }
    }
  }
}
