/* oxlint-disable no-barrel-files/no-barrel-files -- public package entry */

export { esbuild, esbuild as plugin } from '#esbuild.plugin.js';

export { esbuildBundler, esbuildOptionsSchema } from '#esbuild.bundler.js';
export type { EsbuildOptions } from '#esbuild.bundler.js';
