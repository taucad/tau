/* oxlint-disable no-barrel-files/no-barrel-files -- public package entry */

export { manifold, manifold as plugin } from '#manifold.plugin.js';

export { manifoldKernel } from '#manifold.kernel.js';
export type { ManifoldOptions } from '#manifold.kernel.js';
