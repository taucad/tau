# openrscad — Math functions

21 top-level symbols. Signatures are verbatim openscad.

// Sine (degrees)
sin(deg)

// Cosine (degrees)
cos(deg)

// Tangent (degrees)
tan(deg)

// Arcsine, in degrees
asin(x)

// Arccosine, in degrees
acos(x)

// Arctangent, in degrees
atan(x)

// Two-argument arctangent, in degrees
atan2(y, x)

// Absolute value
abs(x)

// -1, 0, or 1 by sign of `x`
sign(x)

// Round down to an integer
floor(x)

// Round up to an integer
ceil(x)

// Round to the nearest integer
round(x)

// Square root
sqrt(x)

// Exponentiation
pow(base, exp)

// e raised to `x`
exp(x)

// Natural logarithm
ln(x)

// Base-10 logarithm
log(x)

// Smallest of the arguments
min(a, b, ...) | min(vector)

// Largest of the arguments
max(a, b, ...) | max(vector)

// Euclidean length of a vector
norm(v)

// Cross product of two 3-vectors
cross(a, b)
