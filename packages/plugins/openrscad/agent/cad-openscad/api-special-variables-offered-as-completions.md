# openrscad — Special variables (offered as completions)

10 top-level symbols. Signatures are verbatim openscad.

// Fixed number of fragments in a circle
$fn

// Minimum angle per fragment (degrees)
$fa

// Minimum fragment size
$fs

// Animation time, 0→1
$t

// Number of active user-module instantiations
$parent_modules

// True during F5 preview, false during F6 render
$preview

// Viewport rotation `[x,y,z]`
$vpr

// Viewport translation `[x,y,z]`
$vpt

// Viewport camera distance
$vpd

// Viewport field of view
$vpf
