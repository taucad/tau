# Changelog

## 0.1.0-beta.0

- Added the native C# runtime plugin, CoreCLR worker, desktop resource packaging, trust lifecycle, and GLB/topology export.
- Made standard PicoGK `Library.Go` programs the authoring contract through an upstream-neutral hosted viewer seam.
- Added byte-pinned PicoGK Examples, ShapeKernel, HelixHeatX, and RoverWheel compatibility corpora with native runtime coverage.
- Added stable component identities so each render settles once on the authoritative terminal GLB (protocol v4; the v3 reset/delta scene delivery and its bookmarks were removed before release).
