# kcl-std — std.sweep

3 top-level symbols. Signatures are verbatim kcl.

// Local/relative to the trajectory curve
sweep::TRAJECTORY: string

// Local/relative to a position centered within the plane being sketched on
sweep::SKETCH_PLANE: string

sweep
