{
  "name": "react",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/react",
  "projectType": "library",
  "tags": ["scope:shared", "type:package-veneer"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "oxlint -c $NX_WORKSPACE_ROOT/.oxlintrc.json src && eslint src"
      }
    }
  }
}
