export {
  fromRenderBounds,
  fromRenderPlane,
  fromRenderPoint,
  resolveCoordinateTransform,
  resolveMetersPerRenderUnit,
  shouldRebaseRenderFrame,
  shouldRescaleRenderFrame,
  toRenderBounds,
  toRenderPlane,
  toRenderPoint,
} from '#spatial-domain.js';
export type {
  CoordinateConvention,
  CoordinateTransform,
  RenderFrame,
  RenderPlane,
  SignedWorldAxis,
  SpatialBounds,
  SpatialMatrix,
  SpatialPlane,
  SpatialQuaternion,
  SpatialVector,
} from '#spatial-domain.js';
