diff --git a/dist/index.js b/dist/index.js
index d4058b1f4606e151e19d8dc660e947826d41a164..e4b76c6e072d8671e93741f9b4debc89218b3203 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -247,7 +247,8 @@ function processModelChunk(chunk, state, controller) {
       state.started = true;
     }
   }
-  const reasoning = extractReasoningFromContentBlocks(chunk) || extractReasoningFromValuesMessage(chunk);
+  const reasoningResult = extractReasoningFromContentBlocks(chunk);
+  const reasoning = (reasoningResult == null ? void 0 : reasoningResult.text) ?? extractReasoningFromValuesMessage(chunk);
   if (reasoning) {
     if (!state.reasoningStarted) {
       state.reasoningMessageId = state.messageId;
@@ -375,6 +376,19 @@ function extractReasoningId(msg) {
   }
   return void 0;
 }
+function peekReasoningSummaryIndex(kwargs) {
+  const additionalKwargs = kwargs.additional_kwargs;
+  const reasoning = additionalKwargs === null || additionalKwargs === void 0 ? void 0 : additionalKwargs.reasoning;
+  if (reasoning === null || typeof reasoning !== "object") return void 0;
+  const summary = reasoning.summary;
+  if (!Array.isArray(summary)) return void 0;
+  for (const summaryItem of summary) {
+    if (summaryItem !== null && typeof summaryItem === "object" && "index" in summaryItem && typeof summaryItem.index === "number") {
+      return summaryItem.index;
+    }
+  }
+  return void 0;
+}
 function extractReasoningFromContentBlocks(msg) {
   if (msg == null || typeof msg !== "object") return void 0;
   const msgObj = msg;
@@ -384,27 +398,43 @@ function extractReasoningFromContentBlocks(msg) {
     const reasoningParts = [];
     for (const block of contentBlocks) {
       if (isReasoningContentBlock(block)) {
-        reasoningParts.push(block.reasoning);
+        if (block.reasoning) {
+          reasoningParts.push(block.reasoning);
+        }
       } else if (isThinkingContentBlock(block)) {
-        reasoningParts.push(block.thinking);
+        if (block.thinking) {
+          reasoningParts.push(block.thinking);
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return { text: reasoningParts.join("\n\n"), summaryIndex: peekReasoningSummaryIndex(kwargs) };
     }
   }
   const additionalKwargs = kwargs.additional_kwargs;
   if ((additionalKwargs == null ? void 0 : additionalKwargs.reasoning) && Array.isArray(additionalKwargs.reasoning.summary)) {
     const reasoningParts = [];
+    let summaryIndex;
     for (const summaryItem of additionalKwargs.reasoning.summary) {
       if (typeof summaryItem === "object" && summaryItem !== null && "text" in summaryItem && typeof summaryItem.text === "string") {
-        reasoningParts.push(summaryItem.text);
+        const text = summaryItem.text;
+        if (!text) {
+          continue;
+        }
+        reasoningParts.push(text);
+        if (summaryIndex === void 0 && "index" in summaryItem && typeof summaryItem.index === "number") {
+          summaryIndex = summaryItem.index;
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return { text: reasoningParts.join(""), summaryIndex };
     }
   }
+  const providerReasoningContent = additionalKwargs == null ? void 0 : additionalKwargs.reasoning_content;
+  if (typeof providerReasoningContent === "string" && providerReasoningContent) {
+    return { text: providerReasoningContent, summaryIndex: peekReasoningSummaryIndex(kwargs) };
+  }
   return void 0;
 }
 function extractReasoningFromValuesMessage(msg) {
@@ -427,7 +457,7 @@ function extractReasoningFromValuesMessage(msg) {
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return reasoningParts.join("\n\n");
     }
   }
   const additionalKwargs = kwargs.additional_kwargs;
@@ -439,9 +469,13 @@ function extractReasoningFromValuesMessage(msg) {
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return reasoningParts.join("\n\n");
     }
   }
+  const providerReasoningContent = additionalKwargs == null ? void 0 : additionalKwargs.reasoning_content;
+  if (typeof providerReasoningContent === "string" && providerReasoningContent) {
+    return providerReasoningContent;
+  }
   return void 0;
 }
 function isImageGenerationOutput(obj) {
@@ -453,8 +487,28 @@ function extractImageOutputs(additionalKwargs) {
   if (!Array.isArray(toolOutputs)) return [];
   return toolOutputs.filter(isImageGenerationOutput);
 }
+function resolveTrailingAssistantMessageId(messages) {
+  var last = messages.at(-1);
+  if (last == null || typeof last !== "object") {
+    return void 0;
+  }
+  if (import_messages.AIMessage.isInstance(last) || import_messages.AIMessageChunk.isInstance(last)) {
+    return getMessageId(last);
+  }
+  if (isPlainMessageObject(last)) {
+    const obj = last;
+    if (obj.type === "ai") {
+      return getMessageId(last);
+    }
+    const isSerializedAi = obj.type === "constructor" && Array.isArray(obj.id) && (obj.id.includes("AIMessageChunk") || obj.id.includes("AIMessage"));
+    if (isSerializedAi) {
+      return getMessageId(last);
+    }
+  }
+  return void 0;
+}
 function processLangGraphEvent(event, state, controller) {
-  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
+  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z;
   const {
     messageSeen,
     messageConcat,
@@ -463,7 +517,9 @@ function processLangGraphEvent(event, state, controller) {
     emittedReasoningIds,
     messageReasoningIds,
     toolCallInfoByIndex,
-    emittedToolCallsByKey
+    emittedToolCallsByKey,
+    messageSummaryIndices,
+    streamedReasoningMessageIds
   } = state;
   const [type, data] = parseLangGraphEvent(event);
   switch (type) {
@@ -539,6 +595,13 @@ function processLangGraphEvent(event, state, controller) {
         }
         const toolCallChunks = dataSource.tool_call_chunks;
         if (toolCallChunks == null ? void 0 : toolCallChunks.length) {
+          _y = messageSeen[msgId];
+          if ((_y == null ? void 0 : _y.reasoning) && (_y == null ? void 0 : _y.reasoningEnded) !== true) {
+            controller.enqueue({ type: "reasoning-end", id: msgId });
+            (_z = messageSeen[msgId]) != null ? _z : messageSeen[msgId] = {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
           for (const toolCallChunk of toolCallChunks) {
             const idx = (_a = toolCallChunk.index) != null ? _a : 0;
             if (toolCallChunk.id) {
@@ -582,23 +645,40 @@ function processLangGraphEvent(event, state, controller) {
           }
           emittedReasoningIds.add(chunkReasoningId);
         }
-        const reasoning = extractReasoningFromContentBlocks(msg);
-        if (reasoning) {
+        const reasoningResult = extractReasoningFromContentBlocks(msg);
+        if (reasoningResult) {
           const reasoningId = (_s = (_r = messageReasoningIds[msgId]) != null ? _r : chunkReasoningId) != null ? _s : msgId;
           if (!((_t = messageSeen[msgId]) == null ? void 0 : _t.reasoning)) {
             controller.enqueue({ type: "reasoning-start", id: msgId });
             (_u = messageSeen[msgId]) != null ? _u : messageSeen[msgId] = {};
             messageSeen[msgId].reasoning = true;
+            messageSeen[msgId].reasoningEnded = false;
+            streamedReasoningMessageIds.add(msgId);
+          }
+          let deltaText = reasoningResult.text;
+          if (reasoningResult.summaryIndex !== void 0) {
+            const prevIndex = messageSummaryIndices[msgId];
+            if (prevIndex !== void 0 && reasoningResult.summaryIndex !== prevIndex) {
+              deltaText = "\n\n" + deltaText;
+            }
+            messageSummaryIndices[msgId] = reasoningResult.summaryIndex;
           }
           controller.enqueue({
             type: "reasoning-delta",
-            delta: reasoning,
+            delta: deltaText,
             id: msgId
           });
           emittedReasoningIds.add(reasoningId);
         }
         const text = getMessageText(msg);
         if (text) {
+          _y = messageSeen[msgId];
+          if ((_y == null ? void 0 : _y.reasoning) && (_y == null ? void 0 : _y.reasoningEnded) !== true) {
+            controller.enqueue({ type: "reasoning-end", id: msgId });
+            (_z = messageSeen[msgId]) != null ? _z : messageSeen[msgId] = {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
           if (!((_v = messageSeen[msgId]) == null ? void 0 : _v.text)) {
             controller.enqueue({ type: "text-start", id: msgId });
             (_w = messageSeen[msgId]) != null ? _w : messageSeen[msgId] = {};
@@ -656,7 +736,7 @@ function processLangGraphEvent(event, state, controller) {
             }
           }
         }
-        if (seen.reasoning) {
+        if (seen.reasoning && !seen.reasoningEnded) {
           controller.enqueue({ type: "reasoning-end", id });
         }
         delete messageSeen[id];
@@ -666,6 +746,7 @@ function processLangGraphEvent(event, state, controller) {
       if (data != null && typeof data === "object" && "messages" in data) {
         const messages = data.messages;
         if (Array.isArray(messages)) {
+          const trailingAssistantMessageId = resolveTrailingAssistantMessageId(messages);
           const completedToolCallIds = /* @__PURE__ */ new Set();
           for (const msg of messages) {
             if (!msg || typeof msg !== "object") continue;
@@ -740,21 +821,22 @@ function processLangGraphEvent(event, state, controller) {
                 }
               }
             }
-            const reasoningId = extractReasoningId(msg);
-            const wasStreamedThisRequest = !!messageSeen[msgId];
+            const reasoningBlockId = extractReasoningId(msg);
+            const isTrailingAssistant = msgId === trailingAssistantMessageId;
+            const wasStreamedThisRequest = streamedReasoningMessageIds.has(msgId);
             const hasToolCalls = toolCalls && toolCalls.length > 0;
-            const shouldEmitReasoning = reasoningId && !emittedReasoningIds.has(reasoningId) && (wasStreamedThisRequest || !hasToolCalls);
+            const shouldEmitReasoning = reasoningBlockId && !emittedReasoningIds.has(reasoningBlockId) && isTrailingAssistant && (wasStreamedThisRequest || !hasToolCalls);
             if (shouldEmitReasoning) {
-              const reasoning = extractReasoningFromValuesMessage(msg);
-              if (reasoning) {
+              const reasoningText = extractReasoningFromValuesMessage(msg);
+              if (reasoningText) {
                 controller.enqueue({ type: "reasoning-start", id: msgId });
                 controller.enqueue({
                   type: "reasoning-delta",
-                  delta: reasoning,
+                  delta: reasoningText,
                   id: msgId
                 });
                 controller.enqueue({ type: "reasoning-end", id: msgId });
-                emittedReasoningIds.add(reasoningId);
+                emittedReasoningIds.add(reasoningBlockId);
               }
             }
           }
@@ -865,8 +947,8 @@ function processStreamEventsEvent(event, state, controller) {
         if (chunkId) {
           state.messageId = chunkId;
         }
-        const reasoning = extractReasoningFromContentBlocks(chunk);
-        if (reasoning) {
+        const reasoningResult2 = extractReasoningFromContentBlocks(chunk);
+        if (reasoningResult2) {
           if (!state.reasoningStarted) {
             state.reasoningMessageId = state.messageId;
             controller.enqueue({
@@ -878,7 +960,7 @@ function processStreamEventsEvent(event, state, controller) {
           }
           controller.enqueue({
             type: "reasoning-delta",
-            delta: reasoning,
+            delta: reasoningResult2.text,
             id: (_a = state.reasoningMessageId) != null ? _a : state.messageId
           });
         }
@@ -964,7 +1046,9 @@ function toUIMessageStream(stream, callbacks) {
     messageReasoningIds: {},
     toolCallInfoByIndex: {},
     currentStep: null,
-    emittedToolCallsByKey: /* @__PURE__ */ new Map()
+    emittedToolCallsByKey: /* @__PURE__ */ new Map(),
+    messageSummaryIndices: {},
+    streamedReasoningMessageIds: /* @__PURE__ */ new Set()
   };
   let streamType = null;
   const getAsyncIterator = () => {
diff --git a/dist/index.mjs b/dist/index.mjs
index c3a7a8445d322a160b82f1d84a6f90ddb2aba854..d067a62d394eff536dbaecf9a5870284d058a67e 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -227,7 +227,8 @@ function processModelChunk(chunk, state, controller) {
       state.started = true;
     }
   }
-  const reasoning = extractReasoningFromContentBlocks(chunk) || extractReasoningFromValuesMessage(chunk);
+  const reasoningResult = extractReasoningFromContentBlocks(chunk);
+  const reasoning = (reasoningResult == null ? void 0 : reasoningResult.text) ?? extractReasoningFromValuesMessage(chunk);
   if (reasoning) {
     if (!state.reasoningStarted) {
       state.reasoningMessageId = state.messageId;
@@ -355,6 +356,19 @@ function extractReasoningId(msg) {
   }
   return void 0;
 }
+function peekReasoningSummaryIndex(kwargs) {
+  const additionalKwargs = kwargs.additional_kwargs;
+  const reasoning = additionalKwargs === null || additionalKwargs === void 0 ? void 0 : additionalKwargs.reasoning;
+  if (reasoning === null || typeof reasoning !== "object") return void 0;
+  const summary = reasoning.summary;
+  if (!Array.isArray(summary)) return void 0;
+  for (const summaryItem of summary) {
+    if (summaryItem !== null && typeof summaryItem === "object" && "index" in summaryItem && typeof summaryItem.index === "number") {
+      return summaryItem.index;
+    }
+  }
+  return void 0;
+}
 function extractReasoningFromContentBlocks(msg) {
   if (msg == null || typeof msg !== "object") return void 0;
   const msgObj = msg;
@@ -364,25 +378,37 @@ function extractReasoningFromContentBlocks(msg) {
     const reasoningParts = [];
     for (const block of contentBlocks) {
       if (isReasoningContentBlock(block)) {
-        reasoningParts.push(block.reasoning);
+        if (block.reasoning) {
+          reasoningParts.push(block.reasoning);
+        }
       } else if (isThinkingContentBlock(block)) {
-        reasoningParts.push(block.thinking);
+        if (block.thinking) {
+          reasoningParts.push(block.thinking);
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return { text: reasoningParts.join("\n\n"), summaryIndex: peekReasoningSummaryIndex(kwargs) };
     }
   }
   const additionalKwargs = kwargs.additional_kwargs;
   if ((additionalKwargs == null ? void 0 : additionalKwargs.reasoning) && Array.isArray(additionalKwargs.reasoning.summary)) {
     const reasoningParts = [];
+    let summaryIndex;
     for (const summaryItem of additionalKwargs.reasoning.summary) {
       if (typeof summaryItem === "object" && summaryItem !== null && "text" in summaryItem && typeof summaryItem.text === "string") {
-        reasoningParts.push(summaryItem.text);
+        const text = summaryItem.text;
+        if (!text) {
+          continue;
+        }
+        reasoningParts.push(text);
+        if (summaryIndex === void 0 && "index" in summaryItem && typeof summaryItem.index === "number") {
+          summaryIndex = summaryItem.index;
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return { text: reasoningParts.join(""), summaryIndex };
     }
   }
   return void 0;
@@ -407,7 +433,7 @@ function extractReasoningFromValuesMessage(msg) {
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return reasoningParts.join("\n\n");
     }
   }
   const additionalKwargs = kwargs.additional_kwargs;
@@ -419,7 +445,7 @@ function extractReasoningFromValuesMessage(msg) {
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join("");
+      return reasoningParts.join("\n\n");
     }
   }
   return void 0;
@@ -433,8 +459,28 @@ function extractImageOutputs(additionalKwargs) {
   if (!Array.isArray(toolOutputs)) return [];
   return toolOutputs.filter(isImageGenerationOutput);
 }
+function resolveTrailingAssistantMessageId(messages) {
+  var last = messages.at(-1);
+  if (last == null || typeof last !== "object") {
+    return void 0;
+  }
+  if (AIMessage.isInstance(last) || AIMessageChunk.isInstance(last)) {
+    return getMessageId(last);
+  }
+  if (isPlainMessageObject(last)) {
+    const obj = last;
+    if (obj.type === "ai") {
+      return getMessageId(last);
+    }
+    const isSerializedAi = obj.type === "constructor" && Array.isArray(obj.id) && (obj.id.includes("AIMessageChunk") || obj.id.includes("AIMessage"));
+    if (isSerializedAi) {
+      return getMessageId(last);
+    }
+  }
+  return void 0;
+}
 function processLangGraphEvent(event, state, controller) {
-  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
+  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z;
   const {
     messageSeen,
     messageConcat,
@@ -443,7 +489,9 @@ function processLangGraphEvent(event, state, controller) {
     emittedReasoningIds,
     messageReasoningIds,
     toolCallInfoByIndex,
-    emittedToolCallsByKey
+    emittedToolCallsByKey,
+    messageSummaryIndices,
+    streamedReasoningMessageIds
   } = state;
   const [type, data] = parseLangGraphEvent(event);
   switch (type) {
@@ -519,6 +567,13 @@ function processLangGraphEvent(event, state, controller) {
         }
         const toolCallChunks = dataSource.tool_call_chunks;
         if (toolCallChunks == null ? void 0 : toolCallChunks.length) {
+          _y = messageSeen[msgId];
+          if ((_y == null ? void 0 : _y.reasoning) && (_y == null ? void 0 : _y.reasoningEnded) !== true) {
+            controller.enqueue({ type: "reasoning-end", id: msgId });
+            (_z = messageSeen[msgId]) != null ? _z : messageSeen[msgId] = {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
           for (const toolCallChunk of toolCallChunks) {
             const idx = (_a = toolCallChunk.index) != null ? _a : 0;
             if (toolCallChunk.id) {
@@ -562,23 +617,40 @@ function processLangGraphEvent(event, state, controller) {
           }
           emittedReasoningIds.add(chunkReasoningId);
         }
-        const reasoning = extractReasoningFromContentBlocks(msg);
-        if (reasoning) {
+        const reasoningResult = extractReasoningFromContentBlocks(msg);
+        if (reasoningResult) {
           const reasoningId = (_s = (_r = messageReasoningIds[msgId]) != null ? _r : chunkReasoningId) != null ? _s : msgId;
           if (!((_t = messageSeen[msgId]) == null ? void 0 : _t.reasoning)) {
             controller.enqueue({ type: "reasoning-start", id: msgId });
             (_u = messageSeen[msgId]) != null ? _u : messageSeen[msgId] = {};
             messageSeen[msgId].reasoning = true;
+            messageSeen[msgId].reasoningEnded = false;
+            streamedReasoningMessageIds.add(msgId);
+          }
+          let deltaText = reasoningResult.text;
+          if (reasoningResult.summaryIndex !== void 0) {
+            const prevIndex = messageSummaryIndices[msgId];
+            if (prevIndex !== void 0 && reasoningResult.summaryIndex !== prevIndex) {
+              deltaText = "\n\n" + deltaText;
+            }
+            messageSummaryIndices[msgId] = reasoningResult.summaryIndex;
           }
           controller.enqueue({
             type: "reasoning-delta",
-            delta: reasoning,
+            delta: deltaText,
             id: msgId
           });
           emittedReasoningIds.add(reasoningId);
         }
         const text = getMessageText(msg);
         if (text) {
+          _y = messageSeen[msgId];
+          if ((_y == null ? void 0 : _y.reasoning) && (_y == null ? void 0 : _y.reasoningEnded) !== true) {
+            controller.enqueue({ type: "reasoning-end", id: msgId });
+            (_z = messageSeen[msgId]) != null ? _z : messageSeen[msgId] = {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
           if (!((_v = messageSeen[msgId]) == null ? void 0 : _v.text)) {
             controller.enqueue({ type: "text-start", id: msgId });
             (_w = messageSeen[msgId]) != null ? _w : messageSeen[msgId] = {};
@@ -636,7 +708,7 @@ function processLangGraphEvent(event, state, controller) {
             }
           }
         }
-        if (seen.reasoning) {
+        if (seen.reasoning && !seen.reasoningEnded) {
           controller.enqueue({ type: "reasoning-end", id });
         }
         delete messageSeen[id];
@@ -646,6 +718,7 @@ function processLangGraphEvent(event, state, controller) {
       if (data != null && typeof data === "object" && "messages" in data) {
         const messages = data.messages;
         if (Array.isArray(messages)) {
+          const trailingAssistantMessageId = resolveTrailingAssistantMessageId(messages);
           const completedToolCallIds = /* @__PURE__ */ new Set();
           for (const msg of messages) {
             if (!msg || typeof msg !== "object") continue;
@@ -720,21 +793,22 @@ function processLangGraphEvent(event, state, controller) {
                 }
               }
             }
-            const reasoningId = extractReasoningId(msg);
-            const wasStreamedThisRequest = !!messageSeen[msgId];
+            const reasoningBlockId = extractReasoningId(msg);
+            const isTrailingAssistant = msgId === trailingAssistantMessageId;
+            const wasStreamedThisRequest = streamedReasoningMessageIds.has(msgId);
             const hasToolCalls = toolCalls && toolCalls.length > 0;
-            const shouldEmitReasoning = reasoningId && !emittedReasoningIds.has(reasoningId) && (wasStreamedThisRequest || !hasToolCalls);
+            const shouldEmitReasoning = reasoningBlockId && !emittedReasoningIds.has(reasoningBlockId) && isTrailingAssistant && (wasStreamedThisRequest || !hasToolCalls);
             if (shouldEmitReasoning) {
-              const reasoning = extractReasoningFromValuesMessage(msg);
-              if (reasoning) {
+              const reasoningText = extractReasoningFromValuesMessage(msg);
+              if (reasoningText) {
                 controller.enqueue({ type: "reasoning-start", id: msgId });
                 controller.enqueue({
                   type: "reasoning-delta",
-                  delta: reasoning,
+                  delta: reasoningText,
                   id: msgId
                 });
                 controller.enqueue({ type: "reasoning-end", id: msgId });
-                emittedReasoningIds.add(reasoningId);
+                emittedReasoningIds.add(reasoningBlockId);
               }
             }
           }
@@ -845,8 +919,8 @@ function processStreamEventsEvent(event, state, controller) {
         if (chunkId) {
           state.messageId = chunkId;
         }
-        const reasoning = extractReasoningFromContentBlocks(chunk);
-        if (reasoning) {
+        const reasoningResult2 = extractReasoningFromContentBlocks(chunk);
+        if (reasoningResult2) {
           if (!state.reasoningStarted) {
             state.reasoningMessageId = state.messageId;
             controller.enqueue({
@@ -858,7 +932,7 @@ function processStreamEventsEvent(event, state, controller) {
           }
           controller.enqueue({
             type: "reasoning-delta",
-            delta: reasoning,
+            delta: reasoningResult2.text,
             id: (_a = state.reasoningMessageId) != null ? _a : state.messageId
           });
         }
@@ -944,7 +1018,9 @@ function toUIMessageStream(stream, callbacks) {
     messageReasoningIds: {},
     toolCallInfoByIndex: {},
     currentStep: null,
-    emittedToolCallsByKey: /* @__PURE__ */ new Map()
+    emittedToolCallsByKey: /* @__PURE__ */ new Map(),
+    messageSummaryIndices: {},
+    streamedReasoningMessageIds: /* @__PURE__ */ new Set()
   };
   let streamType = null;
   const getAsyncIterator = () => {
diff --git a/src/adapter.ts b/src/adapter.ts
index 08c885782c497204982cd0867c3ef66064e242d3..739cd3e4ae045ec52b9179fe1f449541a06a2c05 100644
--- a/src/adapter.ts
+++ b/src/adapter.ts
@@ -177,8 +177,8 @@ function processStreamEventsEvent(
         /**
          * Handle reasoning content from contentBlocks
          */
-        const reasoning = extractReasoningFromContentBlocks(chunk);
-        if (reasoning) {
+        const reasoningResult = extractReasoningFromContentBlocks(chunk);
+        if (reasoningResult) {
           if (!state.reasoningStarted) {
             // Track the ID used for reasoning-start to ensure reasoning-end uses the same ID
             state.reasoningMessageId = state.messageId;
@@ -191,7 +191,7 @@ function processStreamEventsEvent(
           }
           controller.enqueue({
             type: 'reasoning-delta',
-            delta: reasoning,
+            delta: reasoningResult.text,
             id: state.reasoningMessageId ?? state.messageId,
           });
         }
@@ -399,6 +399,8 @@ export function toUIMessageStream<TState = unknown>(
     >,
     currentStep: null as number | null,
     emittedToolCallsByKey: new Map<string, string>(),
+    messageSummaryIndices: {} as Record<string, number>,
+    streamedReasoningMessageIds: new Set<string>(),
   };
 
   /**
diff --git a/src/types.ts b/src/types.ts
index bfc77c63451e573b811f40d1295b2568943f4903..afa7aa17738fc91ef91193b4fd75662796c87fd5 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -7,7 +7,13 @@ export interface LangGraphEventState {
   /** Tracks which message IDs have been seen */
   messageSeen: Record<
     string,
-    { text?: boolean; reasoning?: boolean; tool?: Record<string, boolean> }
+    {
+      text?: boolean;
+      reasoning?: boolean;
+      /** True after {@link processLangGraphEvent} emits `reasoning-end` mid-stream (text/tool transition); avoids double-close in `case 'values'`. */
+      reasoningEnded?: boolean;
+      tool?: Record<string, boolean>;
+    }
   >;
   /** Accumulates message chunks for later reference */
   messageConcat: Record<string, AIMessageChunk>;
@@ -28,6 +34,14 @@ export interface LangGraphEventState {
   currentStep: number | null;
   /** Maps tool call key (name:argsJson) to tool call ID for HITL interrupt handling */
   emittedToolCallsByKey: Map<string, string>;
+  /** Tracks the last-seen summary_index per message ID for newline injection at index boundaries */
+  messageSummaryIndices: Record<string, number>;
+  /**
+   * Message IDs whose reasoning was emitted via `case 'messages'` for this HTTP stream.
+   * Persistent counterpart to the per-step `messageSeen[msgId].reasoning` flag, which is
+   * cleared in `case 'values'` first loop — required for synthetic historical reasoning emission.
+   */
+  streamedReasoningMessageIds: Set<string>;
 }
 
 /**
diff --git a/src/utils.ts b/src/utils.ts
index f70b5d17d9924a9718149b09041a12d8e307dde2..2e44dd1e7c8cc106c8810ea4f55fc99c57fcc7b3 100644
--- a/src/utils.ts
+++ b/src/utils.ts
@@ -444,9 +444,8 @@ export function processModelChunk(
    * For direct model streams, we check both sources since there's no values event
    * that would cause duplication (unlike LangGraph streams)
    */
-  const reasoning =
-    extractReasoningFromContentBlocks(chunk) ||
-    extractReasoningFromValuesMessage(chunk);
+  const reasoningResult = extractReasoningFromContentBlocks(chunk);
+  const reasoning = reasoningResult?.text ?? extractReasoningFromValuesMessage(chunk);
   if (reasoning) {
     if (!state.reasoningStarted) {
       // Track the ID used for reasoning-start to ensure subsequent chunks use the same ID
@@ -559,6 +558,41 @@ export function getMessageId(msg: unknown): string | undefined {
   return undefined;
 }
 
+/**
+ * Returns the message id of the trailing AIMessage in `messages`, or `undefined`
+ * if the last array element is not an AI assistant message (e.g. HumanMessage, ToolMessage).
+ *
+ * Mirrors the AI detection logic used when scanning `messages` in {@link processLangGraphEvent} `case 'values'`.
+ */
+function resolveTrailingAssistantMessageId(
+  messages: readonly unknown[],
+): string | undefined {
+  const last = messages.at(-1);
+  if (last == null || typeof last !== 'object') {
+    return undefined;
+  }
+
+  if (AIMessage.isInstance(last) || AIMessageChunk.isInstance(last)) {
+    return getMessageId(last);
+  }
+
+  if (isPlainMessageObject(last)) {
+    const obj = last as Record<string, unknown>;
+    if (obj.type === 'ai') {
+      return getMessageId(last);
+    }
+    const isSerializedAi =
+      obj.type === 'constructor' &&
+      Array.isArray(obj.id) &&
+      (obj.id.includes('AIMessageChunk') || obj.id.includes('AIMessage'));
+    if (isSerializedAi) {
+      return getMessageId(last);
+    }
+  }
+
+  return undefined;
+}
+
 /**
  * Checks if a message is an AI message chunk (works for both class instances and plain objects).
  * For class instances, only AIMessageChunk is matched (not AIMessage).
@@ -775,6 +809,40 @@ export function extractReasoningId(msg: unknown): string | undefined {
   return undefined;
 }
 
+/**
+ * Reads GPT-5 `summary_index` from `additional_kwargs.reasoning.summary`
+ * alongside V1 `contentBlocks` reasoning so {@link extractReasoningFromContentBlocks}
+ * can expose `summaryIndex` for newline injection between summary parts.
+ *
+ * Prefers the first numeric `index` encountered in `summary`; matches the extractor
+ * used on the fallback branch when there is no `contentBlocks`.
+ */
+function peekReasoningSummaryIndex(kwargs: Record<string, unknown>): number | undefined {
+  const reasoning = (
+    kwargs as {
+      additional_kwargs?: { reasoning?: { summary?: unknown[] } };
+    }
+  ).additional_kwargs?.reasoning;
+  if (reasoning === null || typeof reasoning !== 'object') {
+    return undefined;
+  }
+  const summary = (reasoning as { summary?: unknown }).summary;
+  if (!Array.isArray(summary)) {
+    return undefined;
+  }
+  for (const summaryItem of summary) {
+    if (
+      summaryItem !== null &&
+      typeof summaryItem === 'object' &&
+      'index' in summaryItem &&
+      typeof (summaryItem as { index: unknown }).index === 'number'
+    ) {
+      return (summaryItem as { index: number }).index;
+    }
+  }
+  return undefined;
+}
+
 /**
  * Extracts reasoning content from contentBlocks or additional_kwargs.reasoning.summary
  *
@@ -787,11 +855,11 @@ export function extractReasoningId(msg: unknown): string | undefined {
  * Handles both class instances and serialized LangChain message objects.
  *
  * @param msg - The message to extract reasoning from.
- * @returns The reasoning text if found, undefined otherwise.
+ * @returns The reasoning text and optional summaryIndex if found, undefined otherwise.
  */
 export function extractReasoningFromContentBlocks(
   msg: unknown,
-): string | undefined {
+): { text: string; summaryIndex?: number } | undefined {
   if (msg == null || typeof msg !== 'object') return undefined;
 
   // For serialized LangChain messages, the data is in kwargs
@@ -807,13 +875,20 @@ export function extractReasoningFromContentBlocks(
     const reasoningParts: string[] = [];
     for (const block of contentBlocks) {
       if (isReasoningContentBlock(block)) {
-        reasoningParts.push(block.reasoning);
+        if (block.reasoning) {
+          reasoningParts.push(block.reasoning);
+        }
       } else if (isThinkingContentBlock(block)) {
-        reasoningParts.push(block.thinking);
+        if (block.thinking) {
+          reasoningParts.push(block.thinking);
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join('');
+      return {
+        text: reasoningParts.join('\n\n'),
+        summaryIndex: peekReasoningSummaryIndex(kwargs),
+      };
     }
   }
 
@@ -828,6 +903,7 @@ export function extractReasoningFromContentBlocks(
     Array.isArray(additionalKwargs.reasoning.summary)
   ) {
     const reasoningParts: string[] = [];
+    let summaryIndex: number | undefined;
     for (const summaryItem of additionalKwargs.reasoning.summary) {
       if (
         typeof summaryItem === 'object' &&
@@ -835,11 +911,23 @@ export function extractReasoningFromContentBlocks(
         'text' in summaryItem &&
         typeof (summaryItem as { text: unknown }).text === 'string'
       ) {
-        reasoningParts.push((summaryItem as { text: string }).text);
+        const text = (summaryItem as { text: string }).text;
+        if (!text) {
+          continue;
+        }
+
+        reasoningParts.push(text);
+        if (
+          summaryIndex === undefined &&
+          'index' in summaryItem &&
+          typeof (summaryItem as { index: unknown }).index === 'number'
+        ) {
+          summaryIndex = (summaryItem as { index: number }).index;
+        }
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join('');
+      return { text: reasoningParts.join(''), summaryIndex };
     }
   }
 
@@ -885,7 +973,7 @@ export function extractReasoningFromValuesMessage(
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join('');
+      return reasoningParts.join('\n\n');
     }
   }
 
@@ -909,7 +997,7 @@ export function extractReasoningFromValuesMessage(
       }
     }
     if (reasoningParts.length > 0) {
-      return reasoningParts.join('');
+      return reasoningParts.join('\n\n');
     }
   }
 
@@ -971,6 +1059,8 @@ export function processLangGraphEvent(
     messageReasoningIds,
     toolCallInfoByIndex,
     emittedToolCallsByKey,
+    messageSummaryIndices,
+    streamedReasoningMessageIds,
   } = state;
   const [type, data] = parseLangGraphEvent(event);
 
@@ -1116,6 +1206,22 @@ export function processLangGraphEvent(
           | ToolCallChunk[]
           | undefined;
         if (toolCallChunks?.length) {
+          /**
+           * Close reasoning before tool-input-start when the model transitions
+           * reasoning → tool_calls within the same AIMessageChunk stream.
+           * Without this, reasoning-end is deferred until `case 'values'`, which can
+           * arrive only after the agent step completes (long tool rounds).
+           */
+          if (
+            messageSeen[msgId]?.reasoning &&
+            !messageSeen[msgId]?.reasoningEnded
+          ) {
+            controller.enqueue({ type: 'reasoning-end', id: msgId });
+            messageSeen[msgId] ??= {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
+
           for (const toolCallChunk of toolCallChunks) {
             const idx = toolCallChunk.index ?? 0;
 
@@ -1209,8 +1315,8 @@ export function processLangGraphEvent(
           emittedReasoningIds.add(chunkReasoningId);
         }
 
-        const reasoning = extractReasoningFromContentBlocks(msg);
-        if (reasoning) {
+        const reasoningResult = extractReasoningFromContentBlocks(msg);
+        if (reasoningResult) {
           // Use stored reasoning ID, or current chunk's ID, or fall back to message ID
           const reasoningId =
             messageReasoningIds[msgId] ?? chunkReasoningId ?? msgId;
@@ -1219,12 +1325,27 @@ export function processLangGraphEvent(
             controller.enqueue({ type: 'reasoning-start', id: msgId });
             messageSeen[msgId] ??= {};
             messageSeen[msgId].reasoning = true;
+            messageSeen[msgId].reasoningEnded = false;
+            streamedReasoningMessageIds.add(msgId);
+          }
+
+          let deltaText = reasoningResult.text;
+
+          // Inject paragraph break when summary_index transitions to a new section
+          if (reasoningResult.summaryIndex !== undefined) {
+            const prevIndex = messageSummaryIndices[msgId];
+            if (
+              prevIndex !== undefined &&
+              reasoningResult.summaryIndex !== prevIndex
+            ) {
+              deltaText = '\n\n' + deltaText;
+            }
+            messageSummaryIndices[msgId] = reasoningResult.summaryIndex;
           }
 
-          // Streaming chunks have delta text, emit directly without slicing
           controller.enqueue({
             type: 'reasoning-delta',
-            delta: reasoning,
+            delta: deltaText,
             id: msgId,
           });
           // Also ensure the reasoning ID is marked (handles case where ID wasn't in first chunk)
@@ -1236,6 +1357,21 @@ export function processLangGraphEvent(
          */
         const text = getMessageText(msg);
         if (text) {
+          /**
+           * Close reasoning before text-start when the model transitions
+           * reasoning → assistant text within the same stream (mirrors
+           * {@link processModelChunk}).
+           */
+          if (
+            messageSeen[msgId]?.reasoning &&
+            !messageSeen[msgId]?.reasoningEnded
+          ) {
+            controller.enqueue({ type: 'reasoning-end', id: msgId });
+            messageSeen[msgId] ??= {};
+            messageSeen[msgId].reasoningEnded = true;
+            messageSeen[msgId].reasoning = false;
+          }
+
           if (!messageSeen[msgId]?.text) {
             controller.enqueue({ type: 'text-start', id: msgId });
             messageSeen[msgId] ??= {};
@@ -1315,7 +1451,7 @@ export function processLangGraphEvent(
           }
         }
 
-        if (seen.reasoning) {
+        if (seen.reasoning && !seen.reasoningEnded) {
           controller.enqueue({ type: 'reasoning-end', id });
         }
 
@@ -1331,6 +1467,9 @@ export function processLangGraphEvent(
       if (data != null && typeof data === 'object' && 'messages' in data) {
         const messages = (data as { messages?: unknown[] }).messages;
         if (Array.isArray(messages)) {
+          const trailingAssistantMessageId =
+            resolveTrailingAssistantMessageId(messages);
+
           /**
            * First pass: Collect all tool call IDs that have been responded to by ToolMessages.
            * These are historical tool calls that are already complete.
@@ -1480,47 +1619,39 @@ export function processLangGraphEvent(
             }
 
             /**
-             * Check for reasoning content that wasn't streamed
-             * Use reasoning block ID for deduplication as it's consistent across streaming and values.
+             * Historical reasoning re-emission from LangGraph `values` events.
              *
-             * IMPORTANT: Handle two cases differently:
-             * 1. Message has reasoning WITHOUT tool_calls → emit reasoning (pure reasoning case)
-             * 2. Message has reasoning WITH tool_calls → only emit if streamed this request
-             *    (When resuming from HITL interrupt, historical messages have both reasoning
-             *    AND tool_calls. We skip those to avoid duplicate reasoning entries.)
+             * Matches upstream PR #11417 two-pass pattern for tool calls: only the **trailing**
+             * AIMessage is eligible, and HITL-paused assistant messages (reasoning + tool_calls
+             * that were not streamed this request) are skipped via `streamedReasoningMessageIds`.
+             *
+             * The original gate used `!!messageSeen[msgId]` for "streamed this request", but
+             * `messageSeen` is cleared in the first loop above — always false. We keep a
+             * persistent `streamedReasoningMessageIds` set populated in `case 'messages'`.
              */
-            const reasoningId = extractReasoningId(msg);
-            const wasStreamedThisRequest = !!messageSeen[msgId];
+            const reasoningBlockId = extractReasoningId(msg);
+            const isTrailingAssistant = msgId === trailingAssistantMessageId;
+            const wasStreamedThisRequest =
+              streamedReasoningMessageIds.has(msgId);
             const hasToolCalls = toolCalls && toolCalls.length > 0;
 
-            /**
-             * Determine if we should emit reasoning:
-             * - If we already emitted this reasoning ID, skip
-             * - If the message was streamed this request, emit (normal flow)
-             * - If NOT streamed but has NO tool_calls, emit (pure reasoning in values case)
-             * - If NOT streamed but HAS tool_calls, skip (historical HITL message)
-             */
             const shouldEmitReasoning =
-              reasoningId &&
-              !emittedReasoningIds.has(reasoningId) &&
+              reasoningBlockId &&
+              !emittedReasoningIds.has(reasoningBlockId) &&
+              isTrailingAssistant &&
               (wasStreamedThisRequest || !hasToolCalls);
 
             if (shouldEmitReasoning) {
-              /**
-               * Use extractReasoningFromValuesMessage which extracts from response_metadata.output
-               * This is the full accumulated reasoning, not deltas
-               */
-              const reasoning = extractReasoningFromValuesMessage(msg);
-
-              if (reasoning) {
+              const reasoningText = extractReasoningFromValuesMessage(msg);
+              if (reasoningText) {
                 controller.enqueue({ type: 'reasoning-start', id: msgId });
                 controller.enqueue({
                   type: 'reasoning-delta',
-                  delta: reasoning,
+                  delta: reasoningText,
                   id: msgId,
                 });
                 controller.enqueue({ type: 'reasoning-end', id: msgId });
-                emittedReasoningIds.add(reasoningId);
+                emittedReasoningIds.add(reasoningBlockId);
               }
             }
           }
