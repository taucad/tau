diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index c732f1fa77815c60bae57077a1e00161562548cc..80fcdf96d5b468e5570981d2080171534f97a856 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -441,6 +441,10 @@ export const stream = (model, context, options) => {
                     if (choice?.delta?.tool_calls) {
                         for (const toolCall of choice.delta.tool_calls) {
                             const block = ensureToolCallBlock(toolCall);
+                            const thoughtSignature = toolCall.extra_content?.google?.thought_signature;
+                            if (model.provider === "vertexai" && typeof thoughtSignature === "string") {
+                                block.thoughtSignature = thoughtSignature;
+                            }
                             if (!block.id && toolCall.id) {
                                 block.id = toolCall.id;
                                 toolCallBlocksById.set(toolCall.id, block);
@@ -1033,6 +1037,9 @@ export function convertMessages(model, context, compat, options) {
                             name: tc.name,
                             arguments: JSON.stringify(tc.arguments),
                         },
+                        ...(model.provider === "vertexai" && tc.thoughtSignature !== undefined
+                            ? { extra_content: { google: { thought_signature: tc.thoughtSignature } } }
+                            : {}),
                     };
                 });
             }
