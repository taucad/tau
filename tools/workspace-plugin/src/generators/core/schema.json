{
  "$schema": "https://json-schema.org/schema",
  "$id": "TauCore",
  "title": "Create a @taucad/*-core support package",
  "description": "Scaffolds a publishable non-plugin support package under packages/core/.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Core directory and Nx project name",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the core package short name?",
      "x-priority": "important"
    },
    "packageName": {
      "type": "string",
      "description": "Full npm package name, conventionally @taucad/<name>-core",
      "x-prompt": "What is the full npm package name?",
      "x-priority": "important"
    },
    "description": {
      "type": "string",
      "description": "One-line package description"
    },
    "publishable": {
      "type": "boolean",
      "default": true,
      "description": "Core packages under packages/core are always publishable"
    }
  },
  "required": ["name", "packageName"]
}
