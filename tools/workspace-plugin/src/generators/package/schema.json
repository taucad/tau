{
  "$schema": "https://json-schema.org/schema",
  "$id": "TauPackage",
  "title": "Create a @taucad/* package",
  "description": "Scaffolds a publishable @taucad/* package with an ESM-only build, vitest, and all Tau conventions.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Package name without the @taucad/ prefix (e.g. 'react' → @taucad/react)",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What is the package name (without @taucad/ prefix)?",
      "x-priority": "important"
    },
    "description": {
      "type": "string",
      "description": "Package description for package.json and README",
      "default": ""
    },
    "scope": {
      "type": "string",
      "enum": ["packages", "libs", "apps/libs"],
      "default": "packages",
      "description": "Placement, which derives tags, privacy, and license (packages for publishable Apache surfaces, libs for shared Apache capabilities, apps/libs for private AGPL application capabilities)"
    }
  },
  "required": ["name"]
}
