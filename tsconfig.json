{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "paths": {
            // This path mapping exists to make ShadCN be happy with a default tsconfig.json, enabling seamless installs with the default component install command.
            "~/*": ["./apps/ui/app/*"]
        }
    },
    "include": [
        "eslint.config.ts",
        "vitest.workspace.ts",
        "jest.config.ts",
        "scripts/**/*"
    ]
}
